# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
.venv/

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
*.tmp

# Project specific
*.log
.env*
*.lock
tmp/
output/

# Spec Kit-specific files
.genreleases/
*.zip
.specify/tools/
.migration-backups/

# Node / Playwright skill runtime artifacts
node_modules/
**/.temp-execution-*.js

input/
.qoder/sessions/
.specify/tools/*.json
.specify/instructions.md-*
.specify/teams/.work/

# /speckit.implement long-run mode state (per-spec, transient)
*.local.md

# session export scratch (owner decision 2026-08-15)
.session-export/
# 043: build-embedded source commit (build artifact)
src/specify_cli/_source_commit.json

# sanitize findings ledger (Feature 047): machine-regenerable runtime cache
# (deterministic checkers re-derive findings on every collect); dogfood
# evidence is recorded in the spec's verification.md instead.
.specify/memory/sanitize/
