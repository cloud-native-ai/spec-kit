# Run Report — draw-two-layer-structure

- **Run ID（UTC）**: 2026-09-17T115453Z
- **Team**: `draw-two-layer-structure`（`pattern: serial`，质量优先）
- **Goal（权威定义）**: `.specify/goal/draw-two-layer-structure/goal.md`（`goal_slug` 显式绑定，status `active`，`criteria_count: 3`）
- **Target 指派**: 无（goal 整体）
  - 解析依据：本次未传 `--target`，`team.md` 亦未声明 `focus_target`（`grep -c "focus_target"` = 0）→ `source=none`。五项检查：绑定 goal 命中 archive（engine `list --json` 枚举确认）✓；无悬空引用 ✓；无终态 Target 引用 ✓；无跨 goal 引用 ✓；goal 自身非终态（`active`）✓。
- **Summary 刷新决策**: **不刷新**。门控 = 无可入账的新 goal 级材料（本轮产出落在技能文件与本报告；`.specify/goal/draw-two-layer-structure/summary/` 不写）。
- **本轮用户指令（五条 UX 要求）**: ① 绘图后把图**包装成用户可直接打开的 HTML**；② PlantUML 这类带额外渲染文件的引擎，**源文件（`*.puml`）与图片（svg/jpg）都要保留**；③ 给用户的文字——**图内的与 HTML 说明的**——明确要求 **without jargon, with context**；④ 说明文字**不描述图表本身**（图应自说明：PlantUML 用例图不需要文字说「椭圆代表用例、方框代表对象」）；⑤ 文字应**用用户原始上下文**描述架构或流程。

## 结果摘要

五条要求全部落地为一份**新的单一 owner 文件** + **七处指针接线**，未改动任何引擎的语法与渲染逻辑。

1. **新建 `skills/draw-diagram/references/delivery-contract.md`（91 行）**——交付形态与面向用户文字规则的唯一权威，六条规则 D1–D6：
   - **D1 自足 HTML**：每次交付 MUST 含一个可直接打开的 HTML；图片走**同目录相对路径**；MUST NOT 只给在线渲染 URL；页面语言跟用户一致。
   - **D2 源文件与渲染图一并保留**：**逐引擎产物表**——plantuml `.puml` + `.svg`/`.png`；mermaid `.mmd` + 两者；excalidraw `.excalidraw` + 两者；drawio `.drawio` + `.svg`/`.png`/`.pdf`；d3js / echarts 的 `.html` 本身即源文件（另含 `data.js` / `config.json` / `vendor/`）。复现性附录里的源码 MUST 与磁盘上那份**逐字节一致**。
   - **D3 without jargon, with context**：**图内文字与 HTML 说明文字二者同规**；带一组反例/正例对照；判据 = 「一个了解用户业务但没参与绘制的人，能否不问人就说出这是什么、谁调谁、为什么这样分」。标签**长度与格式**约束的 owner 仍是各引擎的 `guide/content.md`（文中如实注明 mermaid 与 plantuml 各一份且**已分化**，不是同一文件）。
   - **D4 MUST NOT 描述图表本身**：禁图例式解说（「椭圆代表用例」「方框代表对象」「箭头表示调用」「虚线框表示边界」）、禁视觉编码解释（「红色表示关键路径」「线越粗越重要」）、禁把已画出的结构再罗列一遍；附**理由**（与图冗余、图一改就过期）；唯一例外 → 做进**图内 legend**，不写进正文。
   - **D5 用用户原始上下文的术语讲架构或流程**：讲的是**系统本身**不是这张图；用用户自己文档/代码/提问里的名字；给了上下文就 MUST 一致并**带出处**、不臆造；多图时按图序组织、跨图词汇一致。
   - **D6 交付前自检**：10 项可机械核对的清单。
   - 收尾一张**边界表**，指明标签机制、几何与强弱、HTML 模板、引擎选型、大图与 legend 各自的 owner。
2. **`draw-drawio` 补 `## 交付形态（Delivery）`**——七家中**唯一真有缺口**的一家（此前完全没有 HTML 交付）：同目录三件套（`.drawio` 源 + 导出的 `.svg`/`.png`/`.pdf` + 作为入口的 `<name>.html`）、HTML 由 Agent 组装（相对 `<img src>`、禁绝对路径）、复现性附录逐字节一致、**无 draw.io CLI 时的诚实兜底**（MUST 如实说明只交付了源文件，不得假装已交付可直接查看的图）。
3. **前门五处接线**（`skills/draw-diagram/SKILL.md`）：Step 3 委派载荷加「交付契约」、Step 4 新增验收项 2（清单重编号为 4 项，回报项改为「产物路径（HTML + 源文件 + 图片）」）、Document Map 加一行、五层披露阶梯的 L2 与 L3 行各加一处。
4. **其余五家各加一条指针**（`## 输出要求` 中文 / `## Output Requirements` 英文的首条），由**断言恰命中一次**的 Python 脚本落盘。

## 本轮自查发现并修掉的缺陷（2 处，均我的）

### 缺陷 1（实质）：接线写成了「内容副本」，不是「指针」

**事实**：我先给六家 specialist 插入的指针条目**把 D4 的禁用示例整串抄了进去**（「椭圆代表用例、方框代表对象、箭头表示调用」+ legend 例外机制），前门 Step 4 与 Document Map 也各抄了一份规则内容。按 `.specify/shared/guidelines/one-source-of-truth.md` 的机械判据——**「改一个事实若需编辑多于 owner 一处，它就已经是副本」**——改 D4 的示例清单要动 **8 个文件**，判据不通过。

**修正**：七处全部收敛为**指针形**（规则编号 D1/D2、D3–D5、D6 + 一行主题标签 + owner 路径 + 「条文与示例只存在于契约，本节不复写」+ 冲突时以契约为准）；`draw-drawio` 只保留**引擎侧事实**（图内标签走 `mxCell` 的 `value=`）而删掉规则复写。修正后按四个签名短语复测：七家 SKILL.md **全部为 0**，契约文件保有全部条文（`椭圆代表用例` / `方框代表对象` / `箭头表示调用` 各 1、`without jargon` 2）。

**根因（举一反三）**：我在写「指针」时习惯把被指向的内容**摘要进去以求自足**，而自足性正是副本的成因。**规则：指针条目只写「编号 + 主题标签 + 路径 + 冲突裁决」，不写条文、不写示例、不写阈值。**

### 缺陷 2（方法论）：我上一轮的「零回归」结论用错了基线口径

**事实**：我在前几轮报告里写的「契约套件 `14 failed / 298 passed / 1636 deselected`，与既有基线逐字相同」是 **`-k` 过滤后**的子集结果，我却把它当作「契约套件」的整体基线陈述。本轮跑**全量** `tests/contract/` 得到 **24 failed / 1924 passed**，两个数字不可比。

**处置**：不再用口径不一致的数字下结论，改为**做真正的 A/B 基线**——`git worktree add --detach` 检出 `HEAD`（`e67f87c6`）到临时目录，对同样那 9 个失败文件跑一遍：

| 环境 | 结果 |
|------|------|
| HEAD 干净 worktree（无我的改动） | **23 failed / 77 passed** |
| 当前工作树（含本轮 + 前几轮改动） | **24 failed** |
| 差集（只在当前失败） | 仅 `test_docs_command_template.py::test_c10_runtime_copies_exist_for_every_present_tool` |

**该 1 项的归因（已查实，非我的改动）**：它 glob `**/speckit.history.*` 并要求同目录有 `speckit.docs.*`；命中者是 `tmp/e2e-md/.claude/commands/` 与 `tmp/e2e-toml/.qwen/commands/` —— 两个 **7 月 22 日**留下的 git-ignored 试验目录（`.gitignore:40:tmp/`），当时 `/speckit.docs` 命令**尚不存在**，故不可能有同名副本。新建的 worktree **不复制 git-ignored 文件**，所以基线侧看不见它们。`grep -rn "e2e-toml\|e2e-md" tests/` = **0 命中**，即没有任何测试创建它们。

**结论：本轮零回归。** 24 项失败中 23 项在 HEAD 上同样失败（长期既存），第 24 项是两个月前的 ignored 试验目录造成的环境性失败，与 draw 系无关（全部失败项无一提及 draw）。

**未动它**：`tmp/` 是用户的 git-ignored 试验产物，删除属破坏性动作且在本团队 `territory.write` 之外 → 只披露。用户若想让全量套件回到 23，可删除 `tmp/e2e-md`、`tmp/e2e-toml`，或在那两个试验工程里跑一次 `/speckit.instructions` 补齐 `speckit.docs.*` 副本。临时 worktree 已 `git worktree remove --force` 清理，`git worktree list` 只剩主树。

### 附带更正：上一轮报告里的一处表述

上一轮报告写「本轮附带查明：discoverability 无需 `/speckit.instructions`」——该结论仍成立，但本轮全量套件显示 `.specify/templates/commands/plan.md` **在盘缺失**（`test_mirror_identical[V-001]` 因此 `FileNotFoundError`，属既存 23 项之一）。这两件事不冲突（前者讲技能发现，后者讲命令模板镜像），但把「无需 `/speckit.instructions`」读成「本仓镜像完整」就错了。**如实记下，不改写上一轮报告。**

## 验证明细

| 检查 | 方法 | 结果 |
|------|------|------|
| 契约规则文本不外泄 | 四个签名短语在七家 SKILL.md 计数 | **全 0**；契约保有全部条文 |
| 指针接线到位 | `grep -c delivery-contract.md` | 六家各 **1**；前门 **4** |
| 前门验收项存在 | 定位 Step 4 | 第 **55** 行，含「D6 十项自检表」 |
| 全量契约套件 A/B | HEAD worktree vs 当前树，失败集 `comm` 求差 | 差集 **1 项**，归因于 7 月的 ignored 试验目录，**非我的改动** |
| 镜像一致 | `cmp -s` 八个改动文件 vs `.specify/` | **全部 OK**（`sync-mirrors.py --write` 后复测） |
| 无杂散投影 | `.specify/.specify` 是否存在 | **none** |
| 体积纪律 | `wc -l` | 前门 155、d3js 317、drawio 177、echarts 340、excalidraw 168、mermaid 258、plantuml 197、契约 91 —— 全部 **< 500** |
| 写入脚本安全 | 每次替换前 `assert count == 1` | 6 + 1 + 2 处替换，**全部恰命中一次** |

## 过程细节

1. **前置**：`goal-utils.py list --json` 确认 goal `active` / `criteria_count: 3` → 读 `team.md` frontmatter（`pattern: serial`，无 `focus_target`）→ 五项检查全过 → Target 指派 = 无。
2. **先勘查再动手**：查七家现有交付形态，发现 **HTML 包装与源文件保留在五家已存在**（mermaid/plantuml/excalidraw 有「单个 HTML + 复现性附录 + 逐字节一致」；d3js/echarts 的 `.html` 本身即产物），而**文字规则 D3/D4/D5 此前全库为零**（`grep "without jargon"` = 0 命中）。据此把工作量放在「立一个 owner + 补 drawio 的真缺口」，而不是给五家重写已有的 HTML 逻辑。
3. **`draw-drawio` 是唯一真缺口**：它此前只有 `.drawio` 与导出图，没有 HTML 入口 → 补完整的 `## 交付形态（Delivery）`。
4. **五层披露阶梯同步更正**：L2 行原写「今日只有两个文件」，加了契约后改为三个文件，并写明**选型、建模、交付三条判据链齐全**，但语义/图类/版面/样式/措辞/类专属专章**仍未从下层上移**。
5. **自查发现缺陷 1**：落盘后用签名短语复测，发现六家的指针条目里带着 D4 的示例串——即我的「无复写」检查此前用了**错误的短语集**（查的是我没写进去的词），产生假阴性。改为查**实际写入的短语**后暴露真问题，随即收敛为指针形。这是本会话第二次「**错的是我的检查，不是文件**」（前两次是 E4/F3），但这次检查错的方向**掩盖了一个真缺陷**，比前两次的虚警更危险。
6. **自查发现缺陷 2**：跑全量套件得到 24 failed 与我记忆中的 14 failed 不符 → 不辩解，改用 worktree A/B 建立真基线并逐项归因。
7. **未派发 Worker**：本轮全部编辑落在 canonical 写入面（`skills/draw-diagram/**`、`skills/draw-{d3js,drawio,echarts,excalidraw,mermaid,plantuml}/SKILL.md`），按团队纪律 **Meta 是唯一 canonical 写入者**，Worker 只写工作区草稿；把 9 处编辑交给 Worker 再回收只增加一次交接与验证门，无质量增益。
8. **S2/S3 本轮仍未执行**：S2（cleanroom 验证）需真实出图，而渲染路径（轴②/共享渲染服务）用户已裁定在**另一条流程**；此时跑 S2 会把渲染未决误判为技能缺陷。S3（评价单归集）依赖 S2 的真实交付。

## 静态结构（Role × Stage × Type）

| Stage | Role | Type | Lifecycle | 本轮 |
|-------|------|------|-----------|------|
| S1a | semantic-analyst | Worker | temporary | 未派发（见理由 7） |
| S1c | routing-analyst | Worker | temporary | 未派发 |
| S1b | engine-grammar-researcher | Worker | temporary | 未派发 |
| S1d | skill-author | Worker | temporary | 由 Meta 直接执行 |
| S1e | consistency-reviewer | Worker | temporary | 由 Meta 直接执行（自查发现缺陷 1） |
| S1f | open-item-resolver | Worker | temporary | 未派发 |
| S1g | delivery-contract-author | Worker | temporary | 由 Meta 直接执行（本轮新增职责） |
| S2 | cleanroom-verifier | Worker | temporary | **未执行**（渲染未决） |
| S3 | evaluation-intake | Worker | temporary | **未执行**（依赖 S2） |
| — | meta-supervisor | Meta | persistent | 唯一 canonical 写入者，本轮全部落盘由其执行 |

## 动态结构

- **pattern**: `serial`（质量优先，验证式交接）
- **DAG**: `S1a → S1c → S1b → S1d → S1e → S1f → S1g → S2 → S3`（无环）
- **handoff_protocol**: `file-path-only`
- **failure_strategy**: `retry-once-then-escalate`
- **verification**: `independent`——本轮的独立验证由**签名短语计数**与 **HEAD worktree A/B 失败集求差**承担，不采信自述

```
[用户五条 UX 要求]
        ↓
   S1a 语义勘查（七家现有交付形态盘点）
        ↓
   S1d/S1g 立 owner：delivery-contract.md（D1–D6）
        ↓
   S1d 补 drawio 真缺口 + 六家指针接线 + 前门五处接线
        ↓
   S1e 独立复测 →【发现缺陷 1：内容副本】→ 收敛为指针形 → 复测全 0
        ↓
   全量契约套件 →【发现缺陷 2：基线口径错】→ worktree A/B → 零回归
        ↓
   sync-mirrors → cmp 逐字节 → 本报告
        ↓
   S2 / S3（阻塞：渲染路径在另一流程）
```

## 遗留与下一步

**仍待落地（结构重构，本轮按「不做大改」未启动）**
1. **47 个同名但全部已分化的语义层文件**：身份核对显示七技能间 **47 个同名文件、0 个内容相同、全部分化**（例：`12-gantt-diagram.md` 73 行 vs 1708 行；`layout.md` 69 vs 471；`content.md` 61 vs 345）。它们是**各引擎的实现**，不是可去重的副本；重构必须**逐文件 diff**，不能按文件名搬。前门 `references/` 目前持有其中 0 份。原先「14 新文件 + 2 合并」的估算须据此重新推导。
2. 六家 specialist 触发词收敛（d3js 30 / echarts 33 / excalidraw 20 / mermaid 63 / plantuml 65，通用图类违规词 11/11/2/13/13；drawio 6/0 出生即收敛）。
3. 六组 guide 同名文件对的合并。

**轴② 裁定后回填**（另一条流程）：§0 轴② 值 6×4 = 24 格；七份 frontmatter 的「不声明轴②」免责语随之删除。

**移交他处**
- 框架侧：`sync-mirrors.py` 应排除 `node_modules`（`draw-excalidraw/scripts/server/node_modules` 13,952 文件被整树复制）；3 个技能有 `## Feedback` embed 却无 probe Object（`git-fleet` / `git-server-init` / `merge-skills`）；`goal-utils.py` 的 setter 应标记为破坏性、无显式 flag 时拒绝清空。
- `improve-skills` 批次：五家 specialist 缺 `## Self-Improvement Contract`；`draw-mermaid` 的 3,633 字节内联 `## Feedback` 变体 → 改指针形。
- `draw-drawio` 的 9 项渲染/CLI 未决项 + 1 项「结构未经真实导出验证」，待首次真实 draw.io 运行时消解。
- 用户侧（可选）：`tmp/e2e-md`、`tmp/e2e-toml` 两个 7 月试验目录使全量套件多 1 项失败。


---

### 追加（同 run 的后续指令）：交付 HTML 的统一样式

**用户指令**：「针对这个输出的 html 需要配置一个好的 css style，简洁美观」。

**勘查**：七家中只有 d3js / echarts 带 `assets/template.html`，且两者**已经共享一套事实上的卡片配方**（白卡、radius 8、同一阴影、`#fafafa` 底、同一字体栈）但各写一份；其余四家（mermaid / plantuml / excalidraw / drawio）**完全没有模板**，产物 HTML 无样式。即：不是缺一份好样式，而是缺一个 owner。

**落地**：
1. **新建 `skills/draw-diagram/assets/delivery.css`（234 行）= 页面骨架样式的唯一 owner**。文件头注释即期望的页面结构（类名可省略，只写语义标签也有完整样式，故无模板的四家不必自带 template）。内容：设计令牌（CSS 变量）、浅/暗两套配色（`prefers-color-scheme`）、含 CJK 的系统字体栈、正文排版（15px / 1.75 行高、`.prose` 78ch 行宽）、图框与图注、复现性附录（details/summary）、`.meta` 小字、打印规则（隐藏附录、图不跨页）。
2. **消费方式**（写入契约 D1 + D6 第 11 项）：生成产物时把该文件**逐字节内联**进 `<style>`（不凭记忆重写、不 `<link>` 外链、不引在线字体/CDN —— 否则违反「自足」「无需联网」）。
3. **分工边界**：该表只管页面骨架；**图表内部**样式（d3 的 `.axis`/`.tooltip`、SVG `<text>` 的 `text-anchor`/`fill`、ECharts 的 option 配色）归各引擎，写在共享表之后。据此把 d3js / echarts 两个模板的重复骨架 CSS 收敛为「INLINE-VERBATIM 占位注释 + 仅引擎内部规则」（d3js 65→23 行、echarts 24→10 行），并把两者 body 包进 `<main class="container">`。七家从此同一观感，且改样式只动一个文件。
4. **暗色下的「白纸」**：导出的 PNG/SVG 与脚本渲染图表的墨色多为浅色背景写死，直接压在深色卡片上不可读 → 暗色模式给 `figure img/svg` 与 `#chart` 垫白底（比给每个引擎做暗色主题便宜且不失真）。

**渲染验证（不凭想象交付「美观」）**：用真实交付形态（`<img src="order-flow.svg">` + 逐字节内联的 CSS）在 Chrome 里截图三态并逐一看图：
- 桌面浅色：卡片留白、图注、正文行宽、附录、meta 均符合预期；
- 桌面暗色：图落在白纸上完全可读，正文/附录/小字对比度正常；
- 手机 390px：**暴露并修掉两个真缺陷**——
  ① 初版默认 `img{max-width:100%}` 把 760px 的图等比压到 317px，图上文字约 5px 不可读 → 改为**默认保持原尺寸、溢出横向滚动**（`figure{overflow-x:auto}` + `figure img{max-width:none}`，确需缩小时 `class="fit"`），契约与 CSS 头注释同步改写；
  ② 居中若用 `text-align:center`，溢出内容的左半截会落在滚动可达范围之外（负向 scrollLeft 不存在）→ 改用 `display:block; margin-inline:auto`（窄时居中、溢出时左对齐且可滚）。
  复测：`figW=317, scrollW=760, imgW=760, leftOffset=0, scrollLeft=0` —— 左缘可达、全尺寸、可滚。

**验证**：`sync-mirrors.py --write` 后五个改动文件镜像**逐字节相同**；全量契约套件 **24 failed / 1924 passed**，与本轮已建立的基线**同一失败集**（23 项 HEAD 既存 + 1 项 7 月 ignored 试验目录），**无一提及 draw** → 零回归。两个模板改后 HTML 结构解析 OK（标签配对无错）。

**顺带修掉一处自己的指针腐烂**：前门 Step 4 原写「D6 **十项**自检表」，D6 增项后该数字即过期 → 改为「D6 自检表，逐项核对」，数字只留在 owner（D6 表本身）。
