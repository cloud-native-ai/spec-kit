# Run Report — draw-two-layer-structure (run 5)

- **Run ID**: 2026-09-17T072239Z
- **Team**: `.specify/teams/draw-two-layer-structure/team.md` · pattern `serial` · handoff `file-path-only` · failure strategy `retry-once-then-escalate`
- **Goal**: `.specify/goal/draw-two-layer-structure/goal.md`（显式 `goal_slug`，`active`，3 判据）
- **Target 指派**: 无（goal 整体）
  - `resolve_effective_target` → `{effective: null, source: "none", declared_focus: null}`；T-001/T-002/T-003 全 `dropped`，0 个 open；goal 非终态。DAG 9 stage、无环、无悬空引用。
- **用户本次附加指令**: 「我在另一个流程中处理渲染问题，当前流程中继续执行技能的拆分和优化。」→ **轴②（渲染位置）出本次 run 范围**，交由另一流程；本 run 继续拆分与优化。
- **结果**: **S1g 产物落地 canonical（轴② 显式暂缓）**。`skills/draw-diagram/references/routing-matrix.md` 3280 → 11198 字节、43 → 103 行。这是本团队**首次改动 canonical 的路由权威文件**。

## Summary

本 run 做了三件事：① 修正 S1g 定义里我自己写错的一处（「七引擎」→「六引擎」）；② 执行 S1g 派发并验证；③ 把 S1g 产物**落地**进 `routing-matrix.md`，其中轴② 按用户指令暂缓登记。

S1g 的派发一次通过（未撞顶），但其草稿的轴② 列**不可落地**——用户在上一轮把轴② 的判据重新定义了（远端渲染为**优先**而非约束），且实测 mermaid 与 plantuml **都是远端优先**（`MERMAID_BACKEND=server` → mermaid.ink；`PLANTUML_BACKEND=server`），excalidraw 亦声明远端优先（自托管 node server），与草稿所依据的原裁定「plantuml 是唯一远端、mermaid 是本地」不符。用户已将该问题移交另一流程，故本次落地**机械删除轴② 列**并写入显式暂缓说明，而非落地一个已知处于争议中的取值。

## Process detail

### 1. Meta 自查修正：S1g 定义的引擎计数错误

派发前发现 S1g 的 `task` 与 `quality_gate` 写「七引擎 × 四轴 = 28 格」。但用户裁定的四条区分只枚举**六个引擎**（d3js / drawio / echarts / excalidraw / mermaid / plantuml）；`draw-diagram` 是前门、不渲染，轴①（动态图表）与轴②（渲染位置）对它**无取值**。若按 28 格派发，Worker 将被迫为前门编造轴值，而 gate 又禁止空格——是我自己在上一轮结构编辑时把「七个技能」（套件规模）与「六个引擎」混为一谈。

经 4 处精确替换修正（每处 assert 恰好 1 次命中）：(A) 六引擎 × 四轴、并显式禁止 draw-diagram 入轴值表；(D) 六 specialist 陈述自身轴位置 + **前门陈述「以四轴为选引擎判据」**而非声称自己有轴位置；gate ① 24 格且 draw-diagram 出现即不过；gate ⑤ 七技能 frontmatter 全到位，并注明前门**必须**持有通用图类词（那是它的职责），对它只核「不因四轴新增而丢失既有通用词」。

### 2. S1g 派发（1 次，未撞顶）

brief 依 run 4 的教训收敛读取面：**恰好三处读取**（team.md 207–219 自身 stage 定义；`routing-matrix.md` 全文 43 行；一条机械命令抽七份 frontmatter description），明令禁读 progress.md / frontdoor-destination.md / ownership-*.md / 任何完整 SKILL.md / create-skills；四轴取值与 S1a–S1f 已确立的引擎事实全部内联。写序为「先完整写 A+B+C，再逐引擎写 D」，与 stage 定义里预置的 S1g-1/S1g-2 拆分同形。

产物：`routing-axis-matrix.md`（141 行 / 14297 字节）+ `frontmatter-convergence.md`（143 行 / 15969 字节）。

### 3. Meta 复核（机械核验，不采信自述）

| gate | 判定 | 证据 |
|---|---|---|
| ① 六引擎 × 轴全表无空格 | **PASS** | 24/24 格（草稿）有取值 + 一句依据；无 unknown / N/A；`draw-diagram` 未入轴值表（正则核 `^\|\s*`?draw-diagram` 零命中） |
| ② drawio 入 §1/§2/§3 三处 | **PASS** | 落地后三处均命中（`**draw-drawio**` 独占行、`drawio（标准工程观感）` 替代列、`` `standard-engineering` `` tie-break 第 6 条） |
| ③ drawio↔excalidraw 可判优先序 | **PASS** | 五步有序判据，主判据=观感、次判据=交付链路成熟度、缺省 drawio 且附理由；文中两处「皆可/视情况/取决于」命中经核**均为否认句**（"无「皆可/视情况/取决于」出口"），非实际出口 |
| ④ arena 证据逐字保留 | **PASS** | 6 个片段（证据源声明两行 + `cycle 3 冠军 R2 0.95` + `cycle 1-2 冠军` + `cycle 3 R2 0.94` + `mermaid 0.755`）经 **Python 精确子串**比对全部 byte-identical（未用 shell grep，避免 `${...}` 被展开造成假阴性——草稿 §L 自检第 3 条即如此要求） |
| ⑤ frontmatter 收敛 | **产物完成，未落地** | 收敛数：d3js 30→3、drawio 6→6、echarts 33→3、excalidraw 20→11、mermaid 63→3、plantuml 65→4；specialist 通用词命中 0、d3js∩echarts = ∅、前门保留 21/21 通用词。**按用户裁定「先落 routing-matrix.md」，七份 frontmatter 留待与第二步结构重构一并落地**（两者都改同一批 SKILL.md 的 description） |
| ⑥ 轴取值单点拥有 | **PASS** | 落地后取值只在 `routing-matrix.md` §0.2；team.md 的 `capability_axes` 仅登记「轴存在」；各 specialist SKILL.md 未被本次触碰（`git diff` 中 `description:` 行改动数 = 0） |
| ⑦ 增量落盘 | **PASS** | 两份产物均非桩；未撞顶故未触发预案 |

**territory**：Worker 零 canonical 写入。落地前 `git status` 中 6 个 `M` 的 SKILL.md 全部是 run 4 的评价单截断修复（唯一 diff hunk 在 mermaid 第 192 行 +19），非本 run Worker 所为。

### 4. Meta 落地（本 run 唯一的 canonical 写入）

落地脚本 `.specify/teams/.work/draw-two-layer-structure/land-routing-axis.py`：**表格从草稿机械抽取、不经 LLM 转写**（run 3 的教训——转写/抽取环节是缺陷源），先 dry-run 后 `--apply`，脚本内置 assert 与保留片段自检。

- **§0 能力轴层**插在第 4 行（arena 证据源声明）之后、`## 1.` 之前；既有 `## 1.`–`## 4.` **不改号、不重排**（外部按段号引用）。
- **轴② 机械删除**：从草稿的轴定义表删去 `②` 行、从取值表删去第 3 列 → 落地为 **六引擎 × 三轴 = 18 格**（脚本 assert 单元格数 = 18、assert `draw-diagram` 不在表内、assert 抽取后无「渲染位置」残留于表内）。
- **轴② 暂缓说明写入 §0 开头**：明写「渲染方式正在另一条流程中重新裁定（远端渲染优先以减少本地依赖、并统一出图口径；含各引擎是否共用同一远端渲染服务这一未决架构问题）。在其定案前本文件不登记轴②取值——登记一个已知处于争议中的取值，会让路由据错误前提选引擎。轴①③④ 照常生效。」
- **§0.3 交互规则 6 条**（轴层 veto-only、固定执行序 §0→§1→§2→§3→§4、轴取值不得推翻独占命中、冲突升级为一轮 AskUserQuestion 不静默改路、剪枝可位移 §2 默认、轴取值只在 §3 内当最终排序判据且判据文本单点拥有在 §0.4）。
- **§0.4 drawio↔excalidraw 五步优先序**（自草稿 C 节，引用改指落地后的 §0.4；原草稿中依赖轴②的措辞「同为本地 + 自由版面」改为「同为自由版面」，避免引用暂缓轴）。
- **§1** +1 行（drawio 独占：`.drawio`/mxGraph 可编辑产物 + 官方 CLI 无头导出 + 免文本语法中介的原生几何/形状库工程制图）。
- **§2** +1 行（标准工程观感新建 → drawio，证据列标「新增引擎（S1f 引导），待竞技场复评」）+ 改 1 行（自由版面新建行的**替代列**增补 `drawio（标准工程观感）`，`cycle 3 R2 0.94` 证据列逐字保留）。
- **§3** 追加 6–8 条（`standard-engineering` → drawio、`react-embed` → excalidraw、与 artifact_intent 无关的自由版面竞争 → 依 §0.4，缺省 drawio），原 1–5 条与升级路径逐字保留。
- **§4** 追加 4 条轴派生红线（自由版面诉求不得给 mermaid/plantuml；动态图表诉求不得给静态单帧引擎；坐标系语义外自由几何不得压给 echarts；不得以轴取值命中为由跳过独占登记），原 3 条逐字保留 → 红线 3 → 7 条。
- `sync-mirrors.py --write` exit 0（1 文件：`.specify/skills/draw-diagram/references/routing-matrix.md`）。

**落地后核验**：6/6 保留片段精确子串命中；轴值表 6 行 × 4 列（引擎 + 三轴）；`draw-diagram` 不在轴值表；drawio 在 §1/§2/§3 三处齐备；红线 7 条；定向契约测试 **27 passed / 0 failed**（probe registry、skill conformance、sync-mirrors、mirror-root-guard）。

## 未落地 / 遗留

1. **轴② 由另一流程裁定后补入 §0** —— 需回填六引擎取值并重算格数（届时为 6×4=24）。本文件已写明暂缓理由，不留空表格。
2. **七份 frontmatter 收敛未落地**（用户裁定「先落 routing-matrix.md」）：`frontmatter-convergence.md` 在手，宜与第二步结构重构同批落地，避免两次改同一批 SKILL.md 的 description。
3. **第二步结构重构仍未落地**：`frontdoor-destination.md` 的 16 槽位（14 新文件 + 2 existing 合并）、`guide-merge-ruling.md` 六对裁定、specialist 语义段替换为指针。
4. **draw-drawio 技能本体仍未创建**：S1f 草稿在手（370 行），但**落地前 MUST 先核实其 16 条未决项**，尤其两条高优先项（mxGraph 是否有外层容器元素、edge 是否需 `parent`）——它们决定最小骨架能否合成有效文件。核实来源为真实 `.drawio` 文件或官方文档，不得凭合理长相补写。其评价单段须改用 run 4 修复后的 canonical 完整形态（草稿是从截断文本反推的）。
5. **`goal.md` 的 Objective 仍枚举五个引擎**，与判据 1「七个绘图技能」矛盾；`goal-utils.py` 无 `objective` 动作 → 无合法修复路径，建议补该动作。
6. **S2/S3 未启动**：S2 blockedBy S1g 已满足，但 S2 是净室验证「七技能绘制是否符合预期」，在 drawio 未创建、结构重构未落地前跑 S2 只能验到部分面。建议顺序：drawio 落地 + 结构重构 → S2 → S3。
7. 沿自前次：`/speckit.instructions` 同步可发现性；`viz-skill-arena` 悬空 `goal_slug`（按裁定不动）；两团队在 `skills/draw-*/**` 的文件级 territory 重叠 → 不要并发运行。

## Summary refresh

- **Goal 身份**：`draw-two-layer-structure`（显式）；**交付目录**：`.specify/goal/draw-two-layer-structure/summary/`
- **本次决定**：**不刷新**，抑制门 `no-material`。本次确有 canonical 变更（路由权威文件 +7.9KB），但 goal 判据 3 的度量口径是「绘制运行中无评价的占比」与「已提出评价中被处置的占比」——本 run 未产生任何绘制运行，也无评价条目，两个占比**无可度量位移**。路由判据增强属于朝目标推进的结构性工作，不改变判据 3 的当前读数。

## Self-assessment

- **做对的**：① 派发前自查出自己上一轮写进 stage 定义的「七引擎/28 格」错误并修正，避免把矛盾（前门须有轴值 vs gate 禁空格）传给 Worker；② 轴② 按用户指令**机械删列 + 写明暂缓理由**，而不是落地一个已知错误的取值，也不是悄悄留空格；③ 落地走脚本机械抽取而非 LLM 转写，dry-run + assert + 保留片段自检，一次通过；④ 保留片段核验用 Python 精确子串而非 shell grep（草稿 §L 明写此坑：shell 会展开 `${...}` 造成假阴性）。
- **做错的**：轴② 的错误根源在我——上一轮把用户的四条区分写进 S1g 定义时，**没有去核对 mermaid/plantuml/excalidraw 的实际渲染后端**就照抄了「plantuml 唯一远端、mermaid 本地」。若当时核了，就不必在落地前才发现轴② 不可用。教训：**把用户裁定写进 stage 定义前，凡涉及可查证现状的断言 MUST 先对仓核验**；用户的裁定是意图权威，不是现状权威。
- **仍未解决的**：S1g 是首个**未撞顶**的 stage（读取面收敛到三处的效果），但样本量 1，不足以断定 15 轮上限问题已解决。累计 9 次派发中 4 次撞顶。
