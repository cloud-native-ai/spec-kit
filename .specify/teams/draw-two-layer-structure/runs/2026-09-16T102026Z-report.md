# Run Report — draw-two-layer-structure（第 2 次 run）

- **Run ID**: 2026-09-16T102026Z
- **Team**: `draw-two-layer-structure`（pattern `serial`，4 席 / 7 stage，1 Meta）
- **Bound Goal**: `.specify/goal/draw-two-layer-structure/goal.md`（`goal_slug` 显式；`status: active`，3 条判据）
- **Target 指派**: 无（goal 整体）
- **执行时间**: 起于 2026-09-16T07:56Z 之后（下界取本 run 前一条 feedback 记录 `20260916T075612Z`，非精确计时），止于 2026-09-16T10:20:26Z
- **结果**: **S1a PASS · S1c PASS · S1b FAIL → escalate**；S1d/S1e/S2/S3 保持 locked；canonical 零改动

## Pre-flight（引擎判定）

`resolve_effective_target` → `{effective: null, source: "none", declared_focus: null}`；`parse_goal` → `status: active`、criteria 3、open targets `[]`。五项检查：1（绑定 goal 显式解析）与 5（goal 非终态）通过；2/3/4 因 `effective=null` 不适用。DAG 程序化校验：7 stage、无悬空引用、拓扑排序成功无环，根 = `S1a-mermaid` ∥ `S1c-rest-three`。

**Summary 决定**：配置为会刷新（`enabled: true`、`every: 1`、交付目录 `.specify/goal/draw-two-layer-structure/summary/`、goal 身份显式）。**实际未刷新，抑制门 = 无材料**：本次 run 未产生任何 canonical 交付（S1 全段未完成、无落地变更），S1a/S1c 的归属分析是中间制品而非对 goal 判据的进展证据。summary 应在 S1 全段通过并由 Meta 落地后刷新。

## 逐 stage 执行

### S1a-mermaid（structure-adjuster，Worker）— **PASS**

- CHECK：`blockedBy: []` → 可启动。BUILD CONTEXT：无上游。
- INVOKE：1 次派发，**未撞轮次上限**，正常返回。
- 产物：`S1-refactor/ownership-mermaid.md`，150 行 / 21618 字节。
- VALIDATE（gate 逐条）：① mermaid 8 个目录（含顶层 SKILL.md）全部有判定 —— 确定性复核：`SKILL.md`=MIXED、`best-practices/`=DATED-RECORD、`references/`(顶层)=MIXED、`document/`=MIXED、`guide/`=MIXED、`howto/`=MIXED、`scripts/`=SYNTAX、`server/`=SYNTAX；`UNCLEAR` 计数 **0**。② 例外项逐文件有判定，13 个 `MIGRATE→` 目的地（`MIGRATE` 词出现 22 次）。③ frontmatter 触发词逐条列出（63 条中 60 条判为越界）并给收敛规则（收敛到引擎名 `mermaid`/`Mermaid图`/`mermaid diagram`）。④ 未提议触碰任何 off-limits 路径。**PASS**。
- **子代理反驳了 brief 的预设并被抽查证实**：`references/document/` 并非整批引擎语法 —— `01-uml-overview`、`08-modeling-methodology`、`09-grasp-principles`、`10-architecture-diagram`、`11-wbs-diagram` 五个文件是引擎无关的建模知识（Meta 抽查确认五文件存在；"引擎无关"这一内容判断留待 S2 净室复验）。它同时指出前一次 `ownership-map.md` 的 4 处缺陷（把 guide/ 当整批迁移、漏 frontmatter 触发词与「核心原则」语义条目、漏 `server/.gitignore`、缺 plantuml 对照标记）。
- VERIFY HANDOFF（门 A）：产物覆盖 S1b 的 `inputs_from` 契约（mermaid 归属 + guide/ 的 plantuml 对照标记）→ **通过，解锁 S1b**。

### S1c-rest-three（structure-adjuster，Worker，与 S1a 并发）— **PASS**

- INVOKE：1 次派发，**未撞轮次上限**。
- 产物：`S1-refactor/ownership-rest-three.md`，179 行 / 25547 字节。
- VALIDATE：① 14 个目录 + 3 个 SKILL.md 全部有判定；`UNCLEAR` 计数 **0**。② 9 条 `MIGRATE→` 归入 2 个目的地（`skills/draw-diagram/references/semantic-analysis.md` 新建 + `semantic-model.md`）。③ 三个 frontmatter 触发词清单与收敛建议齐备，并发现 d3js 与 echarts 的 frontmatter 在约 8 个通用触发词上**逐字相撞**。**PASS**。
- **子代理反驳了 brief 的预设并被抽查证实**：brief 说"echarts 无残留证据"，实测**不干净** —— Meta 抽查 `skills/draw-echarts/SKILL.md` 确认：L103「布局选择是语义决策，本节只保留引擎事实」**本身是正确的两层形态**（显式把语义决策让给前门），但紧随其后的 `推断边 vs 源描述边`、`校正/仲裁标注`、`Explicit omission annotation` 三条是**语义建模纪律穿着引擎语法外衣**，且无 d3js 那样的〔决策归语义层〕标注。另发现：standalone-derivation 回退**三个 SKILL.md 都有**（不只 excalidraw）；`references/cycle4-improvements.md` 存在于 **4 个技能**（plantuml/d3js/excalidraw/echarts，mermaid 用的是 `cycle3-reproduction-lessons.md`）—— Meta 用 `find` 确认，**我的例外清单与 S1a/S1c 的 stage 定义都漏了它**；excalidraw 的 `references/guide/`(2 文件) 经核为引擎参考而非漂移语义层（与 mermaid/plantuml 的 guide/ 性质不同）。
- VERIFY HANDOFF（门 C）：**通过**。

### S1b-plantuml（structure-adjuster，Worker）— **FAIL**

- CHECK：S1a 通过 → 解锁，可启动。BUILD CONTEXT：读 S1a 产物（21618 字节）。
- INVOKE：1 次派发，**撞 15 轮上限**，自述进度止于 "document/ classified. Now the howto/ exception files — 00 and 10."
- 产物：`S1-refactor/ownership-plantuml.md`，**10 行 / 1476 字节** —— 只有标题与判定词图例（5 个判定词各出现 1 次即为此故）。**它自述已完成的 `document/` 分类从未写进文件**：本次派发未遵守 brief 里的增量落盘指令，进度全损。
- VALIDATE：① 6 目录 + 顶层 SKILL.md + 4 个 `references/` 顶层文件 → **0 个有判定，FAIL**。② 整批判定需点名 ≥2 个实读文件 → 无判定可查，**FAIL**。③ 例外项 → 仅图例，**FAIL**。④ **guide/ 合并裁定（本 stage 最高价值产出）→ 完全缺失，FAIL**。⑤ frontmatter 触发词 → 缺失，**FAIL**。
- VERIFY HANDOFF（门 B）：**不通过**，S1d 不解锁（S1d 需 S1a+S1b+S1c 全通过）。
- **retry 额度**：S1b 自身尝试数 = 1，按 `failure_strategy: retry-once-then-escalate` **尚有 1 次重试**。Meta 判断盲目重试预测仍会失败（见下方诊断），且修好它需要重定义 stage 交付物 = 移动球门，故**不擅自消耗重试**，改为 escalate。

### S1d / S1e / S2 / S3

未启动 —— `blockedBy` 未满足，按 serial 协议不得越过交接门。

## 诊断：目录级粒度是成立的，压垮 S1b 的是任务类型混淆

四次派发的对照（确定性数据，非估算）：

| 派发 | stage | 任务 | 轮次 | 产物 | 结果 |
|---|---|---|---|---|---|
| 1 | 旧 S1（全量） | 逐文件归属 ×6 技能 + patch-set + sequencing + evaluation-form 设计 | 撞顶 | 0 字节 | FAIL |
| 2 | 旧 S1（retry） | 逐文件归属 ×6 技能，增量落盘 | 撞顶 | 110 行 / 5975 字节（13/162 文件） | FAIL |
| 3 | **S1a** | **目录级**归属 ×1 技能（8 目录）+ 例外清单 + 63 条触发词 | 未撞顶 | 150 行 / 21618 字节 | **PASS** |
| 4 | **S1c** | **目录级**归属 ×3 技能（14 目录）+ 例外清单 + 3 份触发词 | 未撞顶 | 179 行 / 25547 字节 | **PASS** |
| 5 | **S1b** | **目录级**归属 ×1 技能（6 目录）+ 例外清单 + **6 对漂移 guide/ 的章节级合并裁定** + 消化 21.6KB 上游产物 | 撞顶 | 10 行 / 1476 字节 | **FAIL** |

结论：**「目录级归属 + 例外清单」这个粒度改动是有效的** —— S1a 覆盖 60 文件的技能、S1c 覆盖 47 文件的三个技能，都在预算内正常完成且 0 个 `UNCLEAR`。S1b 的技能只有 52 文件、6 个目录，**比 S1a 更轻**，却失败了。差异不在归属工作量，而在我额外压进去的两件事：

1. **guide/ 合并裁定是另一种任务类型** —— 对 6 对已漂移文件（共 12 个文件）做**章节级**差异对照并逐处给处置（取基/取另一份/合并/丢弃），还要识别每份里的 plantuml-carrier 章节。这是 reconciliation，不是 attribution，本身可能就值一个 15 轮预算。
2. **消化 21.6KB 的上游产物**（S1a 的 ownership-mermaid.md）作为对照基线，是 file-path-only handoff 的必然成本，但它把预算又吃掉一块。

所以正确的修法不是"再拆细 plantuml 的目录"，而是**把 reconciliation 从 attribution 里分出去**。而 S1d-frontdoor-destination 的定义里本来就写着"落地 S1b 的 guide/ 合并裁定"——合并裁定与"迁入目的地设计"其实是同一件事（哪份为基、差异如何处置、落到前门哪个槽位），把它并入 S1d 比留在 S1b 更自然。

**第二个独立发现**：S1b 未遵守增量落盘指令，导致自述已完成的 `document/` 分类全损。S1a/S1c 遵守了（产物完整），S1b 没有 —— 说明"增量落盘"作为 brief 里的一句指令**不可靠**，应当成为 gate 条件（如"产物字节数 > 图例部分"或"每个已判定目录在文件中可检索"），否则撞顶即全损。

## Territory 与保留边界

**canonical 全绿**：三次派发（S1a、S1c 并发 + S1b）后 `git status --short skills/ shared/ templates/ .specify/shared/` **全空**。Worker/Meta 写权限分离持续成立。

未触碰：`shared/definitions/goal-definitions.md` · `.specify/shared/workflow/feedback-step.md` 的 never-solicit 条款 · `templates/**` · `.specify/teams/viz-skill-arena/**` · `.specify/goal/draw-two-layer-structure/goal.md` · 八个 `best-practices/*.md` 的 dated provenance 行 · 前门对 arena 账本的三处活引用。

## 材料缺口声明

- 无 goal summary 产出（抑制门 = 无材料，理由见 Pre-flight）。
- 无 `items.jsonl` 条目：本次无 Target 指派（`effective: null`），且 S1 段未整体完成、无落地变更，无可入账工作项。
- 无 canonical 改动 → `sync-mirrors.py` 未运行。
- S1b 的 `document/` 分类结论**丢失**（未落盘），重跑需重做。

## Escalation（待人工裁决）

S1b 的 gate FAIL 且诊断指向 stage 定义问题，三条路都属 `improve-team` 权限，不由本次 run 自决：

1. **把 guide/ 合并裁定从 S1b 移入 S1d**（S1d 定义里已有"落地 S1b 的 guide/ 合并裁定"一句，合并裁定与目的地设计本是同一件事）。S1b 收窄为纯 attribution（52 文件 / 6 目录，比已成功的 S1a 更轻，预测可过）。代价：S1d 变重，可能自身需要再拆。
2. **保持 S1b 定义不变，消耗剩下的 1 次重试**，并在 brief 里把增量落盘改成硬性要求（每判完一个目录立即写盘）。Meta 预测仍会失败 —— 合并裁定本身就接近一个预算。
3. **提高子代理轮次预算**（当前 15）后重发 S1b。不改 stage 定义，但预算调到多少够用仍是猜的。

另有一条**不依赖上述选择**的独立修正建议：把"增量落盘"从 brief 指令升格为 gate 条件，避免撞顶即全损（本次已损失一次 `document/` 分类）。
