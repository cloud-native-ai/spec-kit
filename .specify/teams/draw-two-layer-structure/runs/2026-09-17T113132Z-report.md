# Run Report — draw-two-layer-structure

- **Run ID（UTC）**: 2026-09-17T113132Z
- **Team**: `draw-two-layer-structure`（`pattern: serial`）
- **Goal（权威定义）**: `.specify/goal/draw-two-layer-structure/goal.md`（`goal_slug` 显式绑定，status `active`）
- **Target 指派**: 无（goal 整体）
  - 解析依据：本次未传 `--target`，`team.md` 亦未声明 `focus_target` → `source=none`，全流程与引入前等价。五项检查：绑定 goal 命中 archive（engine `list` 枚举确认）✓；无悬空引用 ✓；无终态 Target 引用 ✓；无跨 goal 引用 ✓；goal 自身非终态（`active`）✓。
- **Summary 刷新决策**: **不刷新**。门控 = 无可入账的新 goal 级材料（本轮为结构梳理与检查，产出落在技能文件与本报告；`.specify/goal/draw-two-layer-structure/summary/` 不写）。
- **本轮用户指令**: 「最后跑一轮梳理和检查」，并给出期望的五级逐级披露层次（L1 前门 SKILL 语义分析 → L2 前门 references 选型 → L3 委派 draw-* → L4 引擎 SKILL 自身图表类型逻辑设计 → L5 引擎 references 完善出图），且明确「大体的流程是这样即可，**不需要做太大的修改**」。

## 结果摘要

按「不做大改」的约束，本轮**只做梳理与检查 + 四处小修**，未启动结构重构（14 新文件 + 2 合并 + 六家触发词收敛仍待落地）。

1. **五级披露层次已在前门显式化**：`skills/draw-diagram/SKILL.md` 新增 `## 逐级披露（Progressive Disclosure —— 五层）`，把用户描述的 L1–L5 落为承载面表格（指针形，不复写任何 owner 内容），并**如实登记当前落地状态**（L2 只有 2 个文件；语义专章仍多副本存放在下层）。
2. **修闭一处 L3→L4 契约断裂**：前门 Step 3 明确要求「`weight_plan` 随 LDM 传递，由被委派技能按其**「强弱实现」小节**落地」，而新建的 `draw-drawio` **既无该小节、也无 `sds-realization.md`** —— 委派到 drawio 时 `weight_plan` 无处落地。已补 `## SDS 实现与强弱落地`（含 tier→`strokeWidth` 表、输入契约、几何兑现义务）。
3. **补全前门引擎名册**：description 原列五引擎（漏 `drawio`），而 `routing-matrix.md` §1/§2/§3 均已可路由到 `draw-drawio` —— 名册与路由权威不一致。已补为六引擎。
4. **补全偏离声明分类**：`semantic-model.md` Deviation Declaration 第 1 类「绝对坐标引擎」原列 d3js / echarts / excalidraw，未含 drawio；drawio 同为调用方给绝对几何的引擎。已补入。

**验证**：27 项断言全 PASS；契约套件 `14 failed / 298 passed`（与既有基线逐字相同，**零回归**）；probe 注册表 `6 classes / 83 objects` 有效；三个被改文件的镜像**逐字节相同**；前门 21 条触发词、drawio 6 条触发词**均未变动**（收敛仍按计划与结构重构一并落地）。

## 严重事件：本轮误删 goal 判据（已完整恢复）

**事实**：执行 run 前置的「读 goal 判据」时，我运行了 `goal-utils.py criteria draw-two-layer-structure`，**误把一个 setter 当 reader 用**。该 action 的语义是「replace criteria, recording the prior value」（引擎 `--help` 原文），无 `--criterion` 入参即把三条 Success Criteria **整体替换为空**，`goal.md` 的 `## Success Criteria` 一度变成 `None provided.`。

**影响面**：仅 `.specify/goal/draw-two-layer-structure/goal.md` 的判据块。Objective、Targets（3 条，全 `dropped`）、`status: active`、`created` 未受影响。

**恢复**：引擎的 append-only `## History` 逐字保存了被替换前的值。我用**机械提取**（解析该 History 行 → 按 ` | ` 切分 → 断言恰为 3 条且第 1 条含「七个绘图技能」与 `drawio`）取回原文，再经**引擎**（`criteria --criterion ×3`）写回 —— 未手改 `goal.md`（authored-only，且在本团队 `territory.forbidden` 内）。恢复后核对：`list --json` 报 `criteria_count: 3`，且当前三条与 History 记录的事故前值**逐字节相同**（`BYTE-IDENTICAL: True`）。

**残留痕迹（有意保留）**：`## History` 现有三条 criteria-changed 记录 —— 2026-09-16（六技能版）、2026-09-17（七技能版，即事故前值）、2026-09-17（`None provided.`，即事故本身）。**不改写历史**：事故与恢复都留痕，比抹平更可信。

**根因与规则（举一反三）**：`goal-utils.py` 的 action 名不携带读写倾向 —— `status` 同样要求 `--set`（是 setter），`criteria` 无 flag 即清空。**规则：对 `goal-utils.py`，只有 `list` / `validate` / `check-statement` / `targets --list` 是读操作；`status`、`criteria`、`targets --add/--set` 一律是写操作，MUST NOT 以「查看」为目的调用。** 读判据的正确方式是 `list --json`（给 `criteria_count`）或直接读 `goal.md`。本轮已按此规则复核，未发生第二次误写。

## 检查明细（五层逐级核对）

| 层 | 承载面 | 状态 | 证据 |
|----|--------|------|------|
| **L1** | `draw-diagram/SKILL.md` 语义分析 → 整体逻辑图表设计 | **就位** | Step 1 产出 LDM（11 处）+ 几何层（canvas / box{x,y,w,h} / 分区盒与布局语义 / relation anchor+gap）；`weight_plan` 必填纪律在 `semantic-model.md` 构建纪律第 5 条 |
| **L2** | `draw-diagram/references/*.md` 选型分析 | **部分就位** | 仅 `routing-matrix.md`（§0 轴层 / §1 独占 / §2 图类矩阵 / §3 tie-break / §4 红线）与 `semantic-model.md`（LDM schema / 覆盖检查 / Geometry / 偏离声明）。**选型能力自足**；语义专章未上移 |
| **L3** | 委派 `skills/draw-*` | **就位（本轮补全名册）** | Step 3 以 Skill 调用委派、file-path-only handoff、传 SDS + `weight_plan`；Step 2 Preflight 校验引擎目录存在，缺失即响亮失败。description 名册已补 `drawio` |
| **L4** | `draw-*/SKILL.md` 自身图表类型的逻辑设计 | **六家就位**（drawio 本轮补齐） | 六家均有「SDS 实现与强弱落地」小节；draw-drawio 本轮新增该节 |
| **L5** | `draw-*/references/*.md` 完善并输出最终图表 | **就位，但分布极不均衡** | md 文件数：mermaid 51、plantuml 49、excalidraw 14、d3js 8、echarts 8、drawio 3、前门 3 |

### 查明的结构缺口（**未在本轮修**，遵守「不做大改」）

1. **语义层内容多副本存放在语法层**（最大缺口）：13 个语义类文件名被 >1 家 specialist 各自持有 —— `00-semantic-analysis.md` 与 `index.md` **三家各一份**（excalidraw / mermaid / plantuml）；`layout.md`、`style.md`、`content.md`、`diagram-principles.md`、`large-diagram-playbook.md`、`01-choose-diagram-type.md`、`01-uml-overview.md`、`08-modeling-methodology.md`、`09-grasp-principles.md`、`10-architecture-diagram.md`、`11-wbs-diagram.md` **mermaid 与 plantuml 各一份**。前门 `references/` 持有其中 **0 份**。这是 One Source Of Truth 风险（多副本可各自演化），已在新增的五层小节内**如实登记**，搬迁仍属结构重构待办。
2. **五家 specialist 缺 `## Self-Improvement Contract`**（`draw-{d3js,echarts,excalidraw,mermaid,plantuml}`；前门与 drawio 有）。契约测试只对该节强制于 `SUBJECT_CREATORS` 四家，故不报红 → 路由 `improve-skills`。
3. **六家 specialist 的 frontmatter 触发词仍未收敛**：d3js 30 / echarts 33 / excalidraw 20 / mermaid 63 / plantuml 65 条，通用图类违规词 11 / 11 / 2 / 13 / 13。drawio = 6 条 / 0 违规（出生即收敛）。按用户「先落 routing-matrix.md」裁定，与结构重构一并落地。
4. **`draw-excalidraw/scripts/server/node_modules`（13,952 文件）被镜像进 `.specify/skills/`**。源头 git-ignored、零 tracked 文件，故不入库；但 `sync-mirrors.py` 仍整树复制，使每次 sync 搬运上万文件。**未动**：修在 `scripts/python/sync-mirrors.py`（排除规则），属框架源、在本团队 `territory.write` 之外；删源目录会破坏该技能的渲染服务依赖。→ 移交框架侧处理。
5. **`draw-mermaid/server/`** 位于技能根，而 `draw-excalidraw/scripts/server/` 在 `scripts/` 下 —— 同类资产两种摆放，属结构重构期的归位对象。

### 本轮附带查明（上一轮审查已记，此处不重复处置）

- 3 个技能有 `## Feedback` embed 却无 probe Object：`skill:git-fleet`、`skill:git-server-init`、`skill:merge-skills`（仅 `--action probes --validate --reconcile` 报出；`--validate` 单独跑不报）。与 draw 系无关。
- discoverability **无需** `/speckit.instructions`：`.specify/skills.md` 自声明「not a registry: nothing here needs to be updated when skills are added or removed」，`.github/skills` 符号链接已能解析到 `draw-drawio`。

## 过程细节

1. **前置**：engine `list --json` 枚举 archive（命中 `draw-two-layer-structure`，`active`）→ 读 `team.md` frontmatter（`pattern: serial`，无 `focus_target`）→ 五项检查全过 → Target 指派 = 无。
2. **误写与恢复**：见上「严重事件」。恢复后才继续执行，未带着被破坏的判据往下走。
3. **梳理**：机械盘点七技能文件树（首轮被 excalidraw 的 `node_modules` 刷屏，改为 `-not -path "*/node_modules/*"` 重跑）；核对 L1/L3 承载（前门 headings、六引擎名册、LDM/SDS 提及数）；核对 L4（六家「SDS 实现与强弱落地」小节与 `sds-realization.md` 存在性）；量化语义层多副本（13 个文件名 × 持有者）。
4. **词汇连续性核对**：前门产出 **LDM**（其几何层即 **SDS**），五家引擎以 `sds-realization.md` 消费 SDS，`semantic-model.md` 同时使用两词并给出偏离声明分类 —— L1→L4 词汇**连续**，无断点。drawio 原缺该环节，本轮补齐。
5. **小修（4 处）**：前门 description 名册 +`drawio`；`semantic-model.md` 偏离声明第 1 类 +drawio；`draw-drawio/SKILL.md` 新增 SDS/强弱节；前门新增五层披露节。每处均机械断言（存在性、条数、触发词不变）。
6. **发现并写入的一条非显然约束**：drawio 的 SDS 节明确「**SDS 给了几何就不得开启会移动 vertex 的 pass**」—— `postLayout=elk` / `applyLayouts` / CLI `--layout` 都会重排节点，启用即等于推翻 SDS box、属改写语义；`routing=libavoid` 可用（官方明确 vertex 不动）。这是上一轮从官方文档查实的布局 pass 事实与本团队 SDS 契约的**交叉推论**，两处单独看都正确、合起来才暴露冲突。
7. **收尾**：`sync-mirrors.py --write` → 三个改动文件镜像逐字节核对 → 契约套件 → probe `--validate` → 本报告。工作区中间产物（含上一轮的 `smoke-drawio/`）留在 git-ignored 的 `.specify/teams/.work/`。

## 静态结构（Role × Stage × Type）

| Agent | Role | Stage | Type | Lifecycle | 本轮是否派发 |
|-------|------|-------|------|-----------|-------------|
| `agent-team-supervisor-template` | team-supervisor | meta | **Meta** | persistent | 是（唯一 canonical 写入者） |
| `structure-adjuster` | structure-refactorer | executor | Worker | temporary | **否** |
| `skill-verifier` | cleanroom-verifier | evaluator | Worker | temporary | **否** |
| `agent-stage-executor-template` | evaluation-intake | executor | Worker | temporary | **否** |

**未派发 Worker 的理由**：本轮是「梳理与检查 + 四处小修」，用户明确「不需要做太大的修改」。四处小修全部是 canonical 写入面的事（`skills/draw-diagram/**`、`skills/draw-drawio/**`），按团队纪律 **Meta 是唯一 canonical 写入者**，Worker 只写工作区草稿；把 4 处小修改交给 Worker 再回收，只会多一次交接与一次验证门，不产生质量增益。S2（净室自测）与 S3（评价采集）本轮**未执行**：S2 需要真实出图，而渲染问题（轴② / 共享渲染服务）用户已裁定在**另一条流程**处理，此时跑 S2 会把渲染未决当作技能缺陷误判。

## 动态结构

- **pattern**: `serial`（质量优先）；`handoff_protocol: file-path-only`；`failure_strategy: retry-once-then-escalate`；`verification: independent`。
- **DAG（9 stage，无环）**: `S1a → S1c → S1b → S1d → S1e → S1f → S1g → S2 → S3`，并发根 `S1a ∥ S1c`。
- **本轮在链上的位置**：S1a–S1g 全部已完成并落地（含上一轮 `draw-drawio` 技能本体创建 + 独立审查轮）；**S2、S3 仍未执行**。本轮不是新增 stage，而是 Meta 在 S1g 之后、S2 之前的一次**梳理与检查**，产出为四处小修 + 本报告 + progress 文件更新。

```
S1a ─┐
     ├─→ S1b → S1d → S1e → S1f → S1g → [本轮：梳理与检查 ✓] → S2（阻塞：轴②在另一流程）→ S3
S1c ─┘
```

## 下一步（按优先级）

1. **结构重构**（本轮明确未做）：语义专章从 mermaid / plantuml / excalidraw 上移至 `draw-diagram/references/`，消除 13 处多副本；六家 frontmatter 触发词收敛（217 → 30）；`guide` 六对合并。落地时同步删除前门五层小节里的「当前落地状态」告警段。
2. **轴② 定案后**（另一流程）：`routing-matrix.md` §0 补轴② 取值（6×4 = 24 格）；七份 frontmatter 去掉「不断言轴②」免责句。
3. **S2 净室自测 / S3 评价采集**：待渲染问题定案后执行。
4. **`improve-skills` 批次**：五家补 `## Self-Improvement Contract`；draw-mermaid 的 3633 字节 `## Feedback` 内联变体收敛为指针形。
5. **框架侧移交**：`sync-mirrors.py` 排除 `node_modules`；3 个技能补 probe Object；`goal-utils.py` 的 setter 语义建议在 `--help` 或 `/speckit.goal` 文档中显式标注「破坏性」（本轮事故的根因）。
