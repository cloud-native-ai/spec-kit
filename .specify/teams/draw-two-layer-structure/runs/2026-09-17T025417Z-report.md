# Run Report — draw-two-layer-structure (run 4)

- **Run ID**: 2026-09-17T025417Z
- **Team**: `.specify/teams/draw-two-layer-structure/team.md` · pattern `serial` · handoff `file-path-only` · failure strategy `retry-once-then-escalate`
- **Goal**: `.specify/goal/draw-two-layer-structure/goal.md`（显式 `goal_slug`，status `active`，3 条判据）
- **Target 指派**: 无（goal 整体）
  - 引擎解析：`resolve_effective_target` → `{effective: null, source: "none", declared_focus: null}`（team.md 未声明 `focus_target`）；`parse_goal` → `active`、3 criteria、T-001/T-002/T-003 全 `dropped`（0 个 open）。
- **本次范围**: 结构编辑后的首次 run。S1a–S1e 已 PASS（不重做），从新 stage **S1f-drawio-bootstrap** 起。
- **结果**: **S1f PASS（retry 1 次后过）**；S1g 未启动；S2/S3 locked。**另查出并修复了一处由 Meta 自己在 run 3 落地时引入的 canonical 缺陷。**

## Summary

本次 run 有两件事，第二件比第一件重要。

1. **S1f-drawio-bootstrap 完成**：draw-drawio 技能包草稿 + 2 个 probe Object 提案落到工作区，七条 gate 全过。首派撞 15 轮上限且**零产物**，retry 用「两处 line-ranged 读取 + 写优先」的收窄 brief 一次完成。
2. **查出并修复 Meta 自己引入的缺陷**：run 3 落地评价单时，抽取脚本的正则被**嵌套代码栅栏**截断，导致六个技能的 `## Evaluation Form(绘制评价单)` 段**全部被截断约 57%**——`--action record` 命令块、处置回用步骤、以及「本节绝不自动发送任何内容」这条红线子句**全部缺失**。已修复并重新验证。

## Process detail

### 派发台账（本次 run 共 2 次派发）

| # | Stage | 结果 | 说明 |
|---|-------|------|------|
| 1 | S1f（首派） | 撞 15 轮上限，**零产物** | brief 要求先读 7 份文档（含已增长到很长的 progress.md 与 14.5KB 的 frontdoor-destination.md），预算耗尽在读取阶段，从未进入写入 → 增量落盘 gate 无从保护 |
| 2 | S1f（retry） | **完成 → PASS** | brief 收窄为**恰好两处 line-ranged 读取**（team.md 195–207 自身 stage 定义；draw-plantuml SKILL.md 144–177 两段尾节），其余事实全部内联；并改为**写优先**（先写 16 行的 probe 提案保证一个完整产物，再增量写技能包）→ 370 行 / 31600 字节 + 16 行 / 1683 字节 |

**首派失败归因（Meta 自己的 brief 缺陷，非子代理能力问题）**：run 3 已确立「续做式 retry + 每单元读取预算上限」有效，但本次首派 brief **违反了这条自己的教训**——把 7 份文档列为必读且未给行号范围。另有一处 brief 歧义：「write each reference-file draft as you finish it」可被读成「创建多个独立文件」，而两个 outputs 实为**单份工作区 markdown、技能包以 fenced block 内嵌**。retry brief 显式澄清了这一点。

### 交接验证门 F（S1f → S1g 的 `inputs_from` 契约）

**通过**。S1g 需要的是「drawio 已存在且其轴位置可判定」：S1f 产出的技能包已声明自身引擎身份、边界、与 12 个前门 owner 路径的指针关系，且明确**不含任何「何时该选我」的路由叙事**（该判据留给 S1g 写入 `routing-matrix.md`）——正是 S1g 需要的输入形态。

### S1f gate 逐条判定（全部机械核验，不采信子代理自述）

| gate | 判定 | 证据 |
|---|---|---|
| ① 技能包 + create-skills 形态 | **PASS** | 3 个文件各有一行职责；frontmatter 恰为 `name`/`description`/`skill_id` 三键，`skill_id: "<SKILL:.specify/skills/draw-drawio/SKILL.md>"` 逐字相符 |
| ② 五类引擎知识逐类有着落且点名承载文件 | **PASS** | 产物内有专节「五类引擎知识 → 文件归属」：①② → `references/mxgraph-format.md`，③④⑤ → `references/render-and-export.md` |
| ③ 语义层零内置，≥3 处为指针非复写 | **PASS** | 机械抽取得 **12 个不同的 `skills/draw-diagram/` owner 路径**；抽查证实为真指针：`fillColor`/`strokeColor` 的**取值**明确判为配色决策、指 `guide/style.md`（标 planned）；几何数值明确标为示意、真值指 `howto/10-layout-planning.md` + `guide/layout.md` |
| ④ 触发词全为引擎身份，通用图类词计数 0 | **PASS** | 机械抽取声明段得 6 条：`drawio`/`draw.io`/`.drawio`/`mxgraph`/`mxGraphModel`/`mxGraph XML`；对 28 个通用图类词做包含匹配 → **offenders = 0**；整个 description 内的反引号 token 仅另有 2 个前门路径，无通用词。对比：mermaid 63 条中 60 条越界、plantuml 65 条中 64 条越界 |
| ⑤ 两段并存、评价单在前、不混称、Feedback 为指针形 | **PASS** | 顺序经位置比较证实；`## Feedback` **2560 字节**、指向 `.specify/shared/workflow/feedback-step.md`、**不含 `Nesting rule`**（draw-mermaid 3633 字节内联变体的标记）→ 属多数派紧凑指针形 |
| ⑥ 2 个 probe Object 行格式 + 插入位置守「wrap-up 在前」 | **PASS** | 两行均为注册表真实列格式 `object_id \| class_id \| unit \| lifecycle_point`；wrap-up 行明确插在 `skill-draw-d3js-wrapup` 与 `skill-draw-echarts-wrapup` 之间，evaluation-form 行插在表末 `gate-*` 行之后，并附「行序即解析优先级」的理由 |
| ⑦ 增量落盘为 gate 条件、非桩 | **PASS** | 370 行 / 31600 字节 + 16 行 / 1683 字节；fence 配平 6/6 |

**约束 3（不臆造语法）遵守情况的核验**：初查用英文标记词只找到 2 处，与自述的 16 处不符；改查实际用词后证实为 **7 条 XML 未决项**（外层容器元素、edge 是否需 `parent`、文本载体属性、`mxGeometry` 显示标记、连线样式 key、完整样式 key 目录、文件级属性）+ **9 条渲染/CLI 未决项**（其余 CLI 开关与 `-x` 语义、CLI 输入接受面、失败面、容器附加要求、`viewer-static.min.js` 获取方式、postMessage 协议、自托管部署、导出格式完整性、各平台二进制获取与许可）= **16 条**。**是 Meta 的核验用词错了，不是子代理少报。**

**territory**：两次派发后 `git status --short skills/ shared/ templates/ .specify/shared/` 全空；`skills/draw-drawio/` 未被 Worker 创建（符合 Worker/Meta 写权限分离）。

## 本次 run 查出的 canonical 缺陷（Meta 自己引入，已修复）

**缺陷**：run 3「第一步落地（评价单）」时，`land-evaluation-form.py` 用
`re.compile(r"...```markdown\n(?P<body>.*?)\n```", re.S)` 抽取设计稿的六段嵌入文本。设计稿的外层栅栏是 ` ```markdown `，而段内**嵌套了两个 ` ```bash ` 块**（引擎调用命令）。非贪婪 `.*?` 遇 `\n``` ` 即止 → **在第一个内层栅栏处截断**。

**影响面**：六个 `skills/draw-*/SKILL.md` 的 `## Evaluation Form(绘制评价单)` 段各只剩 **10 行 / 约 1.1KB**（应为 29 行 / 约 2.5KB），**缺失约 57% 内容**，具体丢失：
- `feedback-utils.py --action record` 的完整调用块（含 `--lifecycle-point evaluation-form`、`--review-file`、`--points-file`）
- 「这会经 `<object>` probe 把评价条目写入 `.specify/memory/feedback/`」及 run_id 不得与自省共用的说明
- **「处置、回用与传递边界」整段**——含 `--action list --disposition processed` 的回用查询
- **「本节绝不自动发送任何内容」这条红线子句**

即：run 3 报告所声称的「判据 2 的判定口径在文本中可执行」**在落地后的 canonical 文本里并不成立**——段落在「随后以本节的 probe 记录：」处戛然而止，没有任何命令。该结论当时是**对着设计稿**验证的，不是对着落地结果验证的。

**为何当时没查出**：run 3 的落地后核验只查了**段落存在性、数量与顺序**（各 1 个评价单段 + 各 1 个 Feedback 段 + 评价单在前），未查**内容完整性**。这正是 `AGENTS.md` 记的那类潜在缺陷——「files exist / headings present」型检查漏掉只在运行时暴露的问题。

**如何暴露**：S1f 的子代理在其未决项里如实记下「`skills/draw-plantuml/SKILL.md` 144–154 的该段止于『随后以本节的 probe 记录（不是以 `wrap-up` probe 记录）：』，其后的实际调用块不在可见范围」，并**拒绝臆造**、改为复用 `## Feedback` 段已确证的调用形态。它把自己的读取范围当成了限制，实际上它读到的是**被截断的 canonical 文本**——这条诚实的未决项记录成了缺陷的探针。

**修复**：`repair-evaluation-form.py`（工作区）改用**栅栏感知**抽取——在一个 `## \`skills/draw-X/SKILL.md\`` 区间内，外层闭合是该区间内**最后一个**裸 ` ``` ` 行（内层 bash 栅栏同为裸 ` ``` `，故只有最后一个是外层闭合）。先 dry-run 校验六段均含 `--action record` / `绝不自动发送` / `--action list` 三个标志，再 `--apply`。

**修复后核验**：六技能评价单段各 **30 行**；`--action record`=1、`绝不自动发送`=1、`--action list`=1、`--lifecycle-point evaluation-form`=1；每技能的 `skill:draw-<name>` 与 `skill-draw-<name>-evaluation-form` 各自对应正确。`sync-mirrors.py --write` exit 0（6 文件）；`--action probes --validate` exit 0（6 classes / 81 objects）；定向契约测试 **55 passed / 1 failed**，唯一失败为既有基线项 `test_shared_reference_rewrite`（offender 为 improve-agent / create-team / create-agent，与 draw-* 无关）。

**连带影响（须在 S1f 落地时处置）**：draw-drawio 草稿的评价单段是**从 `## Feedback` 形态反推重构**的（1766 字节），因为它读到的 plantuml 段是截断的。落地 drawio 时 MUST 改用**修复后的 canonical 完整形态**（自 draw-plantuml 现段适配，替换 unit / object id），而非直接落草稿里的那一段。草稿自己的未决项已写明「落地时与该技能对齐后再定稿」，与本判定一致。

## 遗留待办

1. **S1g-routing-axis 未启动**（blockedBy S1f 已满足，下次 run 可直接起）。它要把四条能力轴落进 `routing-matrix.md` 并给出 drawio↔excalidraw 的可判优先序。
2. **run 3 的第二步（六技能结构重构）仍未落地**：`frontdoor-destination.md` 的 16 槽位（14 新文件 + 2 existing 合并）、`guide-merge-ruling.md` 的六对裁定、mermaid 63→3 / plantuml 65→3 的触发词收敛、specialist 语义段替换为指针。与 S1f/S1g 同属 canonical 写入面，宜一次规划。
3. **draw-drawio 落地**（Meta 步）：经 `create-skills` 生成 canonical 骨架 → 落草稿 → 注册 2 个 probe Object → `sync-mirrors` → `--action probes --validate` + `--reconcile`。落地前 MUST 先核实草稿的 16 条未决项（尤其「外层容器元素」与「edge 是否需 `parent`」两条高优先项——它们决定最小骨架是否足以合成有效文件），核实来源为真实 `.drawio` 文件或官方文档，**不得凭合理长相补写**。
4. **`goal.md` 的 Objective 仍枚举五个引擎**，与判据 1 的「七个绘图技能」矛盾。`goal-utils.py` 无 `objective` 动作 → 无合法修复路径；建议给引擎补 `objective` 动作（照 `set_criteria` 语义：替换 + History 留痕 + bump updated + 终态只读）。team.md 的内联 `goal` 渲染亦待定义修正后同步。
5. **沿自前次 run**：`/speckit.instructions` 同步可发现性；`viz-skill-arena` 的悬空 `goal_slug`（按裁定不动）；两团队在 `skills/draw-*/**` 的文件级 territory 重叠（`goal_slug` 不同故 `verify-territory-disjoint.py` 检不出）→ 不要并发运行。

## Summary refresh

- **Goal 身份**：`draw-two-layer-structure`（显式 `goal_slug`，非推断）；**交付目录**：`.specify/goal/draw-two-layer-structure/summary/`
- **本次决定**：**不刷新**，抑制门为 `no-material`。本次 run 产出为工作区设计稿（S1f）+ 一处 canonical 缺陷修复；缺陷修复是把已声明的判据 1 承载面**恢复**到设计稿本来的样子，不是新增达成度。判据 3 的两个度量（无评价占比、已处置占比）仍无真实条目可算——七技能尚无一次实际绘制运行产生评价。

## Self-assessment

- **做错的（本次最重要的一条）**：run 3 落地时写了一个会被嵌套栅栏截断的抽取正则，并且**只核验了段落的存在性与顺序、没核验内容完整性**，据此在 run 3 报告里断言「判据 2 的口径在文本中可执行」。该断言对设计稿为真、对落地结果为假。缺陷进入 canonical 框架源码并被提交，直到本次 run 一个子代理诚实记录未决项才暴露。**教训两条**：① 从含嵌套栅栏的文档抽取内容，MUST 用栅栏感知解析（区间内最后一个裸栅栏为外层闭合），不得用非贪婪 `.*?` + `\n``` `；② 落地类操作的核验 MUST 包含**内容完整性**（抽取产物的关键标志串逐一在场），只做「存在/数量/顺序」检查等于没查。
- **做错的（第二条）**：S1f 首派 brief 违反了 run 3 自己确立的教训——列出 7 份必读文档且不给行号范围，导致预算耗尽在读取、零产物。retry 用「两处 line-ranged 读取 + 事实内联 + 写优先」一次通过。
- **做对的**：全程机械核验。本次两处「子代理自述与 Meta 初查不符」经复查**都是 Meta 的核验方法错了**（未决项用中文标记词而非英文；`grep -v '^[+-][+-]'` 会滤掉新增 History 行），不是子代理报错——两次都改了核验方法而不是改结论。发现缺陷后没有掩盖，直接定位根因、写修复脚本、dry-run 后 apply、再全量重验。
- **仍未解决的**：15 轮派发上限依旧是结构性约束。本次 2 次派发中 1 次撞顶（50%）；累计 8 次派发中 4 次撞顶。缓解手段（增量落盘 + 续做式 retry + 读取预算上限 + 写优先）已稳定生效，但**任何单 stage 仍应假定需要 ≥2 次派发**。用户此前两次未采纳「提高派发轮次预算」。
