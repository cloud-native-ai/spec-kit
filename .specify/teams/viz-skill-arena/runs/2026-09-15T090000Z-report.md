# Run Report — viz-skill-arena Cycle 4

**Date**: 2026-09-15T09:00:00Z
**Team**: viz-skill-arena (continuous, L2, on-demand)
**Goal**: 语义/语法分离的六技能绘图体系竞技 + 两轮多裁判评审 + 一次重绘，建立「需求→技能」匹配知识
**Target 指派**: 无（对 goal 整体运行）
**Task**: 复刻三区域部署拓扑图（SDS 驱动：语义层产几何+weight_plan，5 语法层并行实现）
**架构**: 本 cycle 首次运行「语义/语法分离」新架构——draw-diagram 产 SDS（逻辑模型+几何+视觉权重+排版层级+auto_layout_intent），draw-* 只做语法实现与偏离声明

## 结构变更（cycle 前）

- team.md 重构：新增 semantic-modeler 成员（draw-diagram）；5 个 drawer 职责改写为 SDS 语法实现者；Phase A0（语义建模）插入执行流；territory/co_targets 增 skills/draw-diagram
- 六技能分离重构：5 个语法层各增「SDS 实现与强弱落地」小节 + references/sds-realization.md（tier 映射 T1–T4、几何实现/逼近与偏离声明规则）；draw-diagram 增几何层所有权与 weight_plan 术语统一（T3=数据流、T4=注释）

## Execution Summary

| Phase | Description | Subagents | Status |
|-------|-------------|-----------|--------|
| A0 | semantic-modeler 产 SDS（像素测量：canvas 2000×1257、21 元素、3 等高分区、5 关系、源测权重覆写） | 1 | COMPLETE |
| A | 5 语法实现并行 | 5 | COMPLETE（绝对坐标三引擎零偏离；mermaid 8 项/plantuml 7 项量化声明） |
| B | R1 三裁判（对 SDS 判覆盖） | 3 | COMPLETE（d3js 0.956 领先） |
| C | supervisor 写 6 份工作副本改进单 | 0 | COMPLETE |
| D0 | SDS v2：增 auto_layout_intent（fallback_rank_order/routing_intent/equal_height_priority） | 1 | COMPLETE |
| D1 | 重绘（mermaid 被用户中断拒绝，4/5 完成） | 4 | PARTIAL（按用户裁决收尾） |
| E | R2 两独立裁判 | 2 | COMPLETE |
| F | 合并/账本/report/STATE/run-log | 0 | COMPLETE |

**Subagents dispatched**: 16（预算 16 上限内）

## R1 → R2（归一化）

| Skill | R1 | R2 | Δ | No-Regression | 采纳 |
|-------|-----|-----|-----|---------------|------|
| d3js | 0.956 | 0.95 | +0.004 | PASS | ✅ 冠军 |
| echarts | 0.936 | 0.92 | +0.004 | PASS | ✅ |
| excalidraw | 0.914 | 0.89 | +0.006 | PASS | ✅ |
| plantuml | 0.778 | 0.81 | +0.062 | PASS | ✅（未达冠军线但采纳） |
| mermaid | 0.699 | 0.59 | — | N/A（无重绘） | ❌ 不采纳 |

## Champion

**draw-d3js（R2 0.95）**：SDS YAML 以 sha256 钉扎内嵌为唯一几何源（零内联字面量坐标）、12/12 in-page QA + 光栅读回、渲染与 target 视觉不可区分。
Runner-up：echarts 0.92、excalidraw 0.89（精确坐标族）；plantuml 0.81 = 仅允许声明式语法时的最佳自动布局回退。

## 分离契约验证结论

1. **语义源单一**：裁判对 SDS 判覆盖而非对原图猜测；SDS 建模被 R1-semantic 评为 exemplary
2. **偏离声明机制生效**：自动布局引擎的降级被量化(checkable)——plantuml 重绘后 |dx| 217→39.5px、7 项全在界内；mermaid 的 +216px Δy 超界被如实声明
3. **零偏离从声明变证据**：d3js/echarts/excalidraw 的 readback QA 链（像素读回 62/62、12/12 DOM checks）使"零偏离"可验证
4. **auto_layout_intent 价值**：D0 补入后 plantuml 的等高分区 spread 0.0、秩序错位逼近生效

## Adoption

- 合并回 canonical：draw-d3js / draw-echarts / draw-excalidraw / draw-plantuml 的 `references/cycle4-improvements.md`（dated record）+ draw-diagram `references/semantic-model.md`（auto_layout_intent schema）；镜像 `.specify/skills/` 与 `.qoder/skills/` 已同步
- 不合并：draw-mermaid 工作副本变更（无重绘=未验证）

## 遗留 / 下轮方向

- mermaid：ghost-chain n03/n04 共秩 + 边直线化（本轮改进单已写好未验证）
- 语法层 readback QA 提升为必备项（本轮仅三引擎自发采用）
- cycle-4 全部变更尚未 git 提交（待用户指示）
