# 🌱 Spec Kit Feature Index

**Last Updated**: 2026-09-17
**Total Features**: 51

## Features

| ID | Name | Description | Status | Feature Details | Spec Path | Last Updated |
|----|------|-------------|--------|-----------------|-----------|--------------|
| 001 | Unify Command Handoffs | Make command prerequisites and next steps explicit. | Completed | .specify/memory/features/001.md | .specify/specs/001-unify-command-handoffs/requirements.md | 2026-08-15 (audit: delivered — ## Handoffs section present on all 23 command templates; row was stale-Planned) |
| 002 | Analyze Command | Analyze project context and code structure. | Completed | .specify/memory/features/002.md | - | 2026-02-10 |
| 003 | Checklist Command | Verify project alignment with constitution. | Completed | .specify/memory/features/003.md | - | 2026-02-10 |
| 004 | Clarify Command | Resolve ambiguities in specifications. | Completed | .specify/memory/features/004.md | - | 2026-02-10 |
| 005 | Constitution Command | Manage project governance and principles. | Completed | .specify/memory/features/005.md | - | 2026-02-10 |
| 006 | Feature Command | Manage feature lifecycle and metadata. | Completed | .specify/memory/features/006.md | - | 2026-02-10 |
| 007 | Implement Command | Generate code from tasks and plans. | Completed | .specify/memory/features/007.md | - | 2026-02-10 |
| 008 | Instructions Command | Generate prompts for AI agents. | Completed | .specify/memory/features/008.md | - | 2026-02-10 |
| 009 | Plan Command | Create implementation plans from specs. | Completed | .specify/memory/features/009.md | - | 2026-02-10 |
| 010 | Requirements Command | Define requirements and specifications. | Completed | .specify/memory/features/010.md | - | 2026-02-10 |
| 011 | Research Command | Gather context and dependencies. | Completed | .specify/memory/features/011.md | - | 2026-02-10 |
| 012 | Review Command | Review implementation against rules. | Completed | .specify/memory/features/012.md | - | 2026-02-10 |
| 013 | Skills Command | Manage extensible skills/tools. | Implemented | .specify/memory/features/013.md | .specify/specs/030-summarize-project/requirements.md | 2026-07-25 |
| 014 | Tasks Command | Break down plans into atomic tasks. | Completed | .specify/memory/features/014.md | - | 2026-02-10 |
| 015 | CLI Interface | Rich terminal interface using Typer. | Completed | .specify/memory/features/015.md | - | 2026-02-10 |
| 016 | Tools Command | Definition-first tool management: create, modify, and invoke tools with explicit behavioral rules, replacing discovery-driven approach. | Implemented | .specify/memory/features/016.md | .specify/specs/016-refactor-tools-command/requirements.md | 2026-06-17 (deferred: T022,T042,T043,T046) |
| 017 | Template Engine | Markdown-based template system. | Completed | .specify/memory/features/017.md | - | 2026-02-10 |
| 018 | Configuration Management | Project configuration via pyproject.toml. | Completed | .specify/memory/features/018.md | - | 2026-02-10 |
| 019 | Agents Command | Create or refine custom AI agents (.agent.md) for workspace-specific workflows. Includes EEI (Executor-Evaluator-Optimizer) triad pattern for iterative quality optimization. | Implemented | .specify/memory/features/019.md | .specify/specs/023-agent-framework-redesign/requirements.md | 2026-07-10 (spec 023 agent-framework-redesign implemented: Role/Stage/Type + Team/Loop model, terminology unification, supervisor merge, template canonicalization) |
| 020 | Qoder Support | Add Qoder as a supported CLI assistant across initialization, validation, documentation, and release distribution. | Implemented | .specify/memory/features/020.md | .specify/specs/006-add-qoder-support/requirements.md | 2026-03-30 |
| 021 | Claude Code Support | Add Claude Code as a first-class assistant with custom commands and Claude Code-specific configuration assets. | Implemented | .specify/memory/features/021.md | .specify/specs/009-claude-code-support/requirements.md | 2026-05-14 |
| 022 | AI Tools Support | Ensure all officially supported AI tools receive complete initialization coverage and can coexist without overwriting shared Spec Kit core files. | Implemented | .specify/memory/features/022.md | .specify/specs/021-agent-specific-config/requirements.md | 2026-07-12 (spec 024 agent-env-config implemented: unified env-var one-time config persisted to 6 CLI tools' own config files) |
| 023 | Prompt Template Quality | Structural validation and consistency enforcement across all command and skill templates. | Draft | .specify/memory/features/023.md | - | 2026-06-05 |
| 024 | Specification Workspace Versioning | Version management and migration support for .specify/ workspace structure across CLI releases. | Draft | .specify/memory/features/024.md | - | 2026-06-05 |
| 025 | Todo Command | Discover marked TODO blocks in text files and turn them into reviewable execution plans. | Implemented | .specify/memory/features/025.md | .specify/specs/020-speckit-todo-command/requirements.md | 2026-06-23 |
| 026 | Agent Skill Enablement | Empower the 7 built-in role agents to prefer installed framework skills for role-relevant operations, since skills and agent definitions install together. | Implemented | .specify/memory/features/026.md | .specify/specs/025-agent-skill-enablement/requirements.md | 2026-07-13 (spec 025 implemented: skills: frontmatter + ## Skill Enablement section on all 7 role agents + create-agent templates; contract test_agent_skill_enablement.py green) |
| 027 | Team Management | Dedicated team domain for multi-agent configuration: `/speckit.team` (create/modify/run) as the sole entry point, `create-team` (renamed from organize-agents) + `improve-team` skills, Conceptual Model extracted from create-agent, and single-agent vs team separation. Extended by requirement 036 with a team summary information-management mechanism under a **dual index**: `.specify/teams/<team-slug>/` keeps per-team run info, while `.specify/project/goal/<goal-slug>/` holds the sole complete summary aggregating every team that declares the same `goal_slug` (team↔project-management concept mapping, phase-boundary triggered summarize-project invocation, cumulative cross-team aggregation). Extended by requirement 042 with a goal-based team creation flow in `/speckit.team` create: archive-goal recognition → goal content analysis (advisory) → Target decomposition proposal set ratified via `/speckit.goal targets --add` (goal side unchanged, propose→ratify) → one team per Target carrying a `focus_target` default focus plus pairwise-disjoint territories. | Implemented | .specify/memory/features/027.md | .specify/specs/036-team-summary/requirements.md | 2026-08-04 (requirement 036-team-summary IMPLEMENTED via /speckit.implement — 119/120 tasks closed, 1 deferred (T049, needs a live user-gated team run); 201 new tests green, zero new failures vs baseline (39->39 by name, passes 1108->1309); all 8 Completion Gate items re-validated against the tree; 13/15 SC pass + 2 partial (SC-006 awaiting the live run, SC-010 awaiting a human reader check). Earlier: planned + decomposed; requirement Draft→Planned, Feature stays Implemented per the no-status-regression gate. Phase 0 verified feasibility by **executing** the summarize-project script chain against a real team: R-tier satisfiable from tracked team artifacts alone with zero manual form entry; the DDL identifier constraint, upstream zero-percent/no-Gantt degradation, and the cross-team ID collision were all confirmed by execution rather than inference. Scope-revised the same day to a dual index (goal-indexed aggregation) under the Scope Revision Protocol: 36 FR / 15 SC / 6 user stories / 6 shared strings, 120 tasks across 9 phases, research.md + data-model.md + 4 contracts + quickstart.md + feature-ref.md; Constitution v1.9.0 check 12 Pass / 1 Partial (IX, justified: 1 generator script + 1 per-team ledger). Bound via /speckit.clarify — realizes 027's own Future Evolution item "Team run history/observability under .specify/teams/"; historical spec .specify/archive/spec/026-agent-team-management); 2026-08-17: requirement 042-goal-team-creation planned (goal-based create branch + focus_target run resolution + creation-time territory disjointness; goal-utils +targets --check/resolve_effective_target, +verify-territory-disjoint.py thin wrapper over the existing overlap grammar; 4 contracts; Feature stays Implemented per no-status-regression gate); 2026-08-17 (later): requirement 042 **implemented** via /speckit.implement — 33/33 tasks closed, SC-001..006 all pass, zero new failures vs the 46-failure name-level baseline (+48 tests across 3 new contract files), every Mirror Obligations row verified, end-to-end quickstart walkthrough green in an isolated root (verification.md) |
| 028 | Feedback Mechanism | Feedback Probe–based local feedback layer (req 041 refactor): every wrap-up embed is an explicitly defined Probe Object (Class/Object two-layer, internal/external) in `shared/definitions/probe-definitions.md` (3 Classes + 50 embed-reconciled internal Objects); entries auto-inherit probe/kind/slice and filter by slice/kind/disposition; `/speckit.feedback` three-mode management command (probe overview / process incl. post-package cleanup / external-probe injection); external probes stay host-project-local (Loop B), 100% excluded from upstream packages; legacy store converged via logged one-shot migration; red lines scoped by kind, zero automated transmission. | Implemented | .specify/memory/features/028.md | .specify/specs/027-feedback-mechanism/requirements.md | 2026-08-14 (requirement 041-refactor-feedback-probe IMPLEMENTED via /speckit.implement — 34/34 tasks, 0 deferred; engine 7→13 actions; SC-001..008 all pass; zero new failures vs 38F baseline; sync-mirrors exit 0; this repo migrated 140 legacy entries, legacy_remaining=0. Planned entry + design rationale in features/028.md); 2026-08-28: requirement 047-feedback-introspection IMPLEMENTED via /speckit.implement — 21/21 tasks, 0 deferred; engine 13→14 actions (+introspect-register; dispose --reason/--ref; package --include-introspection); /speckit.feedback 4→5 modes; SC-001..004/006 pass + SC-005 deferred (post-adoption); 42 new contract tests green; zero new failures vs 47F baseline (name-level); sync-mirrors exit 0; quickstart E2E green |
| 029 | Shared Reference Directory | Reclassify `sdd-workflow` from a (non-invocable) skill into a dedicated shared reference directory (`shared/workflow` → `.specify/shared/workflow`) copied wholesale at init like templates/scripts, retained across re-init, with a `.specify/` path-rewrite rule; remove it from the skills registry/count/symlinks and rewrite ~100 references, with a zero-`sdd-workflow`-reference acceptance gate. | Implemented | .specify/memory/features/029.md | .specify/specs/028-sdd-workflow-refactor/requirements.md | 2026-07-14 (implemented: 10 docs relocated to shared/workflow/; skills 20→19; ~38 source refs + 70 generated command files rewritten; .specify/shared installed & retained; SC-001..006 pass; pytest 104F/643P = 0 new failures vs baseline) |
| 030 | History Command | `/speckit.history` distills the current AI tool's past conversations for the current project into a theme-aggregated knowledge base under `.specify/history/` (decisions, reusable lessons, TODOs, interaction flows, user↔model conflicts — not verbatim). Incremental via a manifest; Claude Code supported today with a pluggable `STORE_RESOLVERS` adapter for other tools. | Implemented | .specify/memory/features/030.md | - | 2026-07-14 (implemented: history-utils.py engine + collect-history.sh + templates/commands/history.md + 4 runtime command mirrors + docs/commands/history.md) |
| 031 | Glossary Mechanism | Single project-wide glossary (`.specify/memory/glossary.md`) anchoring project vocabulary and correcting voice/dictated input (homophones,易混淆词); initialized at instruction generation, ambient to all commands via the Documentation Map, progressively enriched at checkpoints with user-confirmed conflict handling and user-authoritative manual edits. | Implemented | .specify/memory/features/031.md | .specify/specs/029-glossary-mechanism/requirements.md | 2026-07-16 (implemented: glossary-utils.py engine + glossary-template + shared/workflow/glossary.md protocol + generate-instructions init hook + Documentation Map wiring + `## Glossary` step on 4 commands & instructions seeding; 23 tests pass, 0 regressions; SC-006 deferred as post-adoption metric) |
| 032 | Task Complexity Rubric | Embed a technology-agnostic task-complexity rubric in the generated instructions doc (`.specify/instructions.md`) mapping observable task signals → complexity tiers → prescribed thinking depth, so agents calibrate effort (avoid under-thinking complex tasks and over-thinking trivial ones) to balance efficiency and quality; delivered via `/speckit.instructions` non-destructively. | Implemented | .specify/memory/features/032.md | .specify/specs/031-task-complexity-rubric/requirements.md | 2026-07-20 (implemented: `## Task Complexity Rubric` section added to `templates/` + `.specify/templates/instructions-template.md` (byte-identical mirror); contract test `tests/contract/test_task_complexity_rubric.py` 10/10 pass (C-1…C-10); full suite 720P/106F/13E/1S = +10 passed, zero new failures vs baseline; SC-001/002/005 pass, SC-003/004 deferred post-adoption; T013 dogfood deferred to interactive `/speckit.instructions` run) |
| 033 | Agent Project Context Parameterization | Make the `## Project Context` section of bundled role agents rendered per target project at init/refresh time (project name, detected tech stack, spec inventory), instead of shipping spec-kit's own hard-coded facts into every consuming project; non-destructive refresh preserves user edits outside that section. | Draft | .specify/memory/features/033.md | - | 2026-07-21 (draft registered from the interview-walkthrough rollout to xuanji-ui) |
| 034 | CI/CD Pipeline | Automated testing, building, and publishing via GitHub Actions workflows for push/PR quality gates and PyPI release automation. | Draft | .specify/memory/features/034.md | - | 2026-07-22 |
| 035 | Code Quality Tooling | Python linting, formatting, and type checking configuration for consistent codebase maintenance. | Draft | .specify/memory/features/035.md | - | 2026-07-22 |
| 036 | Dogfooding Practice | Make Dogfooding explicit with zero new machinery: name the principle in the framework constitution (developer≈user tight feedback loops; Spec Kit develops itself via its own SDD workflow, deviations logged) and identify the two existing loops — Loop A: downstream projects feed the framework via the existing Feature 028 feedback chain (record→threshold→package→manual submission); Loop B: projects reuse framework capabilities (feedback engine, memory, history, review, task records) to build their own product feedback loops. Delivered as constitution principle + instructions-template guidance section; advisory only, no gates, no engine/command/storage changes (guarded by contract). | Ready for Review | .specify/memory/features/036.md | .specify/specs/032-dogfooding-practice/requirements.md | 2026-07-25 (implemented: constitution Principle XI (v1.6.0, bootstrap discipline + Loop A/B) + `## Dogfooding Practice` section in instructions-template (byte-identical mirrors) + docs/skills/feedback.md Loop A note; 19 contract tests C-1…C-7 green incl. C-4 no-new-machinery pins; full suite 748P/106F/13E/1S = +19, zero new failures; SC-001…004 all pass; two real dogfooded findings recorded during the run; review.md produced 2026-07-25: 0 P0 / 3 P1 / 4 P2 process findings) |
| 037 | Docs Command | `/speckit.docs` command standardizing and managing the project documentation space via a single reconcile engine (reconcile-pattern.md): thin root-entry layer (uppercase special-name registry README/ARCHITECTURE/CONTRIBUTING/CHANGELOG with fixed semantics, one-screen rule) + typed `docs/` layer (concepts/tutorials/tasks/reference/decisions/contribute) + lifecycle-constrained `notes/` with exit mechanism (frontmatter + draft/expired/archived state machine + deterministic docs-utils automation); ADR append-only management; docs-sync evaluation step injected at the feedback wrap-up point of all complex commands (shared docs-step convention, non-blocking, incremental); archive-in-`docs/`, run artifacts in `.specify/docs/`; dogfooding run on Spec Kit itself with aggressive re-taxonomy; standard Feedback step (reuses 028/036, zero new loop machinery). | Implemented | .specify/memory/features/037.md | .specify/specs/033-docs-command/requirements.md | 2026-07-28 (implemented: templates/commands/docs.md + 5 tool copies + mirror; docs-utils.py engine (7 actions) ×2 mirrors; shared/workflow/docs-step.md injected into 14 complex commands (classification 13→14); Reserved Filenames strict blocking — constitution X v1.7.0 + ADR-0002, 4 nested README→index.md; dogfooding aggressive re-taxonomy of docs/ (15 moves, ADR-0001, README 110→51 lines, 134→0 broken links); docs suites 50/50 green, full 84F/877P vs baseline 83F/827P with all failures attributed pre-existing; SC-001…007 all pass; T037 appended; verification.md complete); 2026-08-10 follow-up: command core logic extracted into the `create-docs` skill (skills/create-docs/SKILL.md ×2 mirrors, registered + propagated to knowledge-manager) — /speckit.docs is now a thin dispatch layer delegating to the skill; contract docs-command-template.md revised (C-4/C-5/C-6/C-9 re-pinned on the skill, new C-4a mandatory-delegation + C-12 skill shape) and test_docs_command_template.py updated (13/13 green); same-day Hugo presentation layer added to the skill (baseline item 5): docs/ doubles as the Hugo project root, content mounted-never-copied (index.md -> _index.md branch-bundle remaps + Markdown-excluding static mounts for media), repo-native relative .md/image links resolved by render hooks, deterministic scaffold-hugo.py (scaffold/check/mounts/build, stdlib-only, zero-churn, never clobbers user edits) + assets/hugo templates + references/hugo-site.md with CI guidance (documentation-only per user decision, no workflow file); contract C-13…C-17 added, 17 new tests green (incl. a real hugo v0.141.0 build); dogfooded on spec-kit's own docs/ — 82 pages built, docs-utils validate zero new violations, and the run surfaced a latent content defect (17 imported `theme={null}` code-fence artifacts in docs/reference/skills/specification.md, cleaned); 2026-08-20 follow-up: presentation decoupled from structure — the Hugo layer (scaffold-hugo.py + assets/hugo/ + references/hugo-site.md) moved create-docs → `create-pages`, which was itself restructured into an **optional** three-stage pipeline (local doc library → Hugo render → pages service: local / aoneci / github); create-docs keeps only the basic structure (baseline item 5 dropped, site-tooling dirs still skipped as non-documentation, site requests handed off); contract C-13 narrowed to that skip rule and C-14…C-17 withdrawn, C-18 now splits three ways (structure / optional presentation / content); `_OBSOLETE_SKILL_FILES` added so init reclaims the moved paths in upgraded workspaces; 2026-08-24 follow-up: create-pages stage 2 became theme-aware with navigation completion — **Hugo Book** (`alex-shpak/hugo-book`, pinned `v0.14.0`, needs Hugo ≥ 0.158) is the preferred renderer, vendored runtime-only under `docs/themes/book` by the new `--action theme [--fetch]` (the sole networked step; snapshot carries no `.git` so CI needs neither Go nor network, provenance in `.speckit-theme.json`, theme Markdown dropped so `docs-utils validate` stays clean), with the self-contained layouts kept as the `--theme auto` fallback (network/old-Hugo degradation); navigation is completed from the live docs tree **without writing any documentation** — a managed `[[cascade]]` supplies reading order (concepts→tutorials→tasks→reference→decisions→contribute→notes→archive), generated section labels (acronym-aware) and `bookCollapseSection` above `--collapse-threshold` (default 6), while one mounted stub (`docs/.speckit/nav/section-index.md` + `speckit-children` shortcode) restores every Markdown-bearing directory that lacks an `index.md` as a real section with a build-time child index; measured on spec-kit's own docs/ before the change: 5 of 10 directories produced no sidebar group at all (Hugo only sections a nested dir with `_index.md`), the home page rendered blank, and labels were humanized file names; mode switches are atomic (`mode-mismatch` writes nothing without `--force`; untouched other-mode layouts `removed`, edited ones reported `stale_edited`); label override reads `.RawContent` because the theme's heading hook makes a rendered H1 plainify to `Title#`; fixed a pre-existing builtin-mode defect (`.Site.Language.Locale` needs Hugo ≥ 0.158 and broke every page on v0.141.0 — now `.Site.Language.Lang`, which un-fails `test_real_build_renders_links_and_media`); verified by real builds (builtin on hugo v0.141.0; book on v0.163.2 + hugo-book v0.14.0 in the CI image — zero dead sidebar entries, nested groups restored, `docs-utils validate` zero violations) and 15 new tests (contract 18, integration 19, 37/37 green; full suite 2162P vs baseline 2103P with all 44 failures attributed pre-existing); same-day follow-up: **local builds run in the CI image** — `--action build` defaults to docker (`--runner` accepts auto, docker, or local) with the image resolved flag > `SPECKIT_HUGO_IMAGE` > rendered CI pipeline > new shared `ci-templates/hugo-image.txt` that `scaffold-ci.sh` also renders from (single source of truth, so stage 2 and stage 3 cannot drift), new `--action image` reports the resolution, workspace bind-mounted at /workspace with a visibility probe (`workspace-not-visible` instead of a silently empty site) and honest fallback reporting (`runner: local` + warning, `image-unavailable`, `docker-not-installed`); the theme's Hugo floor now gates on what `hugo.toml` actually selects (`theme.config_mode`) rather than what sits in themes/, and the docs correct the enforcement source (theme.toml `min_version` is the author's declaration; Hugo enforces via the theme's hugo.toml `module.hugoVersion.min`); verified locally: default runner built with the image's hugo 0.163.2 while the workstation binary is 0.141.0, image precedence flag/env/pipeline/registry all exercised, `--user` rejected by this image's entrypoint hooks (documented: container runs as root, output root-owned); +4 tests (41/41 pages suites, full suite 2167P, same 44 pre-existing failures); 2026-09-01: requirement 049-docs-reconcile **clarified & bound here** as the fourth follow-up — 命令层从"单技能委托的薄分发层"升级为**目标结构驱动的三段式调协编排**:① 项目专属**目标结构声明**(引用而非复制静态基线;落运行工作区 `.specify/docs/`,跨运行长期契约,与同址按次产物 plans/audit 不同频、不随其轮转)② 现状-目标差异经容忍带先行分解为**分型动作**(类型/目标对象/owning 技能/确认层级)③ 结构类动作分发 create-docs、内容类动作分发 improve-docs——**首次把 improve-docs 纳入命令分发链路**(此前命令模板对其 0 引用,结构收敛与内容质量修复是两条互不知晓的通道);用户带参调用为**叠加而非取代**(基线调协 + 用户要求分解的额外动作同次交付,隐含结构变更经确认回写声明);内容动作量**不设上限**(取单次最大收敛,接受大空间成本不可预期,分发前告知文档数以便中止,中途中止已完成项保留、未开始项转 pending);既有分级门禁、只归档不删除、站点移交 create-pages 全部不变。同日 **planned** via /speckit.plan:plan.md + data-model.md(3 实体)+ contracts ×3(orchestration D-1…D-12 / declaration T-1…T-10 / action-routing A-1…A-9,31 条款)+ quickstart.md(6 场景)+ feature-ref.md,无独立 research.md;Constitution v1.11.0 **14/14 Pass**,Complexity Tracking N/A;设计取向为"零新增面"——声明落 `.specify/docs/target-structure.md` 根级单文件(沿用 `.specify/git-workflow.md` 受管块先例,LLM 撰写非引擎管理,骨架新增 `templates/docs-target-structure-template.md`)、三段全部落在既有 `## Outline` 内(故契约 C-3 六章节枚举免修订)、`docs-utils.py` 零改动、**新增确认门控 0**(声明批准/回写/重设计提议/扇出告知全部并入既有 R4 干跑计划同一门控);强制该取向的实测约束是门控余量为零(`scan-confirmation-gates` total 23 vs SC-002 上限 93×25%=23.25,新增一条即越界,以设计规避而非改写 baseline.json);镜像基线记录:`sync-mirrors --check` 当前 exit 2 系无关的既有 git-fleet 镜像缺失 6 文件(上游 fe70862e),本特性触及四对实测全 ok;实现批次义务:033 契约 C-1/C-4a/C-5/C-7/C-8/C-18 就地修订、improve-docs 的 `.specify/docs/**` "run artifacts" 措辞订正、`docs/reference/commands/docs.md` 更新;Feature 状态**保持 Implemented** 不回退(同 041 吸收 038-goal-target 的处理); 2026-09-02 `/speckit.tasks`:生成 35 项依赖任务(Tests Mode ON,MVP=US1+US2,10 项并行机会,实现期用户修订后 19 FR / 7 SC 全覆盖),随后 analyze 驱动 8 项跨产物修复并在 implement 前完成;状态保持 Implemented,仅刷新阶段演进记录; 2026-09-05 `/speckit.implement`:99/99 目标契约通过,全套 47F/2257P/1S 与基线失败节点完全一致(0 新增);7/7 SC pass;active Qoder Skill 三轮隔离 dogfood 覆盖声明防抖/双技能路由/写作与唯一落位/人类+Agent 固定检索/21→2+19 pending 扇出中止;mirror 维持既有 6 MISS/0 DIFF 且无新增,gates 23/0 violations,docs validate 仅既有 note 状态 residual;Feature 保持 Implemented |
| 038 | Evidence Infrastructure | Public evidence-collection infrastructure ported from Better Harness (source-copy hosted engine under `scripts/js/better-harness/` with UPSTREAM.md provenance + MIT license): a consumer-neutral evidence layer (collect-evidence skill + stdlib-only evidence-utils.py orchestrator) producing a unified `findings.json` contract (7-state evidenceState vocabulary, privacy-redacted, no verdict fields) across 5 lanes (session/project/assets via Node engine; runs/feedback native Python as first-class Spec Kit lanes); improve-skills/agent/team refactored to the evidence-first paradigm (evidence-step.md shared convention, Unobserved red line, candidate freeze); longitudinal verification via intervention ledger + compare (Outcome-supported gating); multi-CLI platform adapters as an owned evolution surface. | Implemented | .specify/memory/features/038.md | .specify/specs/034-evidence-infra/requirements.md | 2026-07-29 (implemented: 41/41 tasks; engine subset ported from b2e621d (~1.1MB, zero npm deps, findings-recommend excised, asset-eval added) + UPSTREAM.md/LICENSE + full mirror; evidence-utils.py 5 actions ×2 mirrors; 5 lanes live (no-Node degradation verified); collect-evidence skill + evidence-step.md; improve-* evidence-first refactor; intervention ledger + compare verdict (honest Unobserved in round-2 dogfood); platform survey delivered; tests: 182/182 node + 62 contract + 13 integration new, full suite 84F/953P = zero new failures vs baseline; SC-001..008 all pass) |
| 039 | Harvested Harness Improvements | Twelve improvements (P1–P12) harvested from 10 reference open-source projects via the cws-workspace-cluster insight-harvest team and adopted in 3 phases: P1 sync-mirrors.py single-source mirror fan-out (replaces manual dual-write); P2 mechanical write gate (.specify/gate.yaml + gate-check.py, deny>confirm>allow, wired into implement + team ACT); P3 pressure-test TDD for skills (RED-GREEN, pressure-testing.md reference); P4 verification-before-completion gate function (IDENTIFY→RUN→READ→VERIFY→CLAIM) + tasks Completion Gate section; P5 finding-validation subagent in analyze/review (confirm/reject/downgrade + DO-NOT-FLAG list); P6 task DAG ([blockedBy] tag + [>] claim sigil, topological execution); P7 optional worktree isolation for parallel teams; P8 AUTO-GENERATED markers on per-tool command copies; P9 description no-workflow-summary rule; P10 Loop Card (WHEN→SEE→DO→CHECK→STOP→LEAVE) in skill/team templates; P11 cannot-self-mark-green promotion gate (run-log machine aggregation); P12 implement long-run mode (state file + max_iterations + 2-strike stagnation exit). | Implemented | .specify/memory/features/039.md | - | 2026-07-30 (implemented in-session from insight-harvest cycle 1 report; sources: OpenSpec/superpowers/claw-code-agent/intellegix/claude-code-ts/claude-code-py/learn-claude-code/loop-engineering/ai-website-cloner-template/better-harness; full suite 38F/1040P = zero new failures vs baseline; sync-mirrors --check clean; gate-check verdicts unit-verified) |
| 040 | Token Efficiency Discipline | Framework-wide LLM token-efficiency discipline in three parts: program-first (fixed-rule text/data judgments — pattern matching, structural validation, counting, dedup, diff — go to deterministic scripts, not the LLM), summary-first (machine-managed data files never injected whole into LLM context; routine access consumes digests/projections/excerpts via an escalation ladder with small-file and edit-target exceptions), and consumption observation (feedback-step carries a token-efficiency self-assessment with stable `token-efficiency` marker; qualitative/proxy metrics only, no fabricated counts; evidence feedback lane surfaces recurrence for improve-*). Single-source discipline doc + authoring-template gates + engine summary output modes + one-time audit with top-5 remediation. | Implemented | .specify/memory/features/040.md | .specify/specs/035-token-efficiency/requirements.md | 2026-08-02 (implemented: 30/30 tasks; discipline doc shared/guidelines/token-efficiency.md + instructions-template ambient section (live instructions.md updated); 5 authoring gates; feedback-step Reflect token self-assessment + feedback-utils list --contains/--limit 0; audit froze 9 violations, top-5 remediated with measured drops (V-001 -88.7%, V-002 -57.3% fixed, V-003 -36.0% preload, V-004 -56.7%, V-005 -82.8%), V-006..009 backlogged; 60 contract tests green; full suite 37F/1049P = zero new failures vs baseline; SC-001/002/003/005 pass, SC-004 partial (live 3-flow spot-check post-merge)) |
| 041 | Goal Registry | Project-level goal as a first-class concept: today goal is only a team field, duplicated per team (frontmatter `goal:` + `## Goal` body — 4 teams write 8 copies) with no definition file behind requirement 036's `goal_slug` aggregation key. Introduces an authored goal **definition** archive at `.specify/goal/<goal-slug>/goal.md` composed of exactly three parts — narrative, zero-or-more verifiable success criteria, lifecycle state (`active`/`achieved`/`abandoned`) — with identity carried by the directory name; teams **reference** it via `goal_slug` instead of copying, and consumers (036's summary mechanism) take project narrative and milestones from the definition rather than from any one team's wording. The goal's object is unrestricted (framework itself, codebase-wide convention convergence, runtime outcomes), it sits on a different plane from Requirement with no hierarchy or structural link between them, and its criteria are measured by degree rather than per-clause pass/fail. Consolidates 036's goal-indexed delivery directory from `.specify/project/goal/` into `.specify/goal/<goal-slug>/summary/`, leaving one goal index in the project with a single-subtree write-set whitelist protecting the definition. `/speckit.goal` is the sole authoring entry (create/modify/view/migrate); legacy teams keep working with zero edits. Adds **multi-team coordination** for N teams : 1 goal — a derived roster of participating teams under `summary/`, a new team-level `territory` declaration (Write/Read Scope + Forbidden, all four collaboration patterns, path-shaped plus typed non-path entries) that lifts the existing member-level zero-write-overlap invariant to team level, overlap detection riding 036's existing summary refresh, and a coordination round that only detects and proposes a re-division for human ratification, writing the ratified division back into each `team.md`. Concept authority is `shared/definitions/goal-definitions.md` (read-only for this Feature, which owns only the layout inside `<goal-slug>/`). Host-CLI `/goal` integration is explicitly out of scope (official docs verified: `/goal set` accepts inline text only). | Implemented | .specify/memory/features/041.md | .specify/specs/037-goal-registry/requirements.md | 2026-08-05 (planned via /speckit.plan: plan.md + research.md + data-model.md + 4 contracts + quickstart.md + feature-ref.md; Constitution v1.9.0 check 12 Pass / 1 Partial (IX, justified: new command surface + the first executable territory validator, both deterministic file/text processing with the coordination round staying proposal-only). Planning exploration corrected two spec defects from measured state: the migration surface is 24 **live** source files (4 canonical + 4 mirrors + 5 generated per-tool copies + 9 tests + 1 user doc + 1 Tool record) with 18 historical files that MUST NOT be rewritten, so SC-011's whole-repo zero was unachievable by construction and was rescoped by face; and `.specify/project/goal/` was never materialized, making this a pure code/doc/test migration with no data move. Test baseline frozen at 40 failed / 1308 passed / 1 skipped in baseline-failed.txt. Earlier the same day: registered via /speckit.clarify when requirement 037 resolved its Related Feature binding — new Feature rather than 027 Team Management, because goal is project-level and has consumers outside the team domain; 027 cross-references it. A second clarify pass conformed the spec to goal-definitions.md: 17 concept points checked — 8 already aligned, 3 conflicts fixed (4-state→3-state lifecycle, 5-item→3-part composition, "different layers"→"different planes, no hierarchy"), 6 gaps closed as FR-027…FR-032. A third pass added multi-team coordination (US5 + FR-033…FR-042 + SC-015…SC-018), grounded in the existing Territory Division discipline which today covers only member level in the parallel pattern; spec now 42 FR / 18 SC / 5 user stories / 5 shared strings / 0 markers) — implemented via /speckit.implement: 72/72 tasks, 17 SC pass / 1 partial (SC-016) / 0 fail, full suite 1480 passed zero new failures, sync-mirrors --check exit 0; 2026-08-11: requirement 038-goal-target **planned** (Target decomposition extension — plan.md + data-model.md + 3 contracts + quickstart.md + feature-ref.md; Constitution 13/13 Pass); 2026-08-12: **implemented** via /speckit.implement — 32/32 tasks, 6/6 SC pass, zero new failures vs baseline (name-level diff; the single suite diff item attributed to in-suite __pycache__ residue, green after cleanup), sync-mirrors --check exit 0, summarize-project untouched per D7 |
| 042 | Interview Mode | Promote interview-style elicitation from a single agent's workflow into a framework-wide **design pattern**: `shared/patterns/interview-pattern.md` defines the generic mode — design tree (every decision branches into the decisions hanging off it), frontier (the questions askable *now*, asked a whole round at a time with numbered questions carrying recommended answers), the fact-vs-decision split (facts are the agent's job via non-blocking subagent probes; decisions are the user's), write-through recording (overwrite-style, latest round wins, before the next round is asked), a durable ledger placed nearest the artifact (`.specify/specs/<feature>/interview-log.md` in a feature context, else `.specify/memory/session/`), and an exit gate that requires the **user's** explicit confirmation that the result is stable rather than the agent running out of questions. Includes an explicit boundary against the four modes it is confused with (reconcile / iteration loop / continuous loop / one-shot confirmation gate) keyed on information source + termination semantics, an embedding contract for the three host types (command / skill / agent-team member), an adoption map naming instances, escalations, and explicit bad fits, and eight anti-patterns. Ships `/speckit.interview` as the standalone unbounded entry (the escalation target for capped flows), demotes the pre-existing `shared/workflow/interview-walkthrough.md` to the requirements-domain instance of the pattern, and wires five integration points: `/speckit.clarify` (bounded instance + escalation path), `/speckit.plan` (interview instead of fabricating an underdetermined design), `/speckit.goal` create (question protocol), `create-team` (goal elicitation), `requirements-analyst` (pattern as mechanics authority). Prompt/doc-only — zero new runtime engine. | Implemented | .specify/memory/features/042.md | - | 2026-08-06 |
| 043 | Session Export | AI agent CLI 会话的导出侧命名与资产化:`/speckit.session export --name <名称>` 把当前(或指定)会话连同原始记录导出为**用户命名的目录**(宿主 CLI 无官方会话命名机制,不改名、只导出后命名),并附带**会话描述文档**(元信息由脚本确定性提取 + 结构化总结由 agent 忠实生成、超预算降级)。执行面为彻底改造后的 `export-session` 技能——支持面收敛为恰好六家 AI agent CLI(Claude Code / Codex CLI / Qoder CLI / GitHub Copilot / opencode / Hermes Agent),其余适配器移除,通用化(去平台专属依赖);产物形态由 zip 改目录;定位机制(--verify/--session/--tool)与五值退出码保留;Copilot/Hermes 落盘形态实现期探测,无源诚实声明。承接前序会话命名 todo 的降级裁定;团队 run 派发会话可经 label 命名导出闭合追溯链。 | Implemented | .specify/memory/features/043.md | .specify/specs/039-session-export/requirements.md | 2026-08-12 (registered as Draft during /speckit.clarify for requirement 039: new Feature rather than binding 022 AI Tools Support / 026 Agent Skill Enablement — session export is a first-class capability consuming them, not a component of either) |
| 044 | Agent Metadata Portability | Separate tool-agnostic metadata from the agent's own body in preset agent definitions, express the metadata in neutral vocabulary (removing the Qoder dialect embedded today), and render it at `specify init --ai <tool>` time into each supported tool's real agent format (real files, replacing the per-file symlink model); also canonically defines the difference between `agents/`, `skills/create-agent/templates/` and `skills/create-team/templates/agents/`. Hosts (not depends on) Feature 033's body-side Project Context parameterization as a complementary layer. | Implemented | .specify/memory/features/044.md | .specify/specs/040-agent-metadata-portability/requirements.md | 2026-08-14 (implemented via /speckit.implement: 42/43 tasks closed + T036 deferred pending user glossary confirmation; SC-001..008 all pass incl. e2e init render; full suite 37F = baseline 39 minus 2 dialect-era fixes, zero new vs name-level baseline; sync-mirrors --check exit 0. Earlier: planned via /speckit.plan: plan.md + data-model.md + 4 contracts + quickstart.md + feature-ref.md; Constitution v1.9.1 check 12 Pass / 1 Partial (IX, justified: render manifest + backup are FR-021's necessary mechanism; codex TOML rendering explicitly refused scope). Registered via /speckit.clarify for requirement 040: new Feature rather than binding 033; second clarify round revised scope to directory-level Worker/Meta taxonomy — 7 role definitions relocate to Worker capability templates, agents/ holds framework-maintenance Meta Agents) |
| 045 | Framework Source Provenance | init 落章:`specify init` 将框架源码 git commit id(唯一标识,不用正式版本号)写入目标项目(默认 `.specify/source.json`),使目标项目可凭其在框架仓 `git show <commit>` 反向定位产出脚手架的精确代码切片。三态语义(有效 commit / `unavailable` 哨兵+原因 / 缺失=来源未知)绝不臆造;重复 init 刷新为零残留;checkout 直测 + 分发构建期嵌入双形态可得。与 024(workspace schema 版本轴)正交,`.specify/version` 路径保留给 024。 | Implemented | .specify/memory/features/045.md | .specify/specs/043-init-commit-stamp/requirements.md | 2026-08-17 (implemented via /speckit.implement — 19/19 tasks, SC-001..005 pass, zero new failures vs 47F baseline (46F final, 1 baseline item fixed: registry count pin synced with 045 registration); real wheel build e2e: package carries _source_commit.json commit==HEAD. Earlier planned via /speckit.plan: plan.md + data-model.md + 3 contracts + quickstart.md + feature-ref.md; design = 唯一探测函数 _probe_head_commit + hatch_build.py 构建期嵌入 _source_commit.json + init 末尾 write_source_stamp 覆写落章;Constitution 13 项 12 Pass / 1 Partial(IX,构建钩子为 FR-004 必要面);零镜像面。 Earlier: registered via /speckit.clarify — new Feature, provenance/source axis orthogonal to 024's schema-version axis; spec: 3 US / 8 FR / 5 SC / 3 shared strings) |
| 046 | Confirmation Gate Governance | 框架级确认门控治理:确立两级分类判据(破坏性/不可撤销动作保留前置确认、可逆动作自动执行 + 事后执行报告),对全框架既有阻塞确认门控逐一归类治理(基线 93 → 残留 22,↓76.3%),并约束未来新增命令/技能不回流非破坏性门控;team 创建/运行/收尾全流程零确认为首要落地场景。交叉引用 027/028/031/041。 | Implemented | .specify/memory/features/046.md | .specify/specs/044-reduce-confirmation-flows/requirements.md | 2026-08-18 (planned via /speckit.plan — plan.md + data-model.md(4 实体)+ 4 contracts(confirmation-taxonomy / team-flow / gate-scanner / execution-report)+ quickstart.md(3 走查)+ feature-ref.md + baseline-gates.md;Constitution 13/13 Pass,无 Complexity Tracking;设计 = 判据真源 shared/guidelines/confirmation-gates.md + 新扫描脚本 scan-confirmation-gates.py(Program-First 度量/防回流)+ team/全框架可逆门控源侧改写经 regen-command-copies + sync-mirrors 传播;治理三分类(destructive/reversible/governance_kept+intrinsic),continuous 循环与保护清单不动。Earlier: registered via /speckit.clarify — new Feature rather than binding 027 Team Management: 治理面横跨 team/goal/tools/skills/agents 多命令域与共享工作流约定,属框架级横向交互策略;spec: 3 US / 11 FR / 5 SC); 2026-08-19 implemented via /speckit.implement — 34/34 tasks, SC-001..005 all pass, GATE-1..5 green, zero new failures vs 46F baseline (3 stale pins fixed along the way); deliverables = confirmation-gates.md 判据真源 + scan-confirmation-gates.py + 4 契约族结构测试 + team/全框架门控治理(instructions Documentation Map 接线); 2026-08-19 Phase 7 追加 — 20 个非 intrinsic 保留门控挂必要性 feedback probe(command-gate/skill-gate 两类 + 20 Objects + 21 处单行指针 + confirmation-gates.md §门控观察协议 + 引擎 (unit, lifecycle_point) 解析 + confirm-gate 标记检索),SC-006..008 pass,扫描器 total 保持 22,契约失败集零新增,dry-run 证据留痕 phase7-evidence/ |
| 047 | Framework Material Hygiene | 框架自有资料卫生:`/speckit.sanitize` 对 parked/draft/索引与存储、specs(含归档)、history、镜像目录、兼容性符号链接、docs 树做过期残留与冗余检测(语义声明 vs 仓库证据,程序采集证据)及正确性检查(死引用/索引一致/链接有效性/镜像漂移,纯确定性判定);发现报告持久化携带 pending 状态,提示是否修复;确认后按两级判据清理(删除/归档前置确认、可逆修复自动执行),处置完成更新状态并出具执行报告。不触及用户代码/脚本/测试。交叉引用 037/008/046/040。 | Implemented | .specify/memory/features/047.md | .specify/specs/045-sanitize-command/requirements.md | 2026-08-20 implemented via /speckit.implement — 32/32 任务关闭,SC-001..005 全 pass(见 verification.md);GATE-1..6 全过(全套件 44F=基线零新增、+109 通过;镜像 diff 一致 + sync-check exit 0;门控扫描 total 23/ violations 空;分类 18;regen 零 stale);dogfood 本仓真实运行:台账 691 pending(689 死引用集中于存活 specs 引用 docs 重构前旧路径、1 skills 孤儿镜像、1 过期残留=动机案例 0801 todo 携带 1a090c72 证据),清理留待用户确认;dogfood 驱动 5 项机制修订(冻结历史豁免/feedback 簿记豁免/孤儿检测收窄 skills 对/证据包无日期过滤+片段 glob/sync-mirrors lane 局部性)+ 2 守卫测试豁免 sanitize 台账;事故记录:Phase 3 提交曾意外包含 0801 todo 删除(未经确认),已恢复并如实入档。Earlier: planned via /speckit.plan — plan.md + data-model.md(5 实体)+ 4 contracts(sanitize-engine / sanitize-findings / sanitize-command-template / sanitize-detection-rules)+ quickstart.md(3 走查)+ feature-ref.md;Constitution 13 项 12 Pass / 1 Partial(IX,已豁免论证:新命令面+引擎+台账均为 FR 必需,复用面 docs-utils/sync-mirrors/.specify/archive/ 全复用);设计 = stdlib-only 引擎 sanitize-utils.py(collect/record/status/apply)+ 累积台账 .specify/memory/sanitize/findings.json(稳定 ID 合并、pending→resolved 状态机、未检出自动收敛)+ 破坏性清理前置确认挂 gate-sanitize-destructive-cleanup probe;实现批次义务:复杂命令分类 17→18、baseline.json total +1、Tool 记录、docs/reference/commands/sanitize.md。Earlier: registered via /speckit.clarify for requirement 045: new Feature rather than binding 037 Docs Command / 038 Evidence Infra / 025 Todo Command — 资料卫生为横向能力,037 仅 docs/ 域、038 是证据采集层(过期证据 todo 只是触发实例)、025 管理 TODO 块;直接动因 = 2026-08-12 结转 parked todo 声称五项未落地而 1a090c72 已于 07-30 全部合入,人工 08-20 才发现;clarify 同轮决策:报告持久化 + pending 状态生命周期(修复后更新状态)、仅手动触发零挂点) |
| 048 | Browser Utils Skill | 浏览器自动化与 Web 应用测试能力本体:能力三分层(Tier 1 内置浏览器 / Tier 2 Playwright 脚本 / Tier 3 MCP 连接器,纯能力导向、不绑定具体 agent 产品)、脚本自动化双方向(页面级模拟 / 请求级调用)、按 domain 键控的站点记忆(site/<host>/)与四态生命周期(exploration→optimization→validation→sealed,失败回退 optimization);任一状态都完整完成用户任务。交叉引用 013(技能管理编排,边界:不含能力本体)。 | Implemented | .specify/memory/features/048.md | .specify/specs/046-browser-site-memory/requirements.md | 2026-08-24 (implemented via /speckit.implement — 23/23 任务 [X];引擎 scripts/site-memory.py 7 actions + SKILL.md 路由段 + references/site-memory.md/request-level-patterns.md;契约测试 44 用例全绿(test_browser_site_memory 35 + test_browser_site_exclusions 9);quickstart 14/14 CLI 示例真实执行通过;T022 范围化验收(用户批准):新增失败 14 全部归因并行 root 会话 create-pages 进行态,自致回归 2 已修复); 2026-08-24 (planned via /speckit.plan — plan.md + research.md(R1 请求级 PoC / R2 实证 hatchling exclude 对 force-include 不生效 / R3-R5 惯例复用)+ data-model.md(5 实体)+ 3 contracts(site-memory-engine 7 actions / site-memory-formats 脱敏写入强制 / framework-exclusions gitignore+sync-mirrors site 分量+hatch_build 舞台拷贝)+ quickstart.md(3 走查+异常)+ feature-ref.md;Constitution 13/13 Pass,无 Complexity Tracking); 2026-08-23 (registered via /speckit.clarify for requirement 046: new Feature rather than binding 013 Skills Command — 013 范围为技能生命周期管理编排,browser-utils 能力本体(三分层路由/双方向/站点记忆)无既有归属;基线含 2026-08-22 已落地的去 agent 化三分层与请求级方向 PoC 验证) |
| 049 | Derivation Command | 推导式架构生成:`/speckit.derive` 以「重放权威源的思维方式」而非「摘要其内容」为方法。强制联网核实每个来源(死链走存档回退、社区改标题由脚本计算的 title_mismatch 检出并双标题留痕),按四级溯源等级落地(仅 primary / authoritative-secondary 可锚定推导步骤,community / unverified 只作线索),把来源的论证方式抽取为带命名槽位、可跨领域复用的思维算子(M-nnn)并累积进项目级算子库(.specify/derive/moves.md — 引擎独写、按归一化 inference_form 去重拒绝、只投影不复制),在 C1–C7 完整性规则下构建可复核推导链(premises→move→conclusion→falsification;禁 best practice 类空洞论证;矛盾标 contested 且不确定性按最小值传播),最终组装出每个元素都可溯源到 D-k 的完整架构(.specify/derive/<topic-slug>/derive.md);前提耗尽处记为 open question,绝不静默填空;无线上能力时诚实降级(全源 unverified、零锚定、建链前停止)。确定性部分交 stdlib 引擎 derive-utils.py(init/validate/moves-list/moves-add/probe-links/stats),语义判断(读懂论证并抽算子、判溯源等级、命名判别性问题、A14)留在 agent。交叉引用 040(Program-First)、041(项目级一等概念归档先例)、046(可逆动作自动执行 + 执行报告)、047(命令自带引擎先例)。 | Implemented | .specify/memory/features/049.md | .specify/specs/048-derive-command/requirements.md | 2026-09-05 (registered — 判定为新 Feature 而非绑定既有:推导式架构生成是新能力面,013 为技能生命周期管理编排、037 仅 docs 域、031/040 为效率与治理纪律,均无既有归属。三项设计决策由用户在本轮明确:走完整 SDD 特性流程、产物落项目级推导档案 .specify/derive/<topic-slug>/ 而非特性目录、思维算子跨运行累积为项目级算子库。基线冻结:全套件 50F(名字级,baseline-failed.txt),门控扫描 total 23 / 上限 23.25 零余量,probe 72 internal + reconcile 既有 2 条错误); 2026-09-05 implemented — 48/48 任务关闭或附因延后(T019–T021 次序倒置、T032 非契约要求),SC-001..SC-006 全 pass(见 verification.md),GATE-1..7 全过:全套件 47F/2466P 且名字级 `comm -13` 为空(较 50 条基线净减 3,系 sync-mirrors 顺带收敛既有陈旧镜像)、新增 232 项 derive 测试全绿 0 xfail、门控扫描 total 保持 23 violations 空、probe internal 72→73 且 `--validate` exit 0 / `--reconcile` 错误集仍恰为既有 2 条、复杂命令分类 18→19、docs-step 15→16、`sync-mirrors --check` 与 `regen --check` 均 exit 0/OK。交付物 = 概念锚 derivation-definitions.md + 命令模板 derive.md + stdlib 引擎 derive-utils.py(六 action,单一传输缝 `_http_get`)+ 新顶层根 `.specify/derive/`(moves.md 算子库 + <topic-slug>/derive.md 档案,git 跟踪、非镜像对)+ 8 个测试文件 + Tool 记录 + 用户文档 + framework-map/glossary 接线。dogfood 真实运行:对用户 handed-in 的六条 REST 文献联网核实,仅 1 条落地、2 条 404 无快照(其中 w3.org TAG finding 是清单**未披露**的死链)、3 条 403,产物 `validate` exit 0 且只推导出 1 个架构元素+ 1 个未决问题(终止条件 b),拒绝编造前提。实现期最重要的一次纠偏 = **契约赢了实现**:引擎先按口头规格落地,独立撰写的契约暴露出接口实质分歧后重写引擎去符合契约(而非弱化契约),随后的端到端驱动与测试又暴露 8 处引擎/契约缺陷(含 1 处不可达 error code、1 处 C-25/C-38 联立不可满足、1 处 superseded 算子可被复活),全部闭合) |
| 050 | Proactive Flow Trigger | 主动触发机制:在每个受支持 agent 的必经指令路径上埋设触发点(init / `/speckit.instructions` 写入 `.specify/instructions.md`,经既有 symlink 抵达 AGENTS.md / CLAUDE.md / QODER.md 等),agent 每回合接续既有 constitution 合规分析做情境评估(默认只消费已在上下文中的环境信息、不足时才升级到确定性探测的摘要输出),主动建议"现在该执行哪个流程"并附**确切可复制的调用形式**,使用户不必记得任何 `/speckit.*` 命令名或技能名。建议的采纳/拒绝记录进项目本地学习状态(规则集 / 晋升态 / 建议事件 / 每回合遥测),同一规则连续采纳达阈值(默认 3,一次拒绝即重置)后晋升为免确认自动执行——**破坏性/不可逆流程永不晋升**(判据归确认门控治理,学习计数不得覆盖)、**建议永不阻塞当前请求**(回流约束);自动执行仍出三段式执行报告。出厂种子规则集从 25 个命令模板既有 `## Handoffs` 条件式**派生**(不另撰第二份 situation→flow 知识),故新项目第一天即有建议;情境身份取粗粒度受控词表(生命周期阶段 × 待处理信号集)使连续计数良定义;规则本身为数据,依命中率/拒绝率/误报/漏报经用户确认后调优。交叉引用 032(同为指令段投递的行为指令,归属判定的决定性先例)、008(投递载体)、001(Handoffs 为种子派生来源)、022(全 agent 覆盖议题)、028(计数-阈值范式)、046(破坏性判据与回流约束)。 | Implemented | .specify/memory/features/050.md | .specify/specs/050-proactive-flow-trigger/requirements.md | 2026-09-07 (registered — 判定为**新 Feature** 而非绑定既有:按绑定先例启发式逐个核验 6 个候选的同胞吸收证据(022 吸收 4、028 吸收 3,均强吸收但主题不符;032/008/001/046 各 1),无一拥有本能力,各只匹配一个侧面(032/008=投递载体、001=能力前身但静态命令域、022=仅 FR-003 议题、028=计数机制先例、046=破坏性判据消费);决定性先例是 **032**——投递载体完全相同却仍自成 Feature 而非绑 008,确立「以指令段形式嵌入是**投递事实、非归属事实**」。两轮澄清完成:首轮三项(指令文件为**唯一**触发通道 / 环境信息为主 + 按需升级探测 / **每回合**评估)、第二轮五项(Feature 绑定 / 种子规则从 Handoffs 派生 / 阈值语义取**连续采纳**拒绝即重置 / 情境身份取**粗粒度受控词表** / 扩展 FR-009 增每回合遥测以补齐 SC-011·SC-012 的分母缺口)。另采纳用户指示把评估**挂在既有 constitution 每回合合规分析之后**,并按源码实测收敛为两级:只挂 Documentation Map 的轻量常驻义务,**禁止**把 plan 期逐原则枚举门控(`plan-template.md:31–33`)每回合化。残留 `NEEDS CLARIFICATION` 0;26 FR / 15 SC / 4 user stories / 7 key entities / 32 acceptance scenarios / 17 edge cases。**待 plan 阶段优先复核的机械约束**:确认门控预算余量为 **0**(扫描 total 23,上限 = 044 `baseline.json` total × 25%),而 `scan-confirmation-gates.py` 的 `SCAN_ROOT_FILES=("templates",)` 覆盖 `templates/*.md`,本特性文案天然是"建议/确认/询问"语义,极易命中 `BLOCKING_PATTERNS` 而同时打爆 sweep 契约。索引行与表头计数一律**手工编辑**——`update-feature-index.sh` 为破坏性脚本(承 Feature 049 记录:整体重写、缺 Spec Path 列、覆盖手写散文、重置全部 Status,而 test_c3 仍通过); 2026-09-07 **planned** via `/speckit.plan`(Draft → **Planned**)——plan.md + data-model.md(7 实体 / 2 状态机 / 受控词表 **13** 命名情境 + 9 阶段 + **12** 信号 / 升级判据 P1–P5 / 校验规则 **6 组 32 条** V1…V6)+ contracts ×4 共 **61 条款**(trigger-section 12 / trigger-engine **23** / seed-derivation 10 / discipline-doc **16**;原 47,analyze 补 C-19…C-23 与 C-8…C-16)+ quickstart.md(6 场景)+ feature-ref.md,无独立 research.md(Phase 0 findings 以 D-1…D-10 内联,全部仓库内部实测);**Constitution v1.11.0 13 Pass / 1 Partial(XIV,已论证)**,Complexity Tracking 已填(原记 14/14 Pass,2026-09-08 经 analyze 订正)。设计取向 = 主动**收缩制品面**:零新命令、零新技能、零新确认门控、零新依赖、零 `src/` 逻辑改动,唯一新制品是一个 stdlib 引擎(`trigger-utils.py`,11 action 封闭枚举,复制 `derive-utils.py` 的 camelCase 信封 + 原子写 + 分级退出码),必要性由 Program-First 强制。四项决定性裁定:① **管理面不引入命令**(取 glossary 域同构形态:引擎 + 触发段路由管理意图)——三个候选里唯一对已钉死计数(复杂命令 19、docs-step 16)、probe 注册表(internal 73)与门控预算全部**零扰动**,且与"用户不必记名字"的特性论点自洽;② **种子规则取「provenance 副本 + 漂移检出」**——`## Handoffs` 是散文不可确定性解析,漂移由**被检出**取代被防止。**归类经 analyze 订正**:该形态**不属** `one-source-of-truth.md` 的三类合法副本(guard copy 须 pinned inside a test 且 serves the build never the reader,而种子是随包分发的运行时制品),故 Principle XIV 记 **Partial** 并在 plan.md §Complexity Tracking 论证;已否决给 25 模板加结构化 frontmatter 键(需再生 100 份 per-tool 副本,且与既有 `handoffs:` 键同名不同义);③ **每回合评估 = 每回合一次紧凑引擎调用**——agent 只传粗信号、引擎做匹配/计数/阈值,遥测因此**机制自产**,闭合 clarify 轮发现的 SC-011/SC-012 分母缺口;④ **状态按变动频率切分跟踪边界**——`.specify/memory/trigger/index.json` 跟踪(用户调优跨 clone 存活)、`telemetry.jsonl` 忽略(每回合 churn,承 `implement-loop.local.md` 先例)。计划期实测**推翻两处既有记录**(Principle VIII 的直接应用):指令模板实为 **14 个 `## ` 章节**而非 spec 原写的 17(14 才是关键数字,因 additive reconcile 只按 `## ` 切分,`###` 与既有章节内部改动**不传播**),已就地订正 spec 锚点;`sync-mirrors.py --check` 当前 **exit 0 全绿**,Feature 049 记录的 git-fleet 6 MISS 基线**已失效**,故全树 exit-0 门禁可用、049 的 `--only` workaround 不再需要。门控预算处置 = **设计规避而非改写 baseline**:两份新文案零 `BLOCKING_PATTERNS` 命中,钉为 `trigger-section.md` C-6 与 `discipline-doc.md` C-3,并在后者给出五处最易误犯表述的合规替代写法(`shared/guidelines/proactive-trigger.md` 不匹配任何 `GOVERNANCE_PATH_PATTERNS`,不享豁免)。实现期义务:新增 Tool 记录 `trigger-utils.py.md`(Principle XII,经 `refresh-tools.sh` 进 `tools.md`)、`generate-instructions.sh` 补 `HERMES.md` 与 `.opencode/instructions.md` 两条 symlink(FR-003 硬前置,消除 `_check_instructions` 两处恒 fail)、开工前重新冻结 `baseline-failed.txt`(当前最近为 049 的 47 条); 2026-09-07 **tasks** via `/speckit.tasks`(状态保持 **Planned**,不推进)——**51 项任务 / 7 Phase**(Setup 3 · Foundational 4 · US1 8 · US2 13 · US3 8 · US4 7 · Polish 8),13 项 `[P]`、**40** 处任务行 `[blockedBy]`,格式经程序校验(ID 连续唯一、单行含路径、故事标签放置正确、DoD 与 Completion Gate 零 checkbox 语法、blockedBy 无悬空引用)。**Tests Mode ON**(Principle IV)+ **混合型判定**:Principle VII 的 template-only 门(constitution.md:95)只豁免文档/模板制品面,`trigger-utils.py` 是可执行运行时代码故单元与集成测试**强制**;测试面由 plan 的 3 契约文件扩为 **8 个**(4 契约一契约一文件以保 `[P]` 资格 + 1 单元 + 3 集成按故事分文件),plan.md 四处已同步订正。**关键路径** = 单文件引擎串行链 T021→T023→T031→T038(US2/US3/US4 共改 `trigger-utils.py`,房式单文件约定),为本特性并行度硬上限;测试任务跨故事可并行。**MVP = US1 + US2**(两个 P1 合起来才构成独立有价值增量)。**Feature 列表复审**:未暴露新 Feature、未使既有失效;发现一处跨 Feature 交付效应——T011 补齐 `HERMES.md` / `.opencode/instructions.md` 闭合的是 **Feature 022** 范围内的既有覆盖缺口(`_check_instructions` 对两 key 恒 fail),归属仍在 022、050 为交付方,已记入 `features/022.md`。Completion Gate 8 项检查命令已逐一存在性核验(13/13 存在); 2026-09-08 **analyzed + remediated** via `/speckit.analyze`(状态保持 **Planned**)——只读分析产出 **37 条发现**(1 CRITICAL / 7 HIGH / 19 MEDIUM / 10 LOW);因全部工件同会话撰写,**检测与验证都委派 fresh-context 子代理**(3 检测通道 + 4 验证代理,验证者只收 finding 与证据位置),10 条 CRITICAL/HIGH 中 7 confirm、1 由 CRITICAL 降 HIGH、2 由 HIGH 降 MEDIUM、0 reject。**CRITICAL** = plan.md 把 Principle XIV 记为 Pass 的归类不成立(种子 JSON 非 `one-source-of-truth.md` 三类合法副本中任何一类),已改记 **Partial** 并补 Complexity Tracking 完整论证,不以重新解释指南清账。**HIGH×7** = ① 14 条 seed provenance anchor 中 **9 条不成立**(交叉扫描全部 25 个 Handoffs 段证实)→ FR-022 派生来源扩为两类(`anchorKind ∈ {handoffs, owning-section}`;`/speckit.feedback` 等在**任何** Handoffs 段都不出现,其 owner 是 `shared/workflow/feedback-step.md` §Threshold prompt protocol)、7 条 anchor 改指、命名情境 14→**13**(删 s14:无状态可触发且与 quickstart 场景 3 矛盾)、信号枚举 9→**12**(新增 checklist-absent / feature-index-absent / constitution-absent,解决 s03/s13 同身份违反 V1.3 与 s05 越界)、新增 V1.5/V1.6(匹配谓词 + 最具体优先消歧);② FR-019 / FR-020 **零机械覆盖**且映射指向不相干条款 → 新增 `trigger-engine.md` C-19(禁网络能力 import)/ C-20(会话级重复抑制,给 FR-008 机制)/ C-21(引擎从不自行执行流程)/ C-22(错误码封闭集)/ C-23(status payload schema),并把 `discipline-doc.md` 原**无编号**的内容表提升为 **C-8…C-16**(此前十余条 FR 被路由到只断言结构属性的 C-1…C-7);③ `complianceDone` 无输入通道致 SC-012 空洞、C-14 分支为死代码 → 封闭 flag 集 19→**20**(新增 `--compliance-done`,缺省 false)。SC-005/011/013/015 口径订正;**Measurement Sources 全节重写,15 条 SC 逐条具名产出任务**(此前 7 条无产出任务);tasks.md 补 T026 的 T023/T024 blocker 与 T044/T045/T049 blocker、DoD-4 豁免 Mirror 第 6 行、门控字面量改引用 T002 冻结前值;`feature-ref.md` FR→契约映射全面重写。契约条款 47→**61**; 2026-09-08 **implemented** via `/speckit.implement`(状态推进见 Pre-Status-Flip Gate)——**51 任务 → 50 `[X]` + 1 `[~]`**(T050 dogfood 需 ≥20 个普通工作回合,本会话在建造机制故非真实样本),**8 新测试文件 / 313 新测试全绿**,名字级回归每个 phase 边界为空(43 = 42 自有 + 1 外部归因);零扰动承诺机械复核成立(门控 **23/0** == 冻结前值、19/4/16 字面量未改写、probe **73**、`regen-command-copies`/`sync-mirrors` 均 exit 0);引擎 **1480 行 stdlib**,11 action / 20 flag / 10 码与契约封闭集逐项相符;**FR-003 缺口闭合**——8/8 agent 路径 symlink 至唯一 canonical,`_check_instructions` **6/6** pass(hermes/opencode 由恒 fail 转 pass,交付落在 Feature 022 范围);**SC 13 pass / 2 deferred**(SC-008 part2、SC-011,同因 T050);破坏性规则连续 10 次采纳 `promoted`/`autoExecute` 恒 false(附可逆对照)、再生后 `index.json` sha256 逐字节相同、三态降级 exit 0 + `suggest-only`、窗口 50 下 60/60 次追加恒 ≤50 且轮转前后规则集 sha256 相同、漂移守卫经受控实验(一次性 worktree,live 仓零修改)+ **负对照**证明为活体;实现期订正两处工件前提缺陷(quickstart 场景 1 的 `specify init` 一步假设;**T044/DoD-8 的 `tools.md` 子句 VOID**——全仓无写入方、该文件是 MCP 索引,详见 tasks.md §Implementation-Period Corrections C-1);类级发现已报告未修(同一虚假陈述散布 **7 个表面**,含 ambient,应乘独立变更);本特性催生一次独立治理变更 `61813fbf`(两顶帽子可达性 + Ask/Record/Repeat 理念,含与 XIV 的裁定:指针形态可重复、内容形态仍禁止) |
| 051 | 面向用户可理解性纪律(User-Facing Comprehension) | 把框架今天已在多处执行、却从未被定义的一条规矩——面向用户的输出**不用行话、带足上下文**——命名为一条独立纪律:在 `shared/guidelines/user-facing-comprehension.md` 建立唯一真源,以行话侧白/黑名单 + 上下文侧下限/上限与裁决顺序 + 第三方可复现机械判据,把"程度"落成可判定条件而非"尽量少用"一类形容;扩散到 **11 类面向用户界面**(门控确认提示、门控执行报告、反馈阈值/提交通知、访谈提问、澄清提问与选项表、主动流程建议行、面向干系人的需求/规划/任务工件、面向外部读者的项目总结报告、词汇表校正的对外呈现、流程收尾报告、失败如实报告),每类真源以单行指针接入;并经宪章模板 + 宪章命令 `MUST include` 清单**双落点**输出到下游项目(再经 `plan` 门控的动态枚举自动生效,`plan-template.md` 零改动)。动因是实测到的**定义缺失而非能力缺失**:38 处独立措辞分散在至少 4 个互不引用的领域、真源文档 **0** 份、统一名字 **0** 个;`confirmation-gates.md` 全文 99 行只判**是否**门控、从不判**如何措辞**(13 个治理保留门控无一附措辞义务,而其最强的措辞规则作用域仅限反馈提交提示、且连契约断言都没有);全仓最成体系的实现(内部标识黑名单 + 读者向改写映射)搁浅在 `summarize-project` 技能内部,框架其余部分够不到。同批回流 **Principle XIV**(One Source of Truth,此前从未回流到 `templates/`,故无下游项目收到它)并把它用作"宪章原则 MUST 双落点"守卫的首个受测样本。交叉引用 040(最接近的结构先例,其"全局阈值 + 场景显式覆盖、声明处生效不回写"形态被复用为读者基准的裁定形状;观察标记形态亦沿用)、032(归属判定的**决定性先例**:同为指令段投递的行为纪律却自成 Feature,确立"投递事实 ≠ 归属事实")、046(消费其治理保留清单与执行报告三要素,只**新增**措辞义务、明确不改"是否门控"判据)、042(把 `interview-pattern.md` 的可理解性四规则**搬出**到独立真源,该文档收敛为指针 + 其模式特有规则)、028(把 `feedback-step.md` 的对外措辞规则降级为本纪律在"反馈通知"界面类上的实例,传播足迹 194 文件)、031(白名单第 ② 条衔接 canonical 术语与出向就地注解)、005(仅投递载体)、023(守卫属结构契约测试,但约束对象是面向用户消息的**措辞**而非模板结构)。 | Draft | .specify/memory/features/051.md | .specify/specs/051-user-facing-comprehension/requirements.md | 2026-09-17 (registered — 判定为**新 Feature** 而非绑定既有:按 `.specify/shared/workflow/feature-integration.md` § Feature Binding Rules 的同胞吸收启发式逐个核验 **8 个候选**,无一拥有本能力、各只匹配一个侧面(040/032/031 各只有自身 1 个同胞;042/023/005 零同胞;028 同胞吸收最强 3 个但主题是反馈存储与 Probe 建模;046 有 1 个但主题是"是否门控"而本需求明确不改它)。决定性先例是 **032** 与 **040**——投递载体与文件形态完全相同却都各自成为独立 Feature,而非绑到 008 Instructions Command。候选核验表见规格 `Related Feature`;八个候选以**消费关系**记入 `features/051.md`,不构成归属。两轮澄清共 **6 项裁定**:首轮(`/speckit.requirements` 内)① 界面类接入广度 = **全做,含两处搬家**(11 类指针 + 访谈模式规则收敛 + 项目总结黑名单提升,同批完成;隔离搬家并不降低其回归面,只会让 38 → 0 的收敛量长期停在中间态)、② Principle XIV 回流缺口 = **一起修并当守卫样本**;第二轮(`/speckit.clarify` Mode A)③ Feature 归属 = **新建 051**、④ 读者基准 = **全局一套 + 按类覆盖**(基准读者「未在本会话打开过本仓库、但具备该项目领域知识的人」;既有 3 种互不引用的基准表述收敛为 1 条全局 + 至多 2 处类级覆盖,覆盖值声明处生效不回写)、⑤ 上下文上限形态 = **定性上限 + 沿用各界面既有数值约束**,不新增裸数值——已知代价(门控提示与收尾报告两类界面因此无长度界)经用户**知情接受**并以 FR-011 成文,接受条件是长度不设界但**形态**设界(复述工件即违规、不论长短)、⑥ 常驻章节与真源文档**投递路径实测不同步**(章节经 `/speckit.instructions` 增量调谐注入既有指令文件;真源文档只经 `specify init` 附加式 copytree 或 `sync-mirrors.py` 抵达,`generate-instructions.sh` 全文**不同步 `shared/`**)= **记录为既有结构性状况 + 加同批抵达与缺失显式提示义务**(FR-038),不修投递脚本(修它收益覆盖既有 11 份 guideline、属另一条 Feature 的归属)。残留活动 `NEEDS CLARIFICATION` **0**(全文 2 处字面命中均为反引号包裹的**标记名引用**,指 `requirements-guidelines.md:72-88` 定义的那个提示模板本身,即 FR-020 要接入的对象);**38 FR / 18 SC(各配度量来源)/ 5 user stories(3×P1 + 2×P2)/ 9 key entities / 26 acceptance scenarios / 15 edge cases / 6 shared strings**,质量清单 **16/16** 通过。FR 编号 FR-001…FR-038 连续且文档序一致,全部 `FR-NNN`/`SC-NNN`/`[[STR-NNN]]` 引用经程序核验零悬空。**零新机制是硬约束**:MUST NOT 引入行话 lint 引擎、措辞评分器、成熟度报告或跟踪台账;执行手段限于既有契约测试与 `scan-confirmation-gates.py` 的 `POLICY_DOCS` 豁免登记,且门控计数前后 MUST 不变。**索引行与表头计数一律手工编辑**——承 Feature 049/050 记录并经本轮源码复核确认:`scripts/bash/update-feature-index.sh:150` 为 `cat > "$FEATURE_INDEX"` 整体重写,且 `:164` 输出的表头只有 **6 列**(缺活动索引实际使用的 `Spec Path` 列),运行它会摧毁 Spec Path 列与全部手写散文;注意 `AGENTS.md` 仍称该计数由此脚本维护,与实测行为矛盾,已作为发现项报告、未在本轮修改) |

## Feature Entry Format

Each feature entry should follow this format in the table:

| ID | Name | Description | Status | Feature Details | Spec Path | Last Updated |
|----|------|-------------|--------|----------------|-----------|--------------|
| NNN | Feature Name | Brief description of the feature | Draft | .specify/memory/features/NNN.md | .specify/specs/NNN-feature-name/requirements.md | 2025-11-21 |

### Column Definitions

| Column | Description |
|--------|-------------|
| ID | Sequential three-digit feature identifier (001, 002, etc.) |
| Name | Short feature name (2-4 words) describing the feature |
| Description | Brief summary of the feature's purpose and scope |
| Status | Current implementation status (Draft, Planned, Implemented, Ready for Review, Completed) |
| Feature Details | Path to feature detail file in .specify/memory/features/[FEATURE_ID].md |
| Spec Path | Path to the latest requirements specification for the feature |
| Last Updated | When the feature entry was last modified (YYYY-MM-DD format) |
