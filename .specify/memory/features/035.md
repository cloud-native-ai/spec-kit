# Feature Detail: Code Quality Tooling

**Feature ID**: 035  
**Name**: Code Quality Tooling  
**Description**: Python linting, formatting, and type checking configuration for consistent codebase maintenance.  
**Status**: Draft  
**Created**: 2026-07-22  
**Last Updated**: 2026-07-22

## Overview

The project has no linter, formatter, or type checker configuration in `pyproject.toml` or any other config file. For a Python project with a growing codebase (`src/specify_cli/`, `scripts/python/`, `tests/`), automated code quality tooling prevents style drift, catches common bugs early, and reduces review friction. The modern Python ecosystem standard is `ruff` (linting + formatting) and optionally `mypy` or `pyright` for type checking.

## Latest Review

Gap identified during full-registry feature reconcile: no linter/formatter config found in `pyproject.toml`; no `.flake8`, `.pylintrc`, `ruff.toml`, or `mypy.ini` present.

## Key Changes

1. Add `ruff` configuration (linting + formatting) to `pyproject.toml`.
2. Optionally add `mypy` or `pyright` for type checking on `src/specify_cli/`.
3. Integrate with CI/CD pipeline (feature 034) as a quality gate.

## Implementation Notes

- The project targets Python 3.8+, so tooling must support that baseline.
- `ruff` is the preferred tool (fast, replaces flake8/black/isort in a single tool).
- Initial configuration should be lenient (avoid mass reformatting of existing code).

## Future Evolution Suggestions

- Add pre-commit hooks for local enforcement.
- Consider adding `pyupgrade` for Python version modernization.
- Add type stubs for public API surfaces.

## Related Files

- Feature Index: .specify/memory/features.md
- Feature Detail: .specify/memory/features/035.md

## Status Tracking

- **Draft**: Gap identified and feature registered. (current)
- **Planned**: Tool selection and configuration scope approved.
- **Implemented**: Linter/formatter configured and passing on existing codebase.
- **Ready for Review**: CI gate enforced; no new violations introduced.
- **Completed**: Codebase fully compliant with configured rules.
