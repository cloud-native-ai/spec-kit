# Feature Detail: Prompt Template Quality

**Feature ID**: 023  
**Name**: Prompt Template Quality  
**Description**: Structural validation and consistency enforcement across all command and skill templates.  
**Status**: Draft  
**Created**: 2026-06-05  
**Last Updated**: 2026-06-05

## Overview

Spec Kit's primary artifacts are markdown prompt templates that drive AI agent behavior. The quality and structural consistency of these templates directly determines the framework's reliability. This feature covers establishing validation rules, structural patterns, and quality gates that ensure all templates (command, skill, agent) follow consistent conventions and produce predictable AI agent behavior.

## Latest Review

Gap identified during project-intrinsic analysis. Command templates in `templates/commands/` follow informal conventions (User Input section, Actions section, Handoffs) but there is no automated validation or documented structural contract for template authors.

## Key Changes

1. Define a structural schema for command templates (required sections, placeholder conventions, handoff format).
2. Define a structural schema for skill templates (SKILL.md required fields, tools/ layout, references/ conventions).
3. Add contract tests that validate all templates against their schemas on every change.
4. Document the template authoring guide for contributors and skill creators.

## Implementation Notes

- Templates are the product — their quality is equivalent to code quality in a traditional project.
- Validation should be schema-based (checking structure) rather than content-based (checking prose quality).
- The existing `tests/contract/` pattern is the right home for template validation tests.
- Must not block AI agent creativity in template content; only enforce structural consistency.

## Future Evolution Suggestions

- Add a template linting step to the skill creation workflow (`create-skills`).
- Consider versioning template schemas so older templates can be incrementally migrated.
- Expose validation as a sub-function of `/speckit.checklist` for self-service quality checks.

## Related Files

- Feature Index: .specify/memory/features.md
- Feature Detail: .specify/memory/features/023.md

## Status Tracking

- **Draft**: Gap identified; no formal template schema exists.
- **Planned**: Schema defined and template authoring guide drafted.
- **Implemented**: Contract tests validate all templates against schemas.
- **Ready for Review**: All existing templates pass validation; guide reviewed.
- **Completed**: Template quality is continuously enforced via automated checks.
