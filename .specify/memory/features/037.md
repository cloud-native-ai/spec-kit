# Feature Detail: Docs Command

**Feature ID**: 037  
**Name**: Docs Command  
**Description**: `/speckit.docs` command that standardizes and manages the project documentation space (thin root-entry layer README/ARCHITECTURE/CONTRIBUTING/CHANGELOG + thick typed `docs/` layer concepts/tutorials/tasks/reference/decisions/contribute + lifecycle-constrained `notes/`), implemented as a single reconcile engine per `.specify/shared/patterns/reconcile-pattern.md` (scope resolution, R0–R6 loop, tolerance band, archive-not-delete, tiered confirmation, four mandatory artifacts). Includes ADR management, notes exit mechanism (frontmatter + state machine + deterministic lifecycle automation), a dogfooding run on Spec Kit itself (aggressive re-taxonomy of `docs/`), and the standard Feedback self-reflection step.  
**Status**: Implemented  
**Created**: 2026-07-28  
**Last Updated**: 2026-09-05

## Overview

`/speckit.docs` 把两份设计笔记（`docs/notes/docs-design.md` 的"K8s 基底 + ADR 机制 + 根目录索引"结构，`docs/notes/notes-design.md` 的 notes 退场机制）确立为文档空间期望态基线，并按调谐模式实现为单一引擎：观察 → 期望态 → 容忍带 → 干跑计划 + 分级确认 → 收敛（正式区只归档不删除，归档区在 `docs/` 内）→ 校验与残差报告（落盘运行产物在 `.specify/` 命令工作区）。含 notes 生命周期（frontmatter + draft/expired/archived 状态机 + 三条退场路径，删除仅限 notes 区且须人工确认）与 ADR 只追加管理。实现后对 Spec Kit 自身执行激进重组基调的 dogfooding 调谐，并经既有 Feedback 机制持续优化（复用 028/036，零新增循环机器）。

## Key Changes

1. 新增 `/speckit.docs` 命令模板并分发到全部受支持 AI 工具，附命令参考文档。
2. 文档期望态基线：薄层根目录（不超过一屏）+ 六类正式目录 + notes 临时区 + 文档生命周期流转 + **大写特殊名注册表**（README/ARCHITECTURE/CONTRIBUTING/CHANGELOG 固定语义，FR-010）。
3. 单一调谐引擎：作用域判定（全量/单目标/扇出/bootstrap）+ 四件强制产物 + 期望态来源优先级。
4. notes 退场机制：frontmatter 规范、状态机、确定性生命周期自动化（scan/expire/clean/archive-check/stats/validate/audit，docs-utils.py）。
5. **文档同步评估步骤**（FR-011）：仿 feedback-step，`shared/workflow/docs-step.md` 单一事实源，注入 14 个复杂命令收尾（非阻断、增量评估）。
6. Dogfooding：对 Spec Kit `docs/` 激进重组 + 两份设计笔记退场归档。

## Latest Review

2026-09-05 `/speckit.implement`(需求 048-docs-reconcile):实现**目标结构驱动三段式编排**——跨运行 `.specify/docs/target-structure.md` 受管块、容忍带后分型动作、create-docs/improve-docs 双技能机械路由、带参调协叠加与顺序内容扇出。实现期用户修订把命令的核心进一步明确为“**写什么 / 放哪里**”:写作计划必须综合目标读者/任务语境/本次输入/仓库证据/写作边界;落位先检索 canonical owner,存在则更新、否则唯一创建;创建/移动后同步人类索引并通过 `/speckit.instructions` 刷新 Agent Documentation Map。

实现证据:99/99 目标契约通过;全套 47 failed / 2257 passed / 1 skipped,失败 node ID 与实现前基线 47 项完全一致(0 新增/0 消失);命令 4 副本与 3 对目标镜像一致,全局 mirror scan 保持既有 6 MISS / 0 DIFF(无新增);confirmation gate 保持 23(`governance_kept=10`,`destructive=13`)/0 violations;docs validate 仅保留既有 `type-follows-stage-misclassification.md` 非规范状态 residual。三轮 active Qoder Skill 隔离 dogfood:US1 首次声明+稳定重跑+3 问零写+实态脱节只提议;US2 2/2 动作路由正确且 79/79 未涉文档 hash 不变;US3 五类内容输入齐全、唯一文档/零近重复、README+目录索引+instructions Map 三入口可达、块外字节不变、扇出 21→完成2/待续19。7/7 SC pass;Feature 状态保持 Implemented。

2026-09-02 `/speckit.tasks`(需求 048-docs-reconcile):产出依赖有序 `tasks.md` 共 **35 项**(Setup 2 / Foundational 3 / US1 8 / US2 9 / US3 7 / Polish 6),Tests Mode ON(宪法 IV + VII template-only gate),MVP=US1+US2,10 项有效并行机会;实现期用户修订后 19 FR / 7 SC 均有覆盖矩阵。后续 `/speckit.analyze` 发现并修复 8 项跨产物问题:移除目标声明契约中的 owner 枚举副本、补齐 FR-003/FR-015 的测试/实现/表面验证、把 GATE-2/5/8 改为确定性命令、统一契约修订计数为 6、串行化 verification.md 写入、更新阶段快照、指定 active CLI Skill + disposable worktree 验证机制、区分生产制品与测试证据文件。Feature 状态保持 Implemented;tasks 阶段仅更新演进记录,不回退状态。

2026-09-01 `/speckit.plan`(需求 048-docs-reconcile,第四次 follow-up):产出 plan.md + data-model.md(3 实体)+ contracts ×3(docs-command-orchestration D-1…D-12 / target-structure-declaration T-1…T-10 / action-routing A-1…A-9,共 31 条款)+ quickstart.md(6 场景)+ feature-ref.md;无独立 research.md(内部勘察即可解决,结论内联 Phase 0)。Constitution v1.11.0 检查 **14/14 Pass**,Complexity Tracking N/A。

核心设计决策(均以"零新增面"为取向,守原则 IX):(1) 目标结构声明落 `.specify/docs/target-structure.md` —— 工作区**根级单文件**,与按次产物子目录 `plans/`、`audit/` 天然可区分(满足 FR-002a),形态沿用 `.specify/git-workflow.md` 受管块先例(成对 HTML 注释标记 + 只替换标记之间内容),由 LLM 撰写而非引擎管理,骨架新增为 `templates/docs-target-structure-template.md`(制品模板家族,镜像对已存在);(2) 三段编排全部落在既有 `## Outline` **之内**,不新增顶层小节 → 契约 C-3 六章节枚举**无需修订**;(3) `docs-utils.py` **零改动**(不新增 action,避其契约 C-1 变更);(4) **新增确认门控数 0** —— 声明批准(FR-003)、隐含结构变更回写(FR-010)、目标重设计提议(FR-015)、扇出规模告知与中止(FR-008)全部并入既有 R4 干跑计划这**同一个**已治理门控,是单引擎坍缩纪律在门控维度的同构应用。

强制该取向的实测约束:门控扫描 `total: 23`(`governance_kept: 10` / `destructive: 13`,`violations: 0`)而 SC-002 上限 = baseline 93 × 25% = **23.25** —— 新增任一阻塞门控即越界,本规划以设计规避而非改写 `baseline.json`;必须存活的字面量已逐条实测确认(`stop-and-confirm` KEEP_LIST、`reconcile-pattern.md`、`docs-utils.py`、`single source of truth`、四件产物名、五个作用域名),必须保持缺席的 `R0 需求解析` 与已退役的 `.specify/templates/commands/docs.md` 亦已核实。镜像基线:`sync-mirrors.py --check` 当前 **exit 2**,系无关的既有 `git-fleet` 镜像缺失 6 文件(上游 `fe70862e`),本特性触及的四对实测全 `ok`,故门禁定为"所触及镜像对无新增漂移"。已识别的实现批次义务:就地修订 033 契约 C-1/C-4a/C-5/C-7/C-8/C-18、订正 `improve-docs` 把 `.specify/docs/**` 整体称作 "run artifacts" 的措辞、更新 `docs/reference/commands/docs.md`(架构陈述/执行流/产物表第五行/Related)。

Feature 状态**保持 Implemented**,不回退为 Planned:037 已交付能力仍成立,048 为增量演进(同 Feature 041 吸收需求 038-goal-target 时的处理)。

2026-07-28 `/speckit.implement`：37/37 任务完成（零延期，含运行中追加的 T037 保留文件名）。落地：命令模板 + 5 工具副本 + 镜像；docs-utils.py（scan/expire/clean/archive-check/stats/validate/audit，含代码块链接豁免与 reserved-name-misplaced 校验）双镜像；docs-step 注入 14 复杂命令（分类契约 13→14）；宪法 X 修订 v1.7.0 + ADR-0001/0002；dogfooding 激进重组（15 搬迁、README 110→51 行、断链 134→0、符号链接 39/39 完好、regen 零漂移）；SC-001…007 全 pass；回归 84F/877P，全部失败经根因归因为基线既有。状态 Planned → Implemented。

2026-07-28 `/speckit.plan`：plan.md（Constitution Check 11 原则，IV 为 template-only Partial 豁免）+ data-model（7 实体）+ contracts ×3（命令模板结构 C-1…C-11 / docs-utils CLI C-1…C-11 / docs-step 注入 C-1…C-9）+ quickstart（6 场景）+ feature-ref。plan 阶段两条用户修订指示并入 spec：FR-010 大写命名语义、FR-011 文档同步步骤（新 US3，原 US3/4 顺延）。技术要点：新增命令零 CLI 源码改动（generate_commands glob）；测试钉点 test_feedback_command_classification COMPLEX 13→14。状态 Draft → Planned。

## Implementation Notes

- 归档区在 `docs/` 内（读者可见）；运行产物（计划/审计日志等）在 `.specify/` 命令工作区（2026-07-28 澄清）。
- 一般项目本地惯例优先；Spec Kit 自身 dogfooding 经用户指示采用激进基调（用户输入为期望态最高优先级来源）。
- 复用关联：028 Feedback Mechanism（自省步骤载体）、036 Dogfooding Practice（治理原则依据）；不新建循环机器。

## Related Files

- Feature Index: .specify/memory/features.md
- Feature Detail: .specify/memory/features/037.md
- 交叉引用: 047 Framework Material Hygiene(sanitize 只做 docs 引用存在性等正确性检查,内容编纂收敛移交本命令)

## Status Tracking

- **Draft**: Feature 登记，spec 033 产出并完成澄清。
- **Planned**: plan.md 及设计产物存在且 Constitution Check 无未豁免 Fail。
- **Implemented**: tasks.md 零 `[ ]` 且 verification.md 覆盖全部 SC-NNN 状态。
- **Ready for Review**: 全部 `[~]` 延期项解决或显式豁免，评审证据产出。
- **Completed**: 人工最终验收通过。

状态机权威定义见 `.specify/memory/features/036.md` § Canonical Status State Machine（单一事实源）。

## Related Specifications/Requirements

- Specification: .specify/specs/033-docs-command/requirements.md
  Quality Checklist: .specify/specs/033-docs-command/checklists/requirements.md
- Specification: .specify/specs/048-docs-reconcile/requirements.md（第四次 follow-up：命令层升级为目标结构驱动的三段式调协编排——目标结构声明（落 `.specify/docs/`）+ 差异分解为分型动作 + create-docs/improve-docs 双技能分发；requirements / clarify / plan / tasks 均已完成，implementation pending；阶段详情以本文件 `## Latest Review` 为准）
  Quality Checklist: .specify/specs/048-docs-reconcile/checklists/requirements.md
