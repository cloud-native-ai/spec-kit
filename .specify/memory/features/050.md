# Feature Detail: Proactive Flow Trigger

**Feature ID**: 050  
**Name**: Proactive Flow Trigger(主动触发机制)  
**Description**: 在每个受支持 agent 的必经指令路径上埋设主动触发点:每回合接续既有 constitution 合规分析做情境评估,主动建议"现在该执行哪个流程"并给出确切调用形式,使用户不必记得任何 `/speckit.*` 命令名或技能名;建议的采纳/拒绝被记录,同一规则连续采纳达阈值后晋升为免确认自动执行(破坏性流程永不晋升),触发规则本身作为数据依证据持续调优。  
**Status**: Implemented  
**Created**: 2026-09-07  
**Last Updated**: 2026-09-08

## Overview

本 Feature 承载需求 050 引入的主动触发机制。动因是一个具体的使用摩擦:框架的能力面已经很大(25 个 `/speckit.*` 命令 + 数十个技能),但**全部只能由用户主动回忆名称并手动触发**。这要求用户记住每个命令与技能的名字及其大致参数结构,心智负担随能力面增长而线性上升——能力越丰富,反而越难用。

反转思路是把"用户去找能力"改成"能力在必经路径上找用户":`specify init` / `/speckit.instructions` 生成指令文件时写入一段主动触发指令,经既有 symlink 模型抵达每个 agent 的必读文件;agent 每回合在同一趟分析里先完成既有的 constitution 合规检查、再据此判断当前项目状态下该执行哪个流程,并以一行非阻塞建议给出用途与**确切可复制的调用形式**。用户只需说"好"或忽略。

机制的另一半是**自我收敛**:建议的采纳/拒绝被记录为证据,同一(情境→流程)规则连续采纳达阈值(默认 3)后晋升为免确认自动执行,把"提醒"升级为"代办";而触发规则本身是数据而非散文,可依命中率/拒绝率/误报/漏报提议调优,经确认后生效。两条硬边界贯穿始终:**破坏性/不可逆流程永不晋升**(判据归确认门控治理,学习计数不得覆盖),以及**建议永不阻塞用户当前请求**(回流约束)。

## Latest Review

**2026-09-07 `/speckit.plan`(需求 050 → Feature 050,Draft → Planned)**:Constitution v1.11.0 ~~14/14 Pass,Complexity Tracking N/A~~ → **2026-09-08 订正为 13 Pass / 1 Partial(XIV),Complexity Tracking 已填**(见下方 analyze 条目;原判定把种子规则集误归入 `one-source-of-truth.md` 的合法副本类别)。设计取向是主动**收缩制品面**——零新命令、零新技能、零新确认门控、零新依赖、零 `src/` 逻辑改动,唯一新制品是一个 stdlib 引擎,其必要性由 Program-First 强制(计数/阈值/身份解析/漂移检出都是固定规则判定,不得交给 LLM)。

四项决定性设计裁定:① **管理面不引入命令**(P-Q1)——取 glossary 域同构形态(引擎 + 由触发段路由管理意图),是三个候选里唯一对已钉死计数(复杂命令 19、docs-step 16)、probe 注册表(internal 73)与门控预算全部零扰动的方案,且与"用户不必记名字"的特性论点自洽;② **种子规则取"provenance 副本 + 漂移检出"**(P-Q2)——`## Handoffs` 是散文,确定性解析不可行,故取"provenance 副本 + 机械漂移检出"形态,漂移由**被检出**取代被防止。**该形态的 Principle XIV 归类经 analyze 2026-09-08 订正**:它**不属** `one-source-of-truth.md` 的三类合法副本(guard copy 须"pinned inside a test"且"serves the build, never the reader",而种子是随包分发、被引擎每回合消费的运行时制品),故 XIV 记 **Partial** 并在 plan.md §Complexity Tracking 论证——与本文件 Implementation Notes 中"另撰一份即造出第二真源,违反 Principle XIV"的表述一致,原先两处自相矛盾已消除;③ **每回合评估的实现形态是每回合一次紧凑引擎调用**——agent 只传粗信号、引擎做匹配/计数/阈值,遥测因此机制自产,闭合了 clarify 轮发现的 SC-011/SC-012 分母缺口;④ **状态按变动频率切分跟踪边界**——`index.json` 跟踪(用户调优跨 clone 存活)、`telemetry.jsonl` 忽略(每回合 churn)。

计划期实测**推翻了两处既有记录**,即 Principle VIII 的直接应用:指令模板是 **14 个 `## ` 章节**而非 spec 原写的 17(17 是含 H1/`###` 的标题总数;14 才是关键数字,因 additive reconcile 只按 `## ` 切分),已就地订正 spec 锚点;`sync-mirrors.py --check` 当前 **exit 0 全绿**,Feature 049 记录的 git-fleet 6 MISS 基线**已失效**,故全树 exit-0 门禁可用、049 的 `--only` 范围化 workaround 不再需要。

最紧的机械约束是**确认门控整数余量为 0**:`test_confirmation_gates_sweep.py` 断言 `total ≤ 044 baseline × 25% = 23.25`,实测 total 23;而 `SCAN_DIRS` 含 `shared/`、`SCAN_ROOT_FILES` 含 `templates/*.md`,新增的指令章节与纪律文档**都在扫描范围内**,且 `shared/guidelines/proactive-trigger.md` 不匹配任何 `GOVERNANCE_PATH_PATTERNS` 故不享豁免。处置方式是**设计规避而非改写 baseline**:两份文案零 `BLOCKING_PATTERNS` 命中,该要求钉为 `trigger-section.md` C-6 与 `discipline-doc.md` C-3,并在 `discipline-doc.md` 给出五处最易误犯表述的合规替代写法。

**2026-09-07 `/speckit.tasks`(需求 050 → Feature 050,状态保持 Planned)**:生成 **51 项任务 / 7 个 Phase**(Setup 3 · Foundational 4 · US1 8 · US2 13 · US3 8 · US4 7 · Polish 8),13 项标 `[P]`、**40** 处任务行 `[blockedBy]` 显式依赖(analyze 订正:原记 39 含 2 处图例/散文行 = 37;补 T026/T044/T045/T049 的 blocker 后为 40),格式经程序校验(T001–T051 连续唯一、每行单行且含文件路径、故事标签放置正确、DoD 与 Completion Gate 零 checkbox 语法、blockedBy 无悬空引用)。**Tests Mode = ON**(Principle IV),并作出**混合型判定**:Principle VII 的 template-only 门(`constitution.md:95`)只豁免文档/模板制品面(指令章节、纪律文档、种子 JSON → 结构契约测试),而 `trigger-utils.py` 是可执行运行时代码,故单元测试与集成测试**强制**,不得以 template-only 为由豁免。测试面因此由 plan 的 3 个契约文件扩为 **8 个**(4 契约 = 一契约一文件以保 `[P]` 资格 + 1 单元 + 3 集成 = 按故事分文件),plan.md 的 Technical Context / Scale&Scope / Source Code 树 / Principle IV 证据行已同步订正。**关键路径约束**:`trigger-utils.py` 是单文件引擎(房式约定),US2/US3/US4 共改它 → 三故事的实现任务严格串行(T021→T023→T031→T038),这是本特性并行度的硬上限;测试文件按故事拆分故测试任务可跨故事并行。**MVP = US1 + US2**(两个 P1,且只有合起来才构成独立有价值的增量:US1 单独只是"多一段不产出建议的文字",US2 单独则"引擎能算但没有常驻触发位置")。**Feature 列表复审结论**:未暴露新 Feature、未使既有 Feature 失效;但发现一处**跨 Feature 交付效应** —— T011 补齐 `HERMES.md` 与 `.opencode/instructions.md` 两条 symlink,闭合的是 **Feature 022「所有官方支持工具获得完整初始化覆盖」范围内**的既有缺口(`_check_instructions` 对这两个 key 恒 fail),归属仍在 022、050 只是交付方,已同步记入 `features/022.md` 交叉引用。Completion Gate 8 项的检查命令已逐一存在性核验(13/13 路径与脚本存在,`run-tests.sh --names-out` 支持已确认)。

**2026-09-08 `/speckit.analyze` + 授权订正(状态保持 Planned)**:只读分析产出 **37 条发现**(1 CRITICAL / 7 HIGH / 19 MEDIUM / 10 LOW)。方法是本 Feature 值得复用的一条:因全部工件由同一会话撰写,**检测环节也委派给 fresh-context 子代理**(3 个通道:覆盖度与一致性 / 契约与数据模型内一致性 / 注册表与宪法对齐),再由**另一波 4 个子代理**按 step 5.5 独立验证每条 CRITICAL/HIGH(验证者只收到 finding 与证据位置,不接收检测推理)——结果 10 条中 1 条由 CRITICAL 降 HIGH、2 条由 HIGH 降 MEDIUM、7 条 confirm,零 reject,说明双向都在纠偏。

两条最重的:① **CRITICAL(宪法)** —— plan.md 把 Principle XIV 记为 Pass,依据是种子 JSON 属 `one-source-of-truth.md` 的"a literal pinned in a test to detect drift"类别;逐字核对该指南 L35–43 后判定**归类不成立**(guard copy 须"pinned inside a test"且"serves the build, never the reader",而种子是随包分发、被引擎每回合消费的运行时制品,守卫它的是另一个文件;另两类亦不符)。已改记 **XIV ⚠ Partial** 并补 Complexity Tracking 完整论证(需要理由 / 四条被否决的更简替代 / 五项缓解 / 残留代价),Constitution Check 由 14/14 Pass 变为 **13 Pass / 1 Partial**。**不以重新解释指南的方式清账**——若项目要承认"由漂移检出测试守卫的派生运行时数据"为第四类合法副本,须由该指南 owner 走独立修订。② **HIGH** —— 14 条 seed provenance anchor 中 **9 条不成立**(对全部 25 个 Handoffs 段做逐流程交叉扫描后证实:被引段落根本不含所声称的流程名,s14 的锚点更落在 Handoffs 段之外),使 FR-022 的派生前提、SC-013 与 `seed-derivation` C-6/C-7 在实现期不可满足。

订正措施(全部已落工件):**FR-022 的派生来源扩为两类**(`anchorKind ∈ {handoffs, owning-section}`,后者用于 `/speckit.feedback` 等在**任何** Handoffs 段中都不出现的流程,其 owner 是 `shared/workflow/feedback-step.md` §Threshold prompt protocol);**7 条 anchor 改指**真实点名该流程的模板;**命名情境 14 → 13**(删除 s14:无状态可触发 `/speckit.skills`,且它与 quickstart 场景 3 直接矛盾);**信号枚举 9 → 12**(新增 `checklist-absent` / `feature-index-absent` / `constitution-absent`,一并解决 s03/s13 同身份违反 V1.3 与 s05 越界);新增 **V1.5/V1.6**(匹配谓词语义 + 最具体优先消歧)。**契约条款 47 → 61**:`trigger-engine.md` 补 C-19(禁网络能力 import,机械钉住 FR-020)、C-20(会话级重复抑制,给 FR-008 一个机制而非散文)、C-21(引擎从不自行执行流程 → FR-019 的抢占在引擎侧结构上不可能)、C-22(错误码封闭集)、C-23(status payload schema);`discipline-doc.md` 把原先**无编号**的"各章节必须承载的规范内容"表提升为 **C-8…C-16**,使原先被路由到"只断言结构属性"的 C-1…C-7 的十余条 FR 真正获得钉桩。**`--compliance-done` 加入封闭 flag 集(19 → 20)**,补上 `complianceDone` 的输入通道——此前 SC-012 空洞、C-14 的 `ordering-violation` 分支为死代码。SC-005/011/013/015 口径订正(破坏性取 10 次强判据、SC-011 诚实声明分母为 `assess` 调用数、SC-013 按 `anchorKind` 分两类、SC-015 改为"追加即截断"的恒真不变量);**Measurement Sources 全节重写,15 条 SC 逐条具名产出任务**(此前 7 条 SC 无任何任务产出其度量)。tasks.md 补 T026 的 T023/T024 blocker(单文件引擎的镜像时序)、T044/T045/T049 的 blocker,DoD-4 豁免无写任务的 Mirror 第 6 行,门控字面量改为引用 T002 冻结前值。`feature-ref.md` 的 FR→契约映射全面重写(原表 FR-012/FR-019/FR-020 三条指向断言别的事的条款)。

**2026-09-08 `/speckit.implement`(需求 050 → Feature 050,状态保持 Planned —— 推进由 Pre-Status-Flip Gate 持有)**:**51 项任务 → 50 `[X]` + 1 `[~]`**(T050 dogfood,理由见 verification.md `deferred_tasks`),开放任务 0。**8 个新测试文件 / 313 个新测试全绿**(4 契约 = 一契约一文件:discipline-doc 44 · trigger-section 16 · seed-derivation 21 · trigger-engine 146;1 单元 38;3 集成 = 按故事分文件:promotion 21 · telemetry 12 · tuning 17),外加治理变更的 test_ask_record_repeat 18。**名字级回归在每个 phase 边界均为空**(`comm -13 baseline current` 输出 0 行;43 = 42 自有基线 + 1 外部归因)。

制品面如计划落地且**零扰动承诺经机械复核成立**:门控扫描 total **23 == T002 冻结前值**、violations **0**(两份新 shipped 文案零 `BLOCKING_PATTERNS` 命中,整数余量 0 得守);复杂命令 **19** / 简单 **4** / docs-step **16** 字面量未被改写(两文件 `git diff` 为空,47 条断言绿);probe internal **73**;`regen-command-copies.py --check` 与 `sync-mirrors.py --check` 均 exit 0(五对镜像全绿,`scripts` STRICT 对字节相同)。引擎实测 **1480 行 stdlib**、11 action / 20 flag / 10 错误码 / 9 阶段 / 12 信号,与契约封闭集逐项相符。

**FR-003 覆盖缺口已闭合**:补齐 `HERMES.md` 与 `.opencode/instructions.md` 后,干净临时根上 **8/8** 条 agent 指令路径全部 symlink 且 realpath 均指向唯一的 `.specify/instructions.md`,`_check_instructions` 对 **6/6** tool key 由 hermes/opencode 恒 `fail` 转为全 `pass`——该交付落在 **Feature 022** 范围内(归属不变,050 为交付方)。

**SC 结果:13 pass / 2 deferred**(SC-008 仅 part 2、SC-011),两者同因:T050 需 ≥20 个**普通工作**回合,而本会话是在**建造**机制而非在其下工作。已取得的实测:破坏性规则连续 **10** 次采纳 `promoted` 恒 false 且 `autoExecute` 恒 false(并以可逆规则第 3 回合晋升、第 4 回合 `autoExecute=true` 作对照,否则恒 false 的 bug 与正确守卫无法区分);一次拒绝使计数 3→0 且 `resetBy` 指向真实 eventId;`generate-instructions.sh` 真实运行后 `index.json` **sha256 逐字节相同**(再生零丢失);三态降级(缺失 / `{not json` / `schemaVersion` 999999)全部 exit 0 + `state-unreadable` + `degraded=suggest-only` + 13 规则计数归零重建 + 后续正常 assess 成功;窗口 50 下 **60/60** 次追加后行数恒 ≤50(追加即截断为恒真不变量,非仅轮转后成立),轮转前后规则集 sha256 相同,窗口收到 1 仍保留 `consecutive=3`;调优链 `tune` 识别噪声规则与漏报情境各附 `evidence{metric,value,sampleSize}`、单独运行**零变更**、`tune-apply` 走 `proposed→ratified→applied` 并写 `tuning.{ratifiedAt,evidenceRef,suppressedBy}` 使规则转 `suppressed` 后确实静默;小样本守卫以**降低 `--min-sample` 后同一证据即产出提议**证明拦阻来自守卫而非证据。漂移守卫经**受控实验**证明为活体(在一次性 `git worktree` 内进行,live 仓的命令模板零修改):两类 `anchorKind` 均触发失败并指名 ruleId/file/anchorKind/缺失 flow,且**负对照**(保留 flow 名与引文的合法改写)仍绿,证明它不是恒真断言。

**实现期订正两处设计工件前提缺陷**(均属"断言/文档与实测矛盾",非实现缺陷):① `quickstart.md` 场景 1 把 8 条 symlink 归给 `specify init` 一步——实测 `init` exit 0 且正确分发模板与纪律文档,但从不写 `.specify/instructions.md`、从不建任何 symlink(`src/specify_cli/__init__.py` 无生成器调用,其 L985 仅是 `_CORE_SPECIFY_ASSETS` 保留清单),真实管线是两步,已补步骤二并复跑 8/8 + 6/6;② **T044 / DoD-8 的 `tools.md` 子句为 VOID**——`refresh-tools.sh --json` 因缺 source flag 而 exit 1,且**全仓无任何写入方**(含 fragment 拼路径检查),该文件是 1984 行 **MCP 索引**,其三个枚举段自诞生(`15b13dba`)起只有一行描述、从无内容,故"经 refresh-tools.sh 再生"从未成立。Tool 记录与 `.specify/tools/*.json` 再生均已完成(`project.json` 含新引擎 6 处),`tools.md` **刻意未手改**(往 MCP 索引里塞项目脚本是错内容进错文件)。详见 tasks.md §Implementation-Period Corrections C-1。

**一项类级发现,已报告未修**:同一条 `tools.md` 虚假陈述散布于 **7 个表面**(`templates/instructions-template.md:132`、`templates/tools.md:14`、`templates/commands/tools.md:103`、`shared/definitions/tool-definitions.md:36`、`skills/create-tools/SKILL.md:65`、`skills/improve-tools/SKILL.md:53`、生成的 `.specify/instructions.md:147`),其中第一条是 ambient 故每个 agent 都会继承——这是 Principle XIV 的 rule of thumb 响亮失败(改一个事实需编辑 6 个框架文件),修复应由 owner 起、以指针形态扫其余五处,且**乘独立变更**而非本特性的提交。

**本特性直接催生了一次治理变更**(独立提交 `61813fbf`,不混入 050):引擎的 workspace-root 解析因本仓**同时含有框架源与 `.specify/` 运行时**而自匹配,把每次调用都解析到框架仓,导致测试把工作区状态写进真仓(生成 `.specify/memory/trigger/` 并追加 `.gitignore`)。该缺陷暴露的根因不是代码而是**可达性**:两顶帽子规则归属正确(宪法 XI + `dogfooding-definitions.md` §2.1)却从 ambient 表面不可达,故执行者径直撞上它所禁止的坑。修复按房式三层表面落地,并新增 `shared/guidelines/ask-record-repeat.md`(问一次 / 记下来 / 说三遍的知识生命周期理念,含与 XIV 的裁定:**指针形态可重复、内容形态仍禁止**)。

## Key Changes (planned)

1. **触发段进入指令模板与生成链**:`templates/instructions-template.md` 新增主动触发章节(挂载在 Documentation Map 常驻指令邻近位置),经 `generate-instructions.sh` 的 additive reconcile 落入 `.specify/instructions.md`,并由既有 symlink 抵达各 agent;引用式表达流程清单,不复制命令/技能枚举
2. **全 agent 必经路径覆盖修复**:补齐 `_INSTRUCTIONS_FILE_MAP` 声明却未生成的 `hermes → HERMES.md` 与 `opencode → .opencode/instructions.md`(或显式裁定不支持并从声明移除)——指令文件既是唯一通道,此缺口即等于这两个 agent 完全没有本机制
3. **每回合情境评估 + 两级证据预算**:默认只消费已在上下文中的环境信息(近零增量成本),不足时才升级到确定性探测程序的摘要输出;评估静默,**评估节奏与建议节奏分离**
4. **出厂种子规则集**:随 `templates/` 分发,使新项目第一天即产出建议;规则**从 25 个命令模板既有的 `## Handoffs` 条件式派生**,不另撰第二份 situation→flow 知识
5. **触发学习状态**:项目本地机器维护状态,承载规则集(含来源:种子/学习)、晋升态、建议事件与每回合遥测;与指令文件分离故再生不吞状态;遥测有保留窗口、可轮转
6. **阈值晋升 + 破坏性豁免**:连续采纳达阈值(默认 3,可配置)晋升为免确认自动执行,一次拒绝即重置;破坏性流程永不晋升;自动执行仍出三段式执行报告;用户可复位单条或全局降级
7. **规则证据驱动优化**:依命中率/拒绝率/误报/漏报提议收紧·抑制·新增,附证据(指标+样本数),经用户确认后生效并留可回查关联;样本不足不得提议变更
8. **接线**:glossary **8 个**新词条(主动触发点 / 情境评估 / 建议规则 / 阈值晋升 / 触发学习状态 / 情境身份 / 回合遥测 / 种子规则)、Tool 记录 `trigger-utils.py.md`、`docs/reference/` 参考文档、结构契约测试。**注**:本特性**不新增命令/技能**,故无 feedback probe 接线(probe internal objects 保持 73)

## Implementation Notes

- **确认门控预算余量为 0(本特性最紧的机械约束)**:`tests/contract/test_confirmation_gates_sweep.py` 断言扫描 `total ≤ baseline["total"] × 0.25`(baseline 取自 `.specify/specs/044-reduce-confirmation-flows/baseline.json`);实测当前 total **23**、violations 0,余量仅 0.25 → **任何一条新增 BLOCKING 匹配都会打爆该契约**。而 `scan-confirmation-gates.py` 的 `SCAN_ROOT_FILES = ("templates",)` **覆盖 `templates/instructions-template.md`**。本特性的文案天然是"建议/确认/询问"语义,极易命中 `BLOCKING_PATTERNS`(等待用户确认 / stop and confirm / yes-no 等)——触发段措辞必须按"非阻塞提示"写,且落地前须复跑扫描器验证 total 未增(承需求 049 同类记录)
- **`update-feature-index.sh` 是破坏性脚本**(承 Feature 049 记录):整体重写 `features.md`、输出 6 列(缺 Spec Path)、以目录 slug 与桩文本覆盖手写散文、重置全部 Status,而 `test_c3` 仍通过(静默损坏)。本 Feature 的索引行与表头计数一律**手工编辑**
- **首轮三项裁定不可拆分实现**:唯一通道(指令文件)、近零默认证据预算、每回合评估三者互相支撑——每回合评估的可负担性**完全依赖**默认预算近零;若只做每回合评估而默认走探测,即每回合跑脚本。规格已用 FR-005a 的"同时落地"约束锁定
- **只挂每回合轻量 constitution 义务**:框架的 constitution 遵从分两级——每回合的 Documentation Map 常驻指令(「ALWAYS check the relevant document from the map above first」)与 plan 期的完整逐原则枚举门控(`plan-template.md:31–33` GATE)。本特性只挂前者;把后者每回合化会同时违反 Principle IX 与 token 效率纪律(FR-005b ②)
- **情境身份必须是粗粒度受控词表**:以完整制品状态指纹作身份会使指纹近乎每次唯一 → 连续计数永不到阈值 → 晋升与 US3 整体失效。词表扩展只经用户确认通道,禁止 agent 临场造词
- **晋升计数是规则上的聚合态**:不得由原始遥测行重算,否则一次遥测轮转即清空全部学习结果(SC-015 为判别该缺陷而设)
- **种子规则派生而非撰写**:`## Handoffs` 已是 situation→flow 知识的既有真源(25/25 命令模板覆盖);另撰一份即造出第二真源,违反 Principle XIV。种子与学习规则同构,只在来源与可覆盖性上有别;**用户已确认的调优不得被后续种子更新覆盖回退**
- **漏报为已接受风险**:指令文件是唯一通道,无确定性触发保证(长上下文稀释、agent 忽略常驻指令都会导致整回合零评估)。补偿控制是漏报检测(用户手动调用某流程而当时无建议 → 提议新增/收紧规则),使漏报可观察可收敛而非静默失效
- **计数-阈值机制复用既有范式**:与 `feedback-utils.py` 的 `resolve_threshold` / `should_prompt` 同构(优先级:显式配置 > 环境变量 > 存储 > 默认),不新建平行引擎(FR-021 单引擎坍缩)。标识符预留已核查:`PROACTIVE` / `AUTO_TRIGGER` / `SUGGEST*` 零占用;须避让 `SPECKIT_FEEDBACK_THRESHOLD`、`should_prompt`、`resolve_threshold`、`--action introspect-register`、`INTROSPECTION_DIRNAME`、`AUTO-GENERATED` 标记、`GIT_WORKFLOW_START/END`
- **术语不得复用既有两词**:项目词汇表里"埋点/插点"锚定 **Feedback Probe**(wrap-up 事实捕获,记录**已发生**运行)、"自省"锚定 **Feedback Introspection**(需求 047,对已积累条目的深加工)。本 Feature 用**主动触发点 / 情境评估**,与之分属不同概念
- **shipped-surface 中性**(承 049 记录):`templates/` 与 `shared/` 内禁止出现 `Feature 0NN` / `requirement 0NN` / `.specify/specs/0NN-`;特性归属只写进本文件、Tool 记录、docs 与 spec 目录

## Future Evolution Suggestions

- **原生 hook 通道**:首轮已裁定指令文件为唯一通道(全 agent 通用、零适配成本)。若漏报证据积累到不可忽视,可另立特性为支持 hook 的 agent 增设会话级确定性触发——前置条件是先建立各 agent hook 能力矩阵(框架当前完全不掌握,`AGENT_CONFIG` 唯一能力位是 `requires_cli`);该矩阵与 Feature 022 的 Future Evolution #1 同源,宜合并考虑
- **两级情境身份**:粗身份用于计数 + 细条件用于触发。当前裁定粗粒度即够,升级路径保留,待 plan/实现阶段出现"粗粒度抹平关键差异"的真实证据再付该复杂度
- **跨项目规则共享**:当前明确排除(学习状态恒项目本地、不自动外传)。若未来出现"同类项目共用规则基线"的需求,需先解决隐私与人工送达纪律
- **遥测的自省消费**:每回合遥测与建议事件是 Feature 028/047 feedback 域之外的第二类行为证据,未来可作为 `/speckit.review` 或 introspection 的输入源(当前不接线)

## Related Files

- Feature Index: .specify/memory/features.md
- Feature Detail: .specify/memory/features/050.md
- 需求 050: .specify/specs/050-proactive-flow-trigger/requirements.md
- 质量检查清单: .specify/specs/050-proactive-flow-trigger/checklists/requirements.md
- 邻近 Feature(**消费关系,非归属**):
  - **032 Task Complexity Rubric** — 最接近的结构先例:同为嵌入 `.specify/instructions.md`、经 `/speckit.instructions` 非破坏投递的行为指令,且同样自成一个 Feature 而非绑 008(本 Feature 归属判定的决定性先例)
  - **008 Instructions Command** — 投递载体(生成触发段所落的指令文件)
  - **001 Unify Command Handoffs** — 能力前身:其 `## Handoffs` 条件式是本 Feature 种子规则的**派生来源**;但 Handoffs 静态且命令域,只在已调用某命令之后可见
  - **022 AI Tools Support** — 拥有 FR-003 的议题(全 agent init 覆盖完整性)
  - **028 Feedback Mechanism** — 计数-阈值机制与事件记录的既有范式来源
  - **046 Confirmation Gate Governance** — 破坏性判据与回流约束的权威来源(FR-011 / FR-007 只消费不修改)

## Status Tracking

- **Draft**: 达成(2026-09-07)——需求 050 已创建并绑定本 Feature;两轮澄清完成(首轮三项:通道/证据预算/评估时机;第二轮五项:Feature 绑定/种子规则/阈值语义/情境身份/遥测缺口),残留 `NEEDS CLARIFICATION` 0;26 FR / 15 SC / 4 user stories / 7 key entities
- **Planned**: 达成(2026-09-07,2026-09-08 经 analyze 订正)——`plan.md`(Constitution v1.11.0 **13 Pass / 1 Partial(XIV,已论证)**,Complexity Tracking 已填)、`data-model.md`(7 实体 + 2 状态机 + 受控词表 **13** 命名情境 / 9 阶段 / **12** 信号 + 升级判据 P1–P5 + 校验规则 **6 组 / 32 条** V1…V6)、`contracts/` 四份共 **61 条款**(trigger-section 12 / trigger-engine **23** / seed-derivation 10 / discipline-doc **16**)、`quickstart.md`(6 场景)、`feature-ref.md` 齐备;无独立 `research.md`(Phase 0 findings 以 D-1…D-10 内联,全部仓库内部实测)
- **Implemented**: 达成(2026-09-08)——Pre-Status-Flip Gate 五项机械检查全通过:① 延后任务已转换(T050 `[~]` 并附 `deferred` 注释)② 开放任务计数为 **0** ③ verification.md 15 条 SC 全部有状态行 ④ `deferred_tasks=T050` 已登记 ⑤ 全部通过后才推进状态。**Completion Gate 8 项逐条对当前树复跑**(不采信「任务看起来做完了」):GATE-1 名字级回归 diff 为空 · GATE-2 `sync-mirrors --check` exit 0 · GATE-3 `regen-command-copies --check` exit 0 · GATE-4 门控 total **23** == T002 冻结前值 / violations **0** / sweep 18 绿 · GATE-5 开放任务 **0** · GATE-6 SC 唯一 token **15** · GATE-7 probes exit 0 / internal **73** · GATE-8 分类与 docs-step 47 绿。**DoD Status = green**,但 **DoD-8 含一条 VOID 子句**(`tools.md` 无法「经 refresh-tools.sh 再生」:全仓无写入方,且该脚本缺 source flag 即 exit 1),已按实测记录为 void 而非当作已满足,详见 tasks.md §Implementation-Period Corrections C-1。SC **13 pass / 2 deferred**(SC-008 仅 part 2、SC-011,同因 T050 需 ≥20 个普通工作回合)
- **Ready for Review**: 延后任务全部解决或显式豁免,且已产出评审证据
- **Completed**: 最终验收通过

## Related Specifications/Requirements

- Specification: .specify/specs/050-proactive-flow-trigger/requirements.md
  Quality Checklist: .specify/specs/050-proactive-flow-trigger/checklists/requirements.md
