# Feature Detail: Team Management

**Feature ID**: 027  
**Name**: Team Management  
**Description**: Dedicated team domain for multi-agent configuration — `/speckit.team` as the sole entry point, `create-team` + `improve-team` skills, and single-agent vs team separation.  
**Status**: Implemented  
**Created**: 2026-07-13  
**Last Updated**: 2026-08-04

## Overview

Team Management establishes a first-class **team** domain that owns everything multi-agent, mirroring the single-agent domain. It introduces `/speckit.team` as the sole entry point with three modes (create / modify / run), backed by `create-team` (renamed from `organize-agents`, retaining orchestration + execution) and a new `improve-team` skill. The multi-agent Conceptual Model and the inherently multi-agent authoring constructs (EEI triad, team-supervisor, orchestration templates) move out of `create-agent`, leaving the single-agent skills strictly single-agent. Teams persist as `.specify/teams/<slug>.team.md` so they can be modified and run across sessions. The run mode renders the team's static (Role×Stage×Type) and dynamic (pattern/parallelism/flow diagram) structure and executes only after explicit user confirmation.

## Latest Review

Planning completed for requirement `036-team-summary` via `/speckit.plan` (2026-08-04). Requirement advances Draft→Planned; Feature 027 stays `Implemented` per the no-status-regression gate.

**Phase 0 was execution-verified, not asserted.** The `summarize-project` script chain was run against a form derived from the real `cws-workspace-cluster` team: `validate → project-db --load → --check → progress-engine` all exit 0, with `status: ready` and `missing_required: []`. This proves the mechanism's central feasibility claim — the invoked skill's R-tier (project name, baseline date, and at least one of work-items/milestones non-empty) is satisfiable from tracked team artifacts alone, with zero manual form entry. The same run produced four further findings: the DDL rejects identifiers containing spaces or non-ASCII characters with exit 3 (so derived identities must be hashed, not titled); the engine itself returns `progress_pct = null` rather than 0 when there is no countable basis; absent schedule material suppresses the Gantt chart upstream; and `coverage` is null unless the form supplies it — which would fail the CG-COVERAGE gate on every run that draws a work-breakdown diagram, a requirement with no corresponding FR and now carried in `research.md` § O.

**Three design decisions.** (1) Cumulative state is authoritative in a new tracked per-team ledger `items.jsonl`, not in the derived SQLite database — so `--load` rebuild stays idempotent and a summary is reproducible from tracked artifacts alone. (2) A new ledger file is justified because `STATE.md` has no machine-parseable schema (measured: the two continuous teams use entirely different section shapes — prose vs status buckets), `run-log.jsonl` carries only per-cycle aggregate counts and exists only for continuous teams, and sub-agent result manifests live in git-ignored paths that provenance rules forbid. (3) FR-029's presentation-level aggregation maps onto the invoked skill's existing `coverage.granularity_truncated` field, keeping breakdown-diagram nodes within the upstream threshold of 15 without any change to that skill.

**Constitution v1.9.0**: 12 Pass, 1 Partial. Principle IX (Framework Scope Discipline) is Partial and justified in Complexity Tracking — the plan adds one generator script and one per-team data file, both compelled by FR-008/FR-026 and each with the simpler alternative rejected on measured evidence.

**Carried forward to `/speckit.tasks`**: the `coverage`-always-emitted requirement has no FR and needs its own task; and no `serial` or `parallel` team exists in-repo (the four existing teams are 2 continuous + 2 iteration), so SC-001's four-pattern coverage needs fixture teams.

**Scope revision — dual index (2026-08-04, after plan.md and tasks.md existed).** The user directed that summary output also land in `.specify/project/goal/<goal-slug>/`, making `.specify/teams/` indexed by team and `.specify/project/goal/` indexed by goal. Handled under the Scope Revision Protocol: requirements.md amended in place with the directive recorded verbatim, downstream artifacts regenerated by hand, residual-reference sweep run, checklist re-validated. Three decisions: **1 goal : N teams** (the goal directory aggregates every team declaring the same `goal_slug`, so a goal outlives team rebuilds and renames); **explicit `goal_slug` in `team.md` frontmatter** (chosen over text derivation because FR-019 permits deliberate goal edits, which would otherwise orphan directories — same explicit-over-derived reasoning already applied to work-item identity); **goal side holds the sole complete summary** while the team side keeps run info only (satisfies the invoked skill's self-containment red line without doubling chart rendering).

One further finding came from executing the aggregation rather than reasoning about it: two teams' independently issued `TI-0001` values **collide** on load, because `entity_ids` is a global namespace enforced by primary key — measured exit 3 with `item_id='TI-0001' 在本实体内重复`. Team-prefixed identifiers (`team-a.TI-0001`) were then measured to load cleanly. So the aggregation layer must namespace identifiers by team, and that prefix conveniently carries the per-team attribution FR-033 requires. Designing from the intuition "per-team IDs are unique enough" would have blocked every multi-team goal.

Artifact deltas: requirements.md 29→36 FR, 12→15 SC, 5→6 user stories, `[[STR-006]]` added, `[[STR-001]]` repointed; research.md gained E-7 and D-7; plan.md re-checked the 13 principles (still 12 Pass / 1 Partial, with II/VIII/IX evidence restated rather than carried over); data-model.md gained goal identity and aggregation semantics; three of four contracts amended; tasks.md 101→120 with a new US6 phase.

### Implementation Review — 026-agent-team-management

Implementation completed for requirement `026-agent-team-management` via `/speckit.implement`. All 33 tasks closed (`[X]`, zero open/deferred). `/speckit.team` (create/modify/run) added; `organize-agents` renamed to `create-team` and `improve-team` created; Conceptual Model extracted to the team domain; multi-agent templates relocated; single-agent skills purified; per-tool command copies + `.specify/instructions.md` refreshed. Pre-Status-Flip Gate passed (0 open tasks, verification.md records pass for SC-001…SC-007). Feature-027 guard suite green (79 passed); baseline `organize-agents` active-tree references reduced 42 → 0. Pre-existing, unrelated contract failures (handoff-chain, skill-home-workdir, agent-env-config, agent-specific-config) confirmed identical on a clean HEAD checkout — no regression introduced.

### Planning Review — 026-agent-team-management

Planning completed for requirement `026-agent-team-management`. Clarifications resolved (feature bind 027; triad + team-supervisor move to team domain; team domain owns execution). Plan, data-model, four contracts (command, create-team, improve-team, migration), and quickstart generated. Constitution Check: all 7 principles pass.

## Key Changes

1. Add `/speckit.team` command (source `templates/commands/team.md`) with create/modify/run modes and a run-mode preview→confirm→execute gate.
2. Rename skill `organize-agents` → `create-team`; add new skill `improve-team`; complete the team create→improve lifecycle.
3. Extract the Conceptual Model out of `create-agent/SKILL.md` into `create-team/references/conceptual-model.md` (single source of truth).
4. Relocate multi-agent templates (team-supervisor, triad stages, triad/parallel/serial orchestration, workflow schema) from `create-agent/templates/` to `create-team/templates/`.
5. Re-scope `create-agent`/`improve-agent` to single-agent modes only; scope `/speckit.agents` off team operations.
6. Persist teams under `.specify/teams/<slug>.team.md` (framework-internal; no per-tool symlink).
7. Update references/registries/tests (NON_DECLARABLE set, create-skills non-declarable list, docs/agents, per-tool command copies) and regenerate `.specify/instructions.md`.

## Implementation Notes

- Backward-incompatible rename is acceptable (internal framework, no external consumers); zero `organize-agents` references may remain in active paths.
- `create-team` owns both team definition and execution (inherited from `organize-agents`); the command's `run` mode adds the confirmation gate.
- Guard tests (Test-First): zero dangling reference, single Conceptual Model, single-agent purity, command routing, skill presence.
- Feature 019 (Agents Command) scope is reduced (multi-agent responsibilities move to 027); note this in 019 when 027 is implemented.

## Future Evolution Suggestions

- Team versioning and templated team presets (e.g., a canned "SDD delivery team").
- Team run history/observability under `.specify/teams/` for improve-team evidence. — **now specified by `036-team-summary`** (Draft): the team treats itself as a project and periodically drives `summarize-project` to emit a cumulative status summary.
- Composable teams (a team member that is itself a sub-team).

## Related Files

- Feature Index: .specify/memory/features.md
- Feature Detail: .specify/memory/features/027.md

## Status Tracking

- **Draft**: Requirement specified and clarified (026-agent-team-management).
- **Planned**: plan.md, data-model.md, contracts/, and quickstart.md exist; Constitution Check has no unjustified Fail rows.
- **Implemented**: tasks.md has zero open `[ ]` rows and verification.md records a status row per Success Criterion. (current)
- **Ready for Review**: deferred tasks resolved/waived; review evidence produced.
- **Completed**: final acceptance.

## Related Specifications/Requirements

- Latest Specification: .specify/specs/036-team-summary/requirements.md
- Latest Quality Checklist: .specify/specs/036-team-summary/checklists/requirements.md
- Historical Specification: .specify/specs/.archive/026-agent-team-management/requirements.md
- Historical Quality Checklist: .specify/specs/.archive/026-agent-team-management/checklists/requirements.md
- Historical Plan: .specify/specs/.archive/026-agent-team-management/plan.md
- Historical Data Model: .specify/specs/.archive/026-agent-team-management/data-model.md
- Historical Contracts: .specify/specs/.archive/026-agent-team-management/contracts/{team-command,create-team-skill,improve-team-skill,team-migration}-contract.md
- Historical Quickstart: .specify/specs/.archive/026-agent-team-management/quickstart.md
