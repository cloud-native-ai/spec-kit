{
  "baselineRunId": "ev-20260907-041346-merge-skills",
  "targetFinding": "ev-001",
  "expectedSignal": {
    "signalKey": "userCorrections",
    "direction": "reduce"
  },
  "change": "Improved skills/merge-skills (SKILL.md + references/coverage-verification.md + references/decommission-and-wiring.md + scripts/coverage_probe.py) from executed evidence. Script: implemented the UPPER-CASE-enum and 3-digit-status-code token classes that SKILL.md and the reference already claimed but no pattern extracted; added exit 4 when the source path is unreadable or yields 0 tokens (previously printed 'coverage clean'); added --allow FILE so a verified false positive is recorded instead of forcing the target doc to be distorted; documented exit 2; filtered UPPER-CASE fragments of UNDER_SCORE constants; stale allow-entry pruning. Docs: gate now keyed to the exit code and to *unresolved* misses in both directions (added the pre-merge-target regression recipe via a temp file, recording that process substitution fails); token-class table corrected to match the code with the lowercase-enum hole stated explicitly; decommission gained the missing 'declarations that name the source by slug' surface (re-point agent skills: frontmatter / Skill Enablement rows; register the retirement in the host obsolete-asset registry) plus a Mirror sync section absorbing the spec-kit command sequence moved out of SKILL.md Phase 8; dropped the misdirected 'create-skills §Report' pointer; added a Resources table.",
  "note": "Degenerate-evidence case: the baseline run's 10 findings were project-level (Present/Exercised) and contained no merge-skills-specific defect entry — the skill was created ~2h earlier and had never been executed. The driving evidence was therefore (a) direct execution of the skill's own coverage_probe.py against constructed positive/negative/reverse cases, and (b) the creation-session record (auto-memory task_summary_experience note + host transcript), which showed the creation smoke test was positive-only and so passed for the wrong reason. Before/after was observed in-session, not deferred: enum-and-status-only source material against an EMPTY target reported 'fact tokens extracted: 0 ... coverage clean' exit 0 before the change, and 'extracted: 6; unresolved missing: 6' exit 3 after; a nonexistent source path reported exit 0 'coverage clean' before and exit 4 after. The creation session's own smoke case was re-run and still yields exactly one miss (MISSING_TOKEN_XYZ, exit 3). A fresh subagent reading only the updated decommission reference independently named both new surfaces and successfully ran the documented pytest selector (49 collected). Contract suite -k 'skill or runtime_mode': 14 failures, byte-identical to a git-worktree baseline at HEAD (only the timing line differs) -> zero regression. Next-run comparison should use a real merge execution; until then this intervention stays Unobserved on the userCorrections signal."
}
