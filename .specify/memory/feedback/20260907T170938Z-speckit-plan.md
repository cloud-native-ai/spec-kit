---
id: "20260907T170938Z-speckit-plan"
unit_id: "/speckit.plan"
unit_type: "command"
run_id: "050-proactive-flow-trigger-plan-20260907"
scope: "local"
probe: "speckit-plan-wrapup"
kind: "internal"
slice: "commands"
feature: "050-proactive-flow-trigger"
partial: false
created: "2026-09-07T17:09:38Z"
summary: "Phase 0/1 全流程走通并到达 wrap-up。Setup 前已核实 plan.md 不存在(首次创建而非 amend,避开 create-new-plan.sh 无条件覆写的已知陷阱)。Phase 0 用两个并行 Explore 通道做定向代码探索(投递链 / 引擎与测试约定), findings 以 D-1…D-10 内联,无独立 research.md(全为仓库内部实测,无外部来源)。"
---

## Review
Phase 0/1 全流程走通并到达 wrap-up。Setup 前已核实 plan.md 不存在(首次创建而非 amend,避开 create-new-plan.sh 无条件覆写的已知陷阱)。Phase 0 用两个并行 Explore 通道做定向代码探索(投递链 / 引擎与测试约定), findings 以 D-1…D-10 内联,无独立 research.md(全为仓库内部实测,无外部来源)。Constitution Check 按 outline 要求**从 constitution.md 动态枚举** 14 条原则(I–XIV,v1.11.0)逐条成表,未用硬编码清单、未继承他 spec 的陈旧原则;结果 14/14 Pass、Complexity Tracking N/A,Phase 1 后复评仍全 Pass。Principle XII 的 Tool 复用门已实际执行:查 .specify/memory/tools/(9 条),核实最接近的 feedback-utils.py 经其 Tool 记录声明作用域为 feedback 存储 + probe 注册表、不覆盖本能力,故新建引擎是预期结果,并把新 Tool 记录列为实现期义务。两处设计分叉(管理面形态、种子制品形态)按 Phase 0 的'underdetermined → interview, never fabricate'规则提交用户裁定而非自行发明,答案写入 Technical Context 与 contracts。Phase 1 产出 data-model.md(7 实体 / 2 状态机 / 14 命名情境 / P1–P5 / V1…V6)+ contracts ×4 共 47 条款 + quickstart.md 6 场景 + feature-ref.md。Post-Generation Quality Gate 执行:契约与 quickstart 零 deliberation marker(grep 核实);CLI 示例因引擎未实现无法执行,改为由 trigger-engine.md C-9/C-10/C-11 封闭枚举 + 末节显式声明测试将解析 quickstart 代码块来钉住;顺带核实 quickstart 里 specify init 的 --here/--ai/--force/--ignore-agent-tools 四个 flag 均真实存在(src/specify_cli/__init__.py:2551–2589),并补 --force 使走查非交互。step 5 完整性门禁三项全过(残留占位符 0、单一 H1、Requirement→Feature 戳在位、模板自指 Note 行已移除)。Feature 集成:Draft → Planned,features/050.md 的 Status/Latest Review/Status Tracking 与 features.md 索引行均手工更新,索引一致性程序复核(50 行/header 50/050=Planned),门控扫描保持 total 23 violations 0。plan 期实测推翻两处既有记录(指令模板 14 个 ## 章节而非 spec 原写的 17;sync-mirrors --check 已 exit 0 全绿、049 的 git-fleet 6 MISS 基线失效),前者已就地订正 spec 锚点并按 append-only 追加第三轮 Clarifications(条目 9→13)。

## Optimization Points
- **"Summarize after, not before" 的模板注释挡不住预写计数——应把待回填单元格做成占位符形状,让既有的完整性门禁机械检出**:plan 模板在 `## Phase 1: Design Artifacts Summary` 上方有明确注释("never pre-write counts here"),outline step 4 也写了"Summarize after, not before"。我仍然在同一次写作过程里预写了 `13 命名情境` 与 `约 9 个 action`,产物落地后实测为 **14** 与 **11**,不得不再做回溯订正(Technical Context 的 Scale/Scope、Source Code 树注释、Phase 1 表各一处)。根因:该表位于文档**末尾**,但填充发生在**同一次**写作 pass 里,注释很容易被顺着写下去的惯性越过;而 step 5 的完整性门禁只检查三项(残留 `[UPPER_SNAKE_CASE]` 占位符、单一 `# Implementation Plan:` 标题、`Requirement → Feature` 戳),**不检查计数是否被预写**。建议把模板里待回填的单元格写成占位符形状(如 `[BACKFILL_ENTITY_COUNT]`),使其自动落入 step 5 检查 (a) 的既有 grep 范围——预写于是变成机械可检出的门禁失败,而不是靠自觉。这是一处"纪律写在注释里、但没有机械执行点"的典型缺口。
- **Post-Generation Quality Gate 的 "pinned by a contract test" 分支,对尚未存在的工具不可直接执行,而命令未给出可操作形态**:该门要求 quickstart / contracts 里每条可执行 CLI 示例"要么本阶段真实执行一次,要么被契约测试钉住有效性"。但 plan 阶段引擎根本还没实现,分支 (a) 恒不可用,只能走 (b);而 (b) 在此语境下含义不明——契约测试也还不存在,"被钉住"到底指什么?本次处置是:在 `contracts/trigger-engine.md` 里把 action 与 flag 钉为封闭枚举(C-9 / C-10 / C-11),再补一节显式声明「quickstart.md 中每条调用的 action ∈ C-9、每个长选项 ∈ C-10、标识符实参满足 C-11,由 `tests/contract/test_trigger_engine.py` 以解析 quickstart 代码块 → 逐条校验的方式机械断言」。这个"在契约里写明测试将解析 quickstart 代码块"的手法可复用,建议直接写进门禁条目作为分支 (b) 的标准形态;否则每个 plan 运行都要重新发明一次,且很可能退化成"示例无人钉、实现后静默漂移"——Feature 048 把 `quickstart 14/14 CLI 示例真实执行通过` 单独记一笔,正说明这件事平时是缺位的。
