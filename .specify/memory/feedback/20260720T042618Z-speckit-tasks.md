---
id: "20260720T042618Z-speckit-tasks"
unit_id: "/speckit.tasks"
unit_type: "command"
run_id: "031-task-complexity-rubric-tasks-20260720T122618"
scope: "local"
feature: "031-task-complexity-rubric"
partial: false
created: "2026-07-20T04:26:18Z"
summary: "Clean run: Tests Mode correctly resolved ON (Principle IV Test-First/Contract-Driven), realized as Layer-1 structural assertions since the feature is template-only. Generated tasks.md with 13 tasks or"
---

## Review
Clean run: Tests Mode correctly resolved ON (Principle IV Test-First/Contract-Driven), realized as Layer-1 structural assertions since the feature is template-only. Generated tasks.md with 13 tasks organized by user story: Setup (baseline+anchor), Foundational (author the failing structural contract test C-1..C-10), US1/MVP (author + byte-identical mirror of the rubric section, then green test), US2 (fresh-generation presence verify), US3 (non-destructive refresh + user-precedence verify), Polish (full-suite regression, SC status, optional dogfood). DoD uses the - DoD-N: prefix (no checkbox violations); all 13 rows well-formed with sequential IDs and correct story labels. Honored the plan's decision that no command/per-tool-mirror edits are needed, so US2/US3 are verification-only. Feature 032 maintained at Planned; breakdown exposed no new features.

## Optimization Points
- For a documentation/prompt-framework feature (Constitution Principle IX; Principle IV marked Partial in plan.md), the tasks-template's runtime-app vocabulary ("Models → Services → Endpoints", "tests must FAIL before implementation") maps awkwardly — there are no models/services/endpoints and the "test" is a static content assertion on a template. /speckit.tasks could detect the template-only/doc-feature case and switch to a doc-feature task taxonomy (author-section / mirror-parity / render-verify / refresh-verify), instead of forcing the reader to manually reshape the app-oriented phase structure.
