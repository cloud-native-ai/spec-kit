---
id: "20260811T171551Z-speckit-todo"
unit_id: "/speckit.todo"
unit_type: "command"
run_id: "session-naming-park-2026-08-12"
scope: "local"
feature: "none"
partial: false
created: "2026-08-11T17:15:51Z"
summary: "Input was a design idea (agent session custom naming) with no TODO-block tie; binary mode detection had no legal path, so the run paused and asked the user, who chose to (a) extend /speckit.todo with "
---

## Review
Input was a design idea (agent session custom naming) with no TODO-block tie; binary mode detection had no legal path, so the run paused and asked the user, who chose to (a) extend /speckit.todo with a Park Mode and (b) park this idea as the first record. Edited canonical templates/commands/todo.md (frontmatter, mode detection, Collection Step 3 recall of parked store, Park Mode Workflow section, Safety Rule 6, Handoffs), fanned out via sync-mirrors.py (--check exit 0, mirror + 4 per-tool copies verified), created .specify/memory/todo/ store (hit and fixed the root-owned-dir container gotcha), parked 20260812-agent-session-custom-naming.md.

## Optimization Points
- Mode detection was binary (insert/collection) so a free-floating idea arrived disguised as a Collection-Mode run and had no legal path — the scanner would have run against a design request. Added Park Mode (--park or idea-shaped input): capture into .specify/memory/todo/ with parked/promoted/dropped lifecycle. Suggestion for the template: mode detection heuristics for "idea-shaped" input are judgment-based; when ambiguous between Collection and Park, the command should ask (as was done this run) rather than default to scanning.
