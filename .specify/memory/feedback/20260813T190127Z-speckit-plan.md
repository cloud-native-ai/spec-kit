---
id: "20260813T190127Z-speckit-plan"
unit_id: "/speckit.plan"
unit_type: "command"
run_id: "040-agent-metadata-portability-plan-2026-08-13"
scope: "local"
feature: "040-agent-metadata-portability"
feature_id: "044"
partial: false
created: "2026-08-13T19:01:27Z"
summary: "无 $ARGUMENTS 的规划完整走完:setup 一次成功;Phase 0 以单个 Explore 子代理完成 8 项设计取证(占位符白名单、preset 断言形态、软链接语义测试清单、模板 vs shipped 重叠关系、MIRROR_PAIRS、命令模板引用点、json 清单先例、preset 枚举文件表),据此定案 9 条设计决策(D1~D9,含中立键集、映射真源形态、渲染清单与备份机制"
---

## Review
无 $ARGUMENTS 的规划完整走完:setup 一次成功;Phase 0 以单个 Explore 子代理完成 8 项设计取证(占位符白名单、preset 断言形态、软链接语义测试清单、模板 vs shipped 重叠关系、MIRROR_PAIRS、命令模板引用点、json 清单先例、preset 枚举文件表),据此定案 9 条设计决策(D1~D9,含中立键集、映射真源形态、渲染清单与备份机制、迁移替换法、Meta 初始集);plan.md 按 constitution v1.9.1 动态渲染 13 条原则(12 Pass / 1 Partial,IX 已在 Complexity Tracking 论证);Phase 1 落盘 data-model + 4 contracts + quickstart + feature-ref,审议标记扫描与完整性门(0 占位符/单标题/stamp)通过;Feature 044 Draft→Planned 并 stage。quickstart 的 CLI 示例按质量门钉住方式声明(渲染类由集成测试钉,镜像类为既有命令)。

## Optimization Points
- **探索取证与设计决策的耦合应显式编号**:本轮 Phase 0 取证(Explore 子代理 8 问)直接改写了两个预设设计 —— capacity 模板与 shipped 定义是"薄脚手架 vs 全量副本"的重叠关系(导致 D6 定为替换而非并存)、以及 AGENTS.md 注册表缺 ux-analyst 的既有漂移(顺带进机械联动清单)。这些取证→决策的因果目前只散落在 Technical Context 的 D1~D9 里,plan 模板没有"取证编号 → 决策"的引用栏。建议 plan 模板在 Phase 0 小节加一张 findings→decisions 映射小表,避免实现期回看时无法追溯某决策是基于哪条实测事实。
- **Constitution Check 的动态派生执行顺畅但证据列容易写虚**:13 条原则逐行渲染无遗漏,但 Evidence 列若不受约束容易退化成口号(如"符合 SDD")。本轮自律为"指向具体设计产物(D 编号/契约条款/SC 编号)",建议把这个要求直接写进 plan 模板该列的注释里。
- **token-efficiency**:Phase 0 用单个 Explore 子代理把 8 个验证问题一次带回(含占位符白名单逐字引用、MIRROR_PAIRS 原文、preset 枚举文件清单),主上下文只消化摘要;contracts 落盘后用一次 grep 批量扫描审议标记与完整性门,未逐文件重读。可避免开销:plan.md 一次性整文写入后未做差异复核 —— 若模板有残留占位符将依赖完整性门兜底,本次门通过了,但"写后即扫"比"写完再扫"更省一轮。
