---
id: "20260814T171220Z-speckit-tasks"
unit_id: "/speckit.tasks"
unit_type: "command"
run_id: "041-refactor-feedback-probe-20260814-tasks1"
scope: "local"
feature: "041-refactor-feedback-probe"
feature_id: "028"
partial: false
created: "2026-08-14T17:12:20Z"
summary: "完整运行:前置检查 → 测试模式判定(原则 IV 强制 ON + template-only 门,双轨测试均置顶)→ 34 任务生成(Setup2/Foundational3/US1:3/US2:3/US3:4/US4:6/US5:4/US6:5/Polish4;8 个 [P])→ DoD/GATE 六门(含 reconcile 守恒与外部隔离两专项)→ 格式 lint 全过 + 自检脚本发现并修"
---

## Review
完整运行:前置检查 → 测试模式判定(原则 IV 强制 ON + template-only 门,双轨测试均置顶)→ 34 任务生成(Setup2/Foundational3/US1:3/US2:3/US3:4/US4:6/US5:4/US6:5/Polish4;8 个 [P])→ DoD/GATE 六门(含 reconcile 守恒与外部隔离两专项)→ 格式 lint 全过 + 自检脚本发现并修正一处 blockedBy 前向引用(cleanup 与模板撰写换号)→ Pin Hygiene 落实(49 与 18→19 为计数即契约,其余 len 派生)→ T024 迁移计划设为唯一用户停点。契约测试文件与 contracts 一一对应;共享 cli 测试文件的跨 story 串行化已在 Dependencies 注明。

## Optimization Points
- 本混合形态 spec(引擎代码+模板/定义)暴露模板空档:Task Generation Rules 只给了 app 形态(models→services→endpoints)与 doc-feature 形态两套顺序,混合 spec 需要 per-story 选择 taxonomy——本次靠 plan.md 的 Mirror Obligations 与 contracts 逐条人工映射。建议 tasks 模板 Notes 增加一句:「混合形态 spec 按 story 分别套用 app 或 doc-feature taxonomy,以 plan.md Mirror Obligations 行为镜像任务的第一来源」。
- 初版任务表出现 blockedBy 前向引用(T017 依赖编号更大的 T018),靠生成后自检脚本发现并换号修正。建议 tasks.md 模板的 lint 检查清单(DoD 格式规则旁)加一条「blockedBy 目标编号 MUST 小于本任务编号」的可 grep 校验,把这类倒置在生成阶段拦下。
