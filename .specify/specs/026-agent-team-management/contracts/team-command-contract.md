# Contract: `/speckit.team` Command

**Feature**: 027 Team Management | **Spec**: [requirements.md](../requirements.md)

`/speckit.team` is the single entry point for all team operations (FR-001). It recognizes intent, routes to the owning team skill, and never renders templates inline. It MUST NOT serve single-agent operations, and single-agent commands MUST NOT serve team operations (FR-003).

## Modes

`/speckit.team` exposes exactly three modes.

| Mode | Intent signals | Delegates to | Definition |
|------|----------------|--------------|------------|
| **create** | "创建团队", "组织一个团队", "new team", "build a team" | `create-team` | Author a new team (static + dynamic structure) from user needs; persist as `.specify/teams/<slug>.team.md` (or run one-shot). |
| **modify** | "修改团队", "调整团队", "improve/adjust team", "优化 team" | `improve-team` | Load an existing team and apply targeted, evidence-based changes. |
| **run** | "运行团队", "执行团队", "run/execute team", "跑一遍" | `create-team` (execution path) | Load a team, render its structure, confirm, then orchestrate. |

## Routing rules

1. Parse `$ARGUMENTS` + conversation/repo context to classify the intent as `create`, `modify`, or `run`.
2. `create` → `create-team`; `modify` → `improve-team`; `run` → `create-team` execution.
3. **Ambiguous / unsupported** intent → MUST report the three recognized capabilities (create / modify / run) and request the missing intent. MUST NOT guess silently (FR-002).
4. `modify`/`run` targeting a team that does not exist under `.specify/teams/` → report "team not found" and offer to `create` (FR-010; Edge Case).

## Run-mode contract (preview → confirm → execute)

The **run** mode MUST follow this sequence and MUST NOT execute before confirmation:

1. **Load** the target team from `.specify/teams/<slug>.team.md`.
2. **Render Static Structure** — the roster as a Role × Stage × Type matrix: each member agent, its role, its kind/type (Worker/Meta), and its lifecycle (persistent/temporary).
3. **Render Dynamic Structure** —
   - the collaboration `pattern` (parallel / serial / team-loop);
   - the **parallelism** (parallel: degree + territories; serial: DAG stage order; team-loop: threshold, max_iterations, regression_limit, quality dimensions);
   - an **execution flow diagram** (a textual/mermaid/PlantUML flow showing dispatch/handoff/loop edges).
4. **Confirmation gate** — present the two structures and ask the user to confirm. The team executes **only** on explicit confirmation.
   - On confirm → orchestrate per the team's pattern (delegating to create-team's execution engine).
   - On decline → stop without executing; optionally suggest `modify`.

## MUST / MUST NOT

- MUST expose exactly the three modes above; MUST route each to the correct skill.
- MUST render both static and dynamic structure before any run.
- MUST block execution until explicit user confirmation (no auto-run).
- MUST NOT be reachable via `/speckit.agents`, and MUST NOT handle single-agent authoring.
- MUST report capabilities on ambiguous intent instead of guessing.

## Generated command artifacts

Source: `templates/commands/team.md`. Per-tool command files are regenerated by init/instructions into each tool's command dir (`.qoder/commands/speckit.team.md`, `.claude/commands/speckit.team.md`, `.github/prompts/speckit.team.prompt.md`, `.opencode/command/speckit.team.md`, `.qwen/…`, etc.). No hardcoded command allow-list requires editing.
