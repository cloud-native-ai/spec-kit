# Contract: Command Complexity Classification (FR-006)

**Feature**: 028 Feedback Mechanism | **Type**: Classification contract

Fixes which of the 25 commands are **complex** (carry feedback) vs **simple** (excluded). This is the authoritative source for the convention + conformance tests.

## Classification rule

A command is **complex** — and therefore MUST carry the feedback step — iff it meets ANY of:

- **(a)** it invokes scripts or command-line tools (e.g. `scripts/bash/*.sh`, `scripts/python/*.py`);
- **(b)** it produces an artifact consumed by another flow;
- **(c)** it consumes an artifact produced by another flow.

A command meeting NONE of these is **simple** and MUST NOT carry the feedback step (FR-006, FR-007).

> Note: `create-*` / `improve-*` authoring is delivered as **skills**, which carry feedback under the all-skills rule (FR-002). The thin command wrappers listed as "simple" therefore do not double up on feedback.

## Classification table (25 commands)

| Command | (a) scripts | (b) produces for | (c) consumes from | Class | Rationale |
|---------|:---:|:---:|:---:|-------|-----------|
| `requirements` | ✅ | plan, clarify, tasks, analyze, checklist, review | — | **Complex** | Emits `requirements.md`; runs create-new-requirements script. |
| `clarify` | ✅ | plan | requirements | **Complex** | Consumes + amends `requirements.md`. |
| `plan` | ✅ | tasks | requirements, research, constitution | **Complex** | Emits plan/data-model/contracts; runs create-new-plan script. |
| `tasks` | ✅ | implement | plan | **Complex** | Emits `tasks.md`; runs prereq script. |
| `implement` | ✅ | review | tasks | **Complex** | Consumes `tasks.md`; emits code + verification. |
| `analyze` | ✅ | — | requirements, plan, tasks | **Complex** | Cross-artifact consistency read. |
| `checklist` | ✅ | — | requirements | **Complex** | Consumes spec; runs script. |
| `review` | ✅ | — | requirements, plan, tasks | **Complex** | Global report over multiple artifacts (owns global scope; still self-reviews locally). |
| `research` | ✅ | plan | — | **Complex** | Emits `research.md`; runs research script. |
| `instructions` | ✅ | (compat symlinks) | project state | **Complex** | Runs generate-instructions script; regenerates instructions + symlinks. |
| `tools` | ✅ | — | — | **Complex** | Orchestrates tool scripts / external CLIs. |
| `skills` | ✅ | — | — | **Complex** | Runs skills-utils / create-new-skill scripts. |
| `todo` | ✅ | — | — | **Complex** | Runs detect/scan scripts over files. |
| `agents` | ❌ | — | — | **Simple** | Self-contained `.agent.md` authoring; no script, no per-run flow I/O. |
| `constitution` | ❌ | — | — | **Simple** | Interactive editing of a shared registry doc; not a per-run producer/consumer flow. |
| `feature` | ❌ | — | — | **Simple** | Feature-index registry editing; not a scripted / chained flow. |
| `team` | ✅ | run, goal coordinate, improve-team | goal | **Complex** | Reclassified 2026-09-23 (introspection-20260923T120035Z#F-02, user-adjudicated). Not a thin wrapper: `templates/commands/team.md` invokes 4 distinct scripts directly (`scripts/python/goal-utils.py`, `skills/create-team/scripts/match-team-preset.py`, `skills/create-team/scripts/verify-territory-disjoint.py`, `build-summary-input.py`), emits `.specify/teams/<slug>/team.md` + `runs/` consumed by `/speckit.team run`, `/speckit.goal coordinate` and `improve-team`, and consumes the bound goal's `## Targets`. All three criteria hold, so the delegation rationale below the table does not apply. |
| `derive` | ✅ | plan, clarify, next derive run | constitution, research, handed-in sources, moves library | **Complex** | Added 2026-09-24 (Known-Gap closure). (a): invokes `.specify/scripts/python/derive-utils.py` in its own Stages 2–7 (`init` / `moves-list` / `probe-links` / `moves-add` / `validate` / `stats`) — 1 distinct flow engine; the `feedback-utils.py` mention sits only inside `## Feedback` and was excluded from the count. (b): emits `.specify/derive/<topic-slug>/derive.md`, whose `A-<k>` elements `/speckit.plan` cites by identity (Boundary section: 依赖方向单向) and whose `## Open Questions` `/speckit.clarify` consumes, plus the project-level `.specify/derive/moves.md` library the next run's `moves-list` projection reads. (c): consumes the project constitution at Stage 5.5 criteria collection, and a handed-in source list that may seed from a feature's accumulated `research.md` citations (Handoffs Before). |
| `docs` | ✅ | instructions, memory-record, next docs run | repo evidence, feedback store, target declaration | **Complex** | Added 2026-09-24 (Known-Gap closure). (a) holds **by delegation**: the template names `docs-utils.py` once, as part of what its `create-docs` skill owns ("and `docs-utils.py` automation"), not as an invocation in its own steps — the same shape the `skills` and `tools` rows above are already ✅ for; (b)+(c) carry the class independently, so the verdict does not rest on (a). (b): produces `.specify/docs/target-structure.md` (reused unchanged by Stage 1.3 of the next run), the four per-run artifacts (观察快照 / 干跑计划 / 审计日志 / 残差报告) whose `pending` items the next run resumes, and the converged `docs/` tree + root entry files that `/speckit.instructions` reads to refresh the Documentation Map; `memory-record` persists the reconcile decisions (Handoffs After). (c): consumes repository shape evidence at Stage 1.1 and content-action evidence drawn from deterministic findings, verified staleness, user correction **or the feedback store** (Stage 2). |
| `feedback` | ✅ | improve-skills / improve-agent / improve-team / improve-tools / improve-docs, requirements, upstream maintainer, next feedback run | every unit's `## Feedback` entry, probe registry, `feedback/` inbound bundles | **Complex** | Added 2026-09-24 (Known-Gap closure). (a): `feedback-utils.py` is this command's **own flow engine**, not the `## Feedback` boilerplate mention — invoked at Routing step 3 (`probes` / `probes --validate` / `probes --reconcile` / `map` / `status` / `list`), A3 (`introspect-register`), A6 (`dispose`), Path B (`status` / `list` / `dispose` / `package` / `cleanup` / `upstream` / `mark-submitted`) and Probe Injection (`probe-inject`), alongside `unzip` / `mktemp` / `rm` / `ls` / `git log` in A1–A5. `regen-command-copies.py` (Routing step 2) is *cited* as an explanation of `rewrite_paths`, never invoked — excluded from the count. (b): produces `.specify/memory/feedback/introspection/<report-id>.md`, `consume-log.md` rows (read back by A1's already-consumed cross-check on the next run), `cleanup-log.md`, `probes/ext-<slug>.md`, `probe-map.md`, and the Path B package zip; A4 routes findings to five `improve-*` channels plus `/speckit.requirements`. (c): consumes the entries every skill and complex command records, the probe registry `/speckit.instructions` installs (Handoffs Before), and inbound bundles in `feedback/`. |
| `goal` | ✅ | team, team run, goal coordinate | team.md inline goal, check-prerequisites output | **Complex** | Added 2026-09-24 (Known-Gap closure). (a): invokes 2 distinct scripts in its own Outline — `.specify/scripts/bash/check-prerequisites.sh --json --paths-only` (step 1) and `scripts/python/goal-utils.py` (step 4: `list` / `validate` / `check-statement` / `targets` / `create` / `objective` / `criteria` / `status` / `migrate`); `feedback-utils.py` sits only inside `## Feedback` and was excluded. (b): emits `.specify/goal/<goal-slug>/goal.md` with the engine-rendered `## Targets`, which `/speckit.team` binds via `goal_slug` (Handoffs After) and `/speckit.team run` reports against — a terminal-state Target reported by a run is a review bifurcation this command adjudicates. (c): `migrate <team-slug>` reads that team's inline goal from its `team.md`, and `coordinate` writes ratified territory back into each affected `team.md`. |
| `history` | ✅ | constitution, instructions, memory-record, next history run | host AI agent CLI session store, own manifest | **Complex** | Added 2026-09-24 (Known-Gap closure). (a): invokes 2 distinct scripts in its own steps — `scripts/bash/collect-history.sh` (frontmatter `scripts:` block → Step 1 `{SCRIPT}` locate, Step 2 `--action extract`, Step 5 `--action manifest-update`) and `scripts/python/history-utils.py` (the pluggable `STORE_RESOLVERS` engine Step 1.3 names); `feedback-utils.py` sits only inside `## Feedback` and was excluded. (b): emits `.specify/history/README.md` + `00-cross-cutting-lessons.md` + `NN-<theme-slug>.md`, routed by its own Handoffs After to `/speckit.constitution`, `/speckit.instructions` and `memory-record`, plus `.manifest.json` which makes the next run incremental. (c): consumes the host tool's entire session store read-only (Safety Rule 1) and its own prior manifest. Note: carries `## Feedback` but **no** `## Documentation` section, so it is legitimately absent from `test_docs_step_injection.py`'s list. |
| `interview` | ✅ | plan, tasks, implement, goal, team, resumed interview | check-prerequisites JSON, phase artifact, features index, constitution, research | **Complex** | Added 2026-09-24 (Known-Gap closure). (a): invokes 2 distinct scripts in its own Outline — `scripts/bash/check-prerequisites.sh --json --paths-only` (frontmatter `scripts:` block → step 1) and `.specify/scripts/python/interview-utils.py` (step 7: `init` / `add` / `frontier` / `conflicts` / `answer` / `retract` / `status`, which owns frontier computation, cycle rejection and retraction propagation); `feedback-utils.py` sits only inside `## Feedback` and was excluded. (b): emits the ledger — `<REQUIREMENTS_DIR>/interview-log.md` in a feature context, else `.specify/memory/session/interview-<topic-slug>.md` — whose decision records (ID, `dependsOn`, artifact span) and header mode a resumed run reads and never renegotiates; it also writes through to the target artifact that artifact's owning command then consumes (Handoffs After: converged `requirements.md` → `/speckit.plan`, `plan.md` → `/speckit.tasks`, `tasks.md` → `/speckit.implement`, goal → `/speckit.goal` / `/speckit.team`). (c): consumes `REPO_ROOT` / `BRANCH` / `REQUIREMENT_ID` / `REQUIREMENTS_DIR` / `FEATURE_SPEC` / `IMPL_PLAN` / `TASKS` from the prereq script, the current phase's primary artifact, `.specify/memory/features.md` index rows, and targeted `constitution.md` / `research.md` / `docs/` excerpts (step 6). |
| `sanitize` | ✅ | docs, instructions, review, sync-mirrors, next sanitize run | git history, path-existence map, framework materials | **Complex** | Added 2026-09-24 (Known-Gap closure). (a): invokes 2 distinct engines in its own Stages — `.specify/scripts/python/sanitize-utils.py` (Stage 2 `collect`, Stage 3 `record`, Stage 4 `status`, Stage 6 `apply`) and `.specify/scripts/python/feedback-utils.py` at Stage 7 with `--lifecycle-point "gate-sanitize-destructive-cleanup"`, a **flow** gate-probe record distinguishable from the `## Feedback` boilerplate by its own section mapping. `sync-mirrors.py --write` / `regen-command-copies.py` (Stage 4) are delegate *suggestions* handed to the user, not invocations — excluded. (b): emits `.specify/memory/sanitize/cleanup-plan.json` (the `confirmed` flag the engine's `apply` gates on) plus the accumulating findings ledger whose stable IDs (`sha1(category\|target)[:12]`) the next run merges into, and archives to `.specify/archive/` preserving relative layout. (c): consumes git log summary lines and path-existence mappings as its evidencePack — semantic staleness detection auto-degrades in a non-git environment (Stage 1) — over the framework's own parked ideas, drafts, indexes, mirrors, compat links and `docs/` tree. |
| `session` | ✅ | team run report | host AI agent CLI session store, own session-meta.json | **Complex** | Added 2026-09-24 (Known-Gap closure). (a): invokes `skills/archive-session/scripts/export.py` directly in Outline step 4 (three invocation forms) — the engine its Positioning names as owning session location, tool detection, meta extraction and budget verdicts; 1 distinct flow script, and `feedback-utils.py` sits only inside `## Feedback` and was excluded. (b): emits the `.session-export/<name>/` bundle (`session-meta.json` + `SESSION.md` + copied raw records), which its Handoffs After route into `/speckit.team` run reports' mapping tables via the `<team-slug>--<run-stamp>--<member-role>` naming convention. (c): consumes the host AI agent CLI's session store copy-only (Boundaries: records are copied, never moved or mutated), and step 5 consumes its own `session-meta.json` `over_summary_budget` verdict to decide whether to write a skeleton summary. |

**Result**: 22 complex (feedback-bearing) · 3 simple (excluded).

## Maintenance

- If a command later starts invoking a script or joining the producer/consumer chain, re-run this rule and update both this table and the command template.
- The conformance test in `tests/contract/` MUST read this table (or an equivalent list) so classification drift is caught automatically.
- **Known gap closed 2026-09-24** (recorded 2026-09-23): the table then covered 17 commands while `templates/commands/` held 25, and the tests' own `COMPLEX_COMMANDS` lists — not this table — were the operative enumeration. All 8 missing commands (`derive`, `docs`, `feedback`, `goal`, `history`, `interview`, `sanitize`, `session`) have now been measured against the three criteria and classified **Complex**; every one of them invokes at least one script in its own flow steps, so no reclassification was needed to reach agreement with the templates. `tests/contract/test_feedback_command_classification.py` now **derives** its `COMPLEX_COMMANDS` / `SIMPLE_COMMANDS` from this table, discharging the bullet above: this table is the operative enumeration. Coverage is machine-checked with an anti-vacuity floor (`MIN_COMMANDS`), so "zero gap because the table is complete" stays distinguishable from "zero gap because no row parsed", and a 26th template with no row fails the suite.
- **Residual, not fixed here (outside this contract's scope)**: `templates/commands/feedback.md` and `templates/commands/sanitize.md` place `## Documentation` *before* `## Feedback`, against the adjacency/order norm `033-docs-command/contracts/docs-step-injection.md` C-3 pins (`test_docs_command_template.py::test_c3_section_order` fixes that order for `docs.md`; `test_feedback_step_reference_form.py` names it "the Feedback → Documentation → Handoffs adjacency"). Both are therefore held out of `test_docs_step_injection.py`'s list, which counts 19 while 21 templates measurably carry `## Documentation`. The repair is the two templates' section order — do not paper over it by weakening C-3 or by widening that test's assertions.
