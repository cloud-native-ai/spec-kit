统一约定：所有 `/speckit.*` 命令模板中的 `$ARGUMENTS` 字段为**可选补充输入**。
- 当 `$ARGUMENTS` 为空时：命令必须仍然按模板中定义的完整主流程执行，基于当前仓库/feature/spec/plan/tasks/constitution 上下文做决策，不得因缺少 `$ARGUMENTS` 而直接中止。
- 当 `$ARGUMENTS` 非空时：在不违背宪法 MUST 原则的前提下，将其视为本次调用的附加约束、偏好或聚焦点，在决策与输出中优先考虑。

本说明仅作为命令模板目录内的维护提示，不参与运行时逻辑。