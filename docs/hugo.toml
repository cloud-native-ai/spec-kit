# Hugo configuration for this project's documentation space.
# Generated by create-pages (scaffold-hugo.py). docs/ is the Hugo project root AND
# the single content source: every content path is *mounted*, never copied, so the
# Markdown tree stays pure and no file is duplicated.
#
# Edit this file freely — only the managed mount block at the bottom is regenerated.

baseURL = "/"
locale = "en"
title = "Spec Kit"

# Repo-native relative Markdown links (`../concepts/overview.md`) keep working on the
# rendered site because layouts/_default/_markup/render-link.html resolves each link
# through Hugo's page graph and emits the target's real URL — so the default pretty-URL
# scheme is kept, and no link rewriting is needed in the Markdown itself.
# relativeURLs keeps every generated URL relative, so the built site also works when
# hosted under a sub-path and when browsed straight off disk.
relativeURLs = true
disableKinds = ["taxonomy", "term", "RSS"]

[markup.goldmark.renderer]
  unsafe = true   # documentation legitimately contains raw HTML (<details>, <br>, badges)

[markup.highlight]
  noClasses = true   # self-contained styling; no chroma stylesheet to ship

[markup.tableOfContents]
  startLevel = 2
  endLevel = 3

[params]
  description = "Spec-Driven Development toolkit documentation"

# >>> speckit:mounts — generated by scaffold-hugo.py — re-run after adding a docs/ directory
[[module.mounts]]
  source = "static"
  target = "static"

[[module.mounts]]
  source = "archive"
  target = "content/archive"

[[module.mounts]]
  source = "archive"
  target = "static/archive"
  excludeFiles = ["*.md", "**/*.md"]

[[module.mounts]]
  source = "concepts"
  target = "content/concepts"

[[module.mounts]]
  source = "concepts"
  target = "static/concepts"
  excludeFiles = ["*.md", "**/*.md"]

[[module.mounts]]
  source = "contribute"
  target = "content/contribute"

[[module.mounts]]
  source = "contribute"
  target = "static/contribute"
  excludeFiles = ["*.md", "**/*.md"]

[[module.mounts]]
  source = "decisions"
  target = "content/decisions"
  excludeFiles = ["index.md", "**/index.md"]

[[module.mounts]]
  source = "decisions"
  target = "static/decisions"
  excludeFiles = ["*.md", "**/*.md"]

[[module.mounts]]
  source = "notes"
  target = "content/notes"
  excludeFiles = ["index.md", "**/index.md"]

[[module.mounts]]
  source = "notes"
  target = "static/notes"
  excludeFiles = ["*.md", "**/*.md"]

[[module.mounts]]
  source = "reference"
  target = "content/reference"
  excludeFiles = ["index.md", "**/index.md"]

[[module.mounts]]
  source = "reference"
  target = "static/reference"
  excludeFiles = ["*.md", "**/*.md"]

[[module.mounts]]
  source = "tasks"
  target = "content/tasks"
  excludeFiles = ["index.md", "**/index.md"]

[[module.mounts]]
  source = "tasks"
  target = "static/tasks"
  excludeFiles = ["*.md", "**/*.md"]

[[module.mounts]]
  source = "tutorials"
  target = "content/tutorials"

[[module.mounts]]
  source = "tutorials"
  target = "static/tutorials"
  excludeFiles = ["*.md", "**/*.md"]

[[module.mounts]]
  source = "assets"
  target = "static/assets"

[[module.mounts]]
  source = "decisions/index.md"
  target = "content/decisions/_index.md"

[[module.mounts]]
  source = "notes/index.md"
  target = "content/notes/_index.md"

[[module.mounts]]
  source = "reference/history/index.md"
  target = "content/reference/history/_index.md"

[[module.mounts]]
  source = "tasks/index.md"
  target = "content/tasks/_index.md"

# <<< speckit:mounts
